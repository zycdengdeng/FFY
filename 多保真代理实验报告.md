# 多保真物理残差代理 · 样本效率实验报告

> 生成式代理设计框架 · 第一块基石 · 2026-08 · 配套:`outputs/surrogate/mf_combined.png`、`src/stage6_surrogate/`

## 1. 动机与主张

课题组前期工作(AESCTE)的代理流程:9 次 ANSYS FE → 黑盒 GPR → 贝叶斯优化。FE 昂贵导致样本极少,黑盒 GPR 在小样本下学不好整个设计空间。

**主张**:我们手里有仿生连杆的**解析动力学模型**(免费、无限次调用)。让代理不学"性能本身",只学"高保真与校准后解析模型的**残差**"——残差比全函数平滑得多,同样的高保真预算,精度显著更高。这是"仿生物理先验进入代理架构"的第一步。

## 2. 实验设置

- **低保真 LF**:1-DOF 力臂折算线性落震(解析,微秒级);
- **高保真 HF**:2-DOF 平面连杆拉格朗日动力学——大转角非线性、位形相关惯量、液压式平方阻尼、膝部行程限位撞块(这些正是 FE/多体有而线性模型没有的物理);本实验以它替代 FE 做受控验证,方法对 FE 即插即用;
- **输入空间**:3 维(L1, r2, r3,蓝本设定)与 **5 维(几何 + 触地速度 v0∈[2,4] m/s + 质量 m∈[150,260] kg)**——工况维度是做"预判式预设"设计所必需;
- **对比方法**(同一 HF 预算 n∈{4,6,9,12,16,24,32},30 次重复,测试集 200 点):
  A. 校准解析模型(a·yLF+b,2 参数)· B. 黑盒 GPR(蓝本做法)· C. 物理残差 MF(本方案)· D. LF 作特征的 GPR;
- 指标:NRMSE;LF–HF 相关:3 维 0.982,5 维 0.968。

## 3. 结果(NRMSE,均值)

**3 维(蓝本设定)**

| n | 4 | 6 | **9=蓝本预算** | 12 | 16 | 24 | 32 |
|---|---|---|---|---|---|---|---|
| 校准解析 | 0.260 | 0.220 | 0.214 | 0.211 | 0.205 | 0.199 | 0.197 |
| 黑盒 GPR | 0.824 | 0.490 | **0.322** | 0.194 | 0.142 | 0.060 | 0.035 |
| 物理残差 MF | 0.258 | 0.212 | **0.200** | 0.179 | 0.156 | 0.091 | 0.051 |
| LF 作特征 | 0.507 | 0.270 | 0.274 | 0.203 | 0.158 | 0.049 | 0.029 |

**5 维(几何+工况)**

| n | 4 | 6 | **9** | 12 | 16 | 24 | 32 |
|---|---|---|---|---|---|---|---|
| 校准解析 | 0.397 | 0.308 | 0.288 | 0.285 | 0.282 | 0.274 | 0.268 |
| 黑盒 GPR | 1.140 | 1.113 | **0.703** | 0.572 | 0.452 | 0.281 | 0.213 |
| 物理残差 MF | 0.396 | 0.312 | **0.283** | 0.269 | 0.242 | 0.205 | 0.165 |
| LF 作特征 | 0.731 | 0.439 | 0.328 | 0.284 | 0.225 | 0.200 | 0.134 |

## 4. 发现(四条,含诚实的交叉现象)

1. **蓝本自己的设定里(3 维、n=9):连 2 参数校准的解析模型(0.214)都优于训练出的黑盒 GPR(0.322)**;物理残差 MF 再降到 0.200(误差 −38%)。
2. **加入工况维度后(5 维、n=9):黑盒 GPR 基本失效(0.703,接近"猜均值"),物理残差 MF 保持 0.283——2.5 倍精度**;黑盒要 24–32 个样本才追平物理先验 9 个样本的水平,**约 3 倍样本效率**。
3. **诚实的交叉**:3 维、大样本(n≥16)时黑盒反超残差法(LF 先验开始"帮倒忙")——物理先验的价值精确地位于**昂贵评估、样本稀缺**的区间,这正是 FE 场景。
4. **架构建议**:小预算用"残差 MF",大预算切换"LF 作特征";两者可统一在一个自动切换的框架里(后续工作)。

## 5. 对课题的意义与下一步

- 这是"生成式代理框架"三步走的第一步:**多保真残差代理(本报告)→ 时序响应代理(接生物着陆律做轨迹目标)→ 条件逆向生成(目标/物种 → 设计+柔顺律)**;
- 对组里前作是直接增强:同样 9 次 FE,换上物理先验代理,精度即提升;工况扩展(5 维)则是做"预判式预设"设计的必要基础;
- 下一步:①把 HF 换成组内 ANSYS 验证真 FE 场景;②时序代理(POD+GPR 预测整条冲击时程);③把柔顺律参数并入设计空间,为逆向生成铺路。

## 6. 复现

```bash
python src/stage6_surrogate/mf_experiment.py --out outputs/surrogate            # 3 维
python src/stage6_surrogate/mf_experiment.py --out outputs/surrogate5d --dim 5  # 5 维
```

模型定义见 `src/stage6_surrogate/models.py`(LF/HF 场景参数、设计空间边界均在其中)。

---

# 附加实验(同日):HF 升级为 Exudyn 开源多体仿真

## A. 为什么升级

原实验的 HF 是自制 2-DOF 解析模型,可被质疑"玩具"。调研 Python 开源仿真生态后选定 **Exudyn**
(C++ 内核多体动力学,2023 Multibody System Dynamics 期刊论文,pip 即装,纯代码可服务器运行),
重建高保真:三段刚体杆(分布惯量)+ 髋/膝/踝转动副+扭簧阻尼 + 机身质量竖直滑轨 +
**足端球-地面 penalty 接触(含库仑摩擦,可弹跳/滑移)**——落震试验台标准拓扑。
单次仿真 ~2s(CPU),响应 2–9g,零失效;LF–Exudyn 相关 0.93(3维)/0.95(5维),
线性校准后残差仍有 ~43%——比玩具 HF(0.98)难得多,是"delta 可学性"的真实考场。

## B. 结果(NRMSE)

**Exudyn · 3 维**:n≤6 物理残差胜;**n≥9 黑盒反超**(0.13 vs 0.24)——真接触响应面在低维空间很光滑,
黑盒 9 点即学好;而 delta 混入 LF 的结构性偏差,反而更难学。

**Exudyn · 5 维(几何+工况)★ 决胜局**

| n | 4 | 6 | **9=蓝本预算** | 12 | 16 | 24 | 32 |
|---|---|---|---|---|---|---|---|
| 黑盒 GPR | 1.13 | 0.92 | **0.58** | 0.29 | 0.25 | 0.12 | 0.076 |
| 物理残差 MF | 0.58 | 0.36 | **0.27** | 0.25 | 0.20 | 0.13 | 0.084 |

n=9 时物理残差 **2.2×** 精度,领先维持到 n≈16,大样本收敛打平。

## C. 四组实验的统一结论(诚实版)

1. **物理先验的价值 = f(维度↑, 样本↓)**:维度越高、高保真预算越紧,先验越值钱;
   在"设计×工况、昂贵仿真、~9 样本"的现实区间(蓝本场景+预判式设计所需),收益 2–3×。
2. **delta 可学性的条件**:LF 的误差必须比 HF 本身更平滑。低维+中等预算+光滑响应面时,
   黑盒直接学更好(Exudyn 3 维 n≥9)——此时应自动切换或用 LF-作特征。
3. **工程建议**:框架按预算/维度自动选择{残差 MF | LF 特征 | 纯黑盒},而非一招鲜。
4. 全链开源可复现(numpy + sklearn + Exudyn),蓝本的 ANSYS 样本可作为更高层直接插入。

## D. 复现

```bash
pip install exudyn scikit-learn
python src/stage6_surrogate/mf_experiment.py --out outputs/surrogate_exu   --hf exudyn --ntest 120 --reps 12
python src/stage6_surrogate/mf_experiment.py --out outputs/surrogate_exu5d --hf exudyn --dim 5 --ntest 120 --reps 12
```

总图:`outputs/surrogate/mf_grand.png`(四面板)。Exudyn 模型:`src/stage6_surrogate/hf_exudyn.py`。

---

# 追加:先验质量实验(LF=2-DOF vs 1-DOF, HF=Exudyn)

问题:"残差学不好,是不是先验约束不够多?" 实验:把 LF 从 1-DOF 换成运动学更丰富的 2-DOF。
结果:corr 0.933→0.937,n=9 残差 NRMSE 0.237→0.247——**没有改善**。
原因:两个解析先验缺的是**同一块物理(真实接触:滑移/弹跳/动态力臂)**,而这正是与 Exudyn 分歧的主因;
2-DOF 的"额外物理"(平方阻尼/限位)Exudyn 模型反而没有——它不是更像,是错得不一样。
**结论:先验的价值在于是否补上主导性缺失物理,而非自由度多少(复杂度≠相似度)。**
工程含义:选 LF 应选"含接触的最便宜模型"(如粗网格多体/FE),同族物理 delta 才平滑。
展望:physical AI(神经算子+多保真)用于时序代理阶段;control generation(条件生成柔顺调度)
即路线第三步的准确形态,与"预判式预设"直接衔接。复现:`--hf exudyn --lf 2dof`。

---

# 追加 2:生物大数据进场(Duong 12 段 → 7 维实验)

用 Duong 4 物种 12 段实测的**触地姿态分布**(θA 113–160°,θK 118–158°;对比我们原用常数 120°/90°,
且物种间系统不同:鸭踝 152° vs 天鹅 132°)替换拍脑袋范围,构成 7 维「几何×工况×姿态」空间(HF=Exudyn):

- **corr(LF,HF) 从 0.93 崩至 0.44**——解析先验对姿态效应的刻画定性错误(近伸直腿的力臂几何 vs 真接触分歧);
- **n=9 全体方法失效(NRMSE≈0.97)**,n=32 仅至 0.26——蓝本预算在生物真实空间中绝对不足;
- 生物数据的角色实证:不是装饰,是**质检员**——一进场即暴露先验盲区并划定问题真实难度。

下一步(由数据指定):① LF 换"含接触的粗粒度多体"(同族物理);② 姿态分桶校准;
③ 敏感性检验:膝角为隐藏关节标注(按可观测性原则存疑),仅用可信踝角的 6 维重跑。
复现:`--hf exudyn --dim 7`(姿态范围硬编码自 Duong 数据统计)。
