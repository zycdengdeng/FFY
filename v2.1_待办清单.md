# v2.1 待办清单

本文件汇总 v2 定稿后陆续发现、但**刻意不在 v2 里改**的问题。
不改的统一理由:改设计盒的形状或宽度会让已有的 12 万次落震训练集失效,
cVAE 需要重训,代价远大于收益。全部记在此处,v2.1 一次性处理。

标注:**[盒子]** = 会改变设计盒 → 必须重跑数据工厂;**[无损]** = 不影响已有数据。

---

## A · 命名与叙述(无损,应尽快改)

### A1 [无损] 设计变量 ζ 改名为 τ
代码里 `c = ζ · k`,扭簧 k 单位 N·m/rad、扭转阻尼 c 单位 N·m·s/rad,
所以 **ζ = c/k 的量纲是秒**,是 Kelvin–Voigt 松弛时间,不是无量纲阻尼比。
- 物理本身合法,错的是叙述:文档与 PPT 一直写作"无量纲阻尼比"。
- 真正的阻尼比 ζ_true = c/(2√(kI)) = τ·ω_n/2,随设计变化;
  实测设计盒两端对应 ζ_true ≈ 0.06 – 2.29(踝/膝),跨 38 倍。
- **改法**:全局改名 ζ → τ,并在文档补一句换算与"需按算例反算"的说明。
- 顺带修正 v2 设计文档里的一句原话:
  "κ、ζ、r2、r3 继续用无量纲形式,它们是良构的无量纲群" —— ζ 是这句话的反例。

### A2 [无损] 样本量统一为 n = 213
AVONET 水鸟表去重后是 **213** 个物种;拟合元数据 `fit_n` 记的是 214。
图、文档、元数据统一改成 213。

---

## B · 数值与实现缺陷(需对照实验)

### B1 [盒子·小] σ 应直接由残差算,而非反推
`bioprior.sigma_of_fit` 由 (ci95, n, SD_LOG_M) 反推 σ,其中
`SD_LOG_M = 0.35` 是**估计值**(样本 log10 体重的标准差),真实值为 **0.3639**(差 4.0%)。
- 反推得 σ = 0.0784;**直接从 213 个残差算得 σ = 0.0819**。
- 后果:先验带比应有的**窄 4.5%**(倍数 1.570× vs 1.602×),方向是略微不够保守。
- 代码注释已写明该待办:"拿到原始数据后应直接算残差 σ 覆盖此估计。"

### B2 [已完成→转盒子] 髋关节阻尼的 v1 遗留特例式(对照已做,见《路线图》三·五;偏置方向为**偏保守**,原文'偏乐观'有误)
```
c_ankle = τ · k_ankle          c_knee = τ · k_knee
c_hip   = (τ/0.03) × 0.2 × k_hip        ← 等价于 6.67·τ·k_hip
```
髋的等效松弛时间是踝/膝的 **6.67 倍**,没有物理理由,是 v1 调参留下的硬编码。
- 后果:髋被过度阻尼(实测髋关节峰值力矩中 **84% 来自阻尼项、仅 16% 来自弹簧项**),
  峰值可能被低估 → 现有结果偏乐观。
- **改法**:做一次"统一成 c = τ·k"的对照,量清楚影响幅度再决定保留哪个。

### B3 [无损] 小质量 + 硬地面组合的求解器失败
E20/E21 中散见 `solver` 失败,集中在 m ≲ 1 kg 且 kc = 1e6 的组合。
刚度比很大,积分步长 h = 0.2 ms 可能不足。
- **改法**:对该角落自适应减小步长,或改用更稳的积分器设置;失败率应降到 0。

---

## C · 先验形式的升级(改盒子,是 v2.1 的主体)

### C1 [盒子] 三维条件先验 p(L1, r2, r3 | m)
当前 r2/r3 是**与体重无关的固定窄带**;实测否定了这个假设。
用不含比值的回归(避免 Pearson 1897 的比值伪相关)直接测得:

| | p (L_seg ∝ L1^p) | 与几何相似 1.0 相差 | \|t\| |
|---|---|---|---|
| L2 胫跗骨 | 0.877 ± 0.031 | −0.123 | 7.7 |
| L3 股骨 | 0.766 ± 0.039 | −0.234 | 11.8 |
| 全腿长 | 0.879 ± 0.024 | −0.121 | 9.7 |

- 现窄带**完整覆盖**该漂移(r2 趋势端点 1.58–1.92 ⊂ [1.49,2.09];
  r3 0.85–1.23 ⊂ [0.84,1.28]),所以方向保守、不误伤,代价是盒子偏宽:
  趋势幅度占盒宽 **r2 56% / r3 86%**。
- **改法已有确定形式**,不需要猜:
  `r2(L1) = r2_ref · (L1/L1_ref)^(p−1)`,指数 p−1 = −0.123 / −0.234。
- 串联 b 后每段对体重的标度指数(**两个不同样本串联的推算值**):
  L1 ∝ m^0.391 · L2 ∝ m^0.343 · L3 ∝ m^0.300 · **全腿 ∝ m^0.344**
  → 全腿长与几何相似 1/3 在误差内一致,**陡的不是腿,是跗跖骨**。

### C2 [盒子] κ 的敏感性体检需在 v2 物理上复验

**已有的依据(不要说成"没有出处"):**
- **量纲论证**:κ = k/(m·g·L_leg),分母是"关节至少要撑住的重力力矩",κ 即安全倍数,默认取 4。
- **髋取 4κ 有生物依据**:课题组前期实测结论(触水后髋大幅弯曲、膝踝稳定);
  且本工作的结构定尺**独立**算出髋关节峰值力矩为膝/踝的 **3–4 倍**,两者互证。
  ⚠️ **不能说成"我们的 DLC 视频看到的"** —— 项目自订原则写明:近端(膝/髋)藏体内、
  体外不可观测,不逐帧重建;视频只测远端(足-水平角 φ、跖骨-水平角 ψ、踝轨迹)。
  正式引用时须确认《参数溯源表》所指"v2 论文"具体是哪一篇。
- **做过敏感性体检**:κ ∈ {3, 4, 6} 重跑,标度指数 0.37/0.45/0.49、天鹅位置结论均不变。
- 设计盒 = 覆盖默认值 (4, 4, 16) 并向两侧展开约 2 倍。

**仍然存在的两条欠账:**
1. 上述体检是在 **v1 物理**上做的(即体重死维那一版),**v2 上未复验**;
   而 v2 改了接触与结构定尺,κ 的影响路径已经变了。
2. 因阻尼按 `c = τ·k` 设定,**改 κ 实际上同时在改阻尼**,而实测显示冲击时段以阻尼项为主
   (髋峰值力矩 84% 来自阻尼项)。v1 那次体检没有这层认识,"κ 是刚度维"的说法在 v2 里不准确。

**改法**:(a) 在 v2 物理上重做 κ ∈ {3,4,6} 体检;(b) 用运动生物力学的无量纲腿刚度
(SLIP/spring-mass 体系)给盒子标注生物观测落在哪一段;(c) 边界灵敏度扫描,确认最优不在盒壁上。

### C3 [盒子] 触地姿态被冻结成常数,且与自有视频不符
模型固定 跗跖 50° / 踝 120° / 膝 90°;组内 12 段着水视频实测中位为
**踝 144° / 膝 133°**。腿越伸直可用行程越小、峰值越高 → **现取法偏乐观**。
- 更深的问题:姿态是生物**主动调节**的控制量,不是常数。
- **改法**:先按视频中位重设初始构型复跑关键算例;进一步可把姿态升为
  第 8 维设计变量或一个随工况的控制策略(可接 LCE 半主动方案)。

### C4 [盒子] 边界饱和信号:τ 与 κ踝 贴着下界
cVAE 在五个体重上选的 τ 为 0.0118–0.0202,盒子是 [0.01, 0.10] ——
**全部落在下缘 2%–11%**;κ踝 亦偏低(盒内 6%–28%)。
- 提示最优可能在盒外。**改法**:把下界放到 0.003 重跑一小批,看是否继续往下跑。
- 限定:五个设计、单次生成,这是**旗标不是结论**。

---

## D · 判据与工况(部分改盒子)

### D1 [无损] D ≤ 0.25·段长 的灵敏度扫描
该几何上限直接决定"谁造不出来",数值 0.25 是杆件理论有效域的经验界,不是精确值。
- **改法**:扫 0.20 / 0.25 / 0.30 三档,看结论是否翻转。

### D2 [无损] 硬地 + v0 = 2.0 的要求组合物理上不可能
10 g 恒定减速从 2.0 m/s 停下,理论最小行程 = v0²/(2a) = **20.4 mm**,
而 s_max = 24 mm → 需 85% 方波效率,被动系统做不到(v0=1.2 只需 31%)。
- 该工况格四臂、随机、cVAE 全部为零,量的是"要求不可能"而非"标度律不行"。
- **改法**:该格从消融对比中剔除,或改用与速度匹配的要求(如 15 g / 40 mm)重跑。

---

## E · 模型本身

### E1 [训练] cVAE 后验塌缩:解码器忽略隐变量 z
同一条件下 216 次采样,7 个设计维的**中位变异系数仅 0.21%** —— 输出的实为同一个设计。
- 影响:不能声称"生成设计族/答案族";"通过率"实为 0/1,须改写口径。
- **不影响** E5 自提升循环:其探索来自新鲜 LHS(`lhs(n_exp, DU, rng)`),不是 z。
- **改法**:若需要真正的多样性,调 β / 换 KL 退火 / 检查训练目标是否每个条件只剩单点;
  也可接受它退化为"条件到设计的确定性回归器"并如实叙述。

---

## 优先级建议

1. **A1 / A2** —— 无损,改名与统一口径,今天就能做完
2. **B2** —— 一次对照实验即可量清楚,影响方向是"偏乐观",必须知道幅度
3. **C3** —— 影响方向同样偏乐观,且与自有数据冲突,是最容易被问的一条
4. **C1** —— v2.1 的主体工作,函数形式已确定
5. **B1 / C2 / C4 / D1** —— 随 C1 一并重跑数据工厂时处理
6. **E1 / B3 / D2** —— 视需要
