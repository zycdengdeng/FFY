# E28 · 与赵老师 Cranfield 数据的对照 —— 已完成

数据：`FFY/duong_data`，Duong Vu (Cranfield MSc CSTE, 2025)，DeepLabCut 姿态估计。
12 次水鸟落水（鸭 3 / 雁 3 / 鹈鹕 3 / 天鹅 3），30 fps，逐帧 16 个关键点。
`Duck_1_final` 剔除（压缩后髋点被跟踪到足端下方，明显失败），有效 11 次。

复算脚本：`fig_duong.py`（本目录）。角度由本工作从原始关键点独立重建，
与其 `kinematic_processed_dataset.csv` 交叉核对，ITJ 均值差 1.8°、KNEE 均值差 2.4° —— 约定一致。

---

## 1. 骨骼命名对上了

| Duong 的关键点 | 骨骼 | 我们的量 |
|---|---|---|
| hip → knee | 股骨 femur | L₃ |
| knee → ankle | 胫跗骨 tibiotarsus | L₂ |
| **ankle → foot** | **跗跖骨 tarsometatarsus** | **L₁** |
| foot → foot_tip | 趾 | （模型中无） |
| `ITJ_angle` | 跗间关节 | **θ_A（踝）** |
| `KNEE_angle` | 膝 | **θ_K** |
| `HIP_angle` | 髋（相对躯干） | 模型中无对应，见下 |

## 2. 三条结论

**① 两个角度搜索盒被真实落水完全覆盖，一个没漏。**

| 量 | 实测（触水瞬间，n=11） | 我们的盒 |
|---|---|---|
| θ_A（ITJ） | 140° ± 14°，跨度 113.5–159.9 | `THA_RANGE = (113, 160)` |
| θ_K | 135° ± 12°，跨度 118.1–156.8 | `THK_RANGE = (118, 157)` |

**已从代码确证不是独立验证**：`bioprior.py` 第 50 行注释写着
`# 1) 姿态进设计向量(度):范围 = Duong 12 段着水视频 water_contact 帧实测全距。`
所以这两个盒就是从这份数据定的，写论文时不能当作"外部数据验证了我们的先验"。
可以写的是：盒边界有明确的实验出处（可引 Duong 2025），且 11 次落水无一越界。

**② `q1_0 = 50°` 站得住，不必改。**

跗跖骨与水平面夹角（= 模型中 `a1 = q1_0`）实测 **43° ± 9°**，跨度 27.6–54.8°。
50° 落在均值 +0.8σ 内。

> 修正一条旧结论：早先我从那张鸟骨骼示意图量到跗跖骨 ≈71°、并说"髋在足正上方"。
> 那是**站立**姿态。触地姿态是另一回事，以这 11 次落水为准。

**③ "髋在足正上方"不是触地时的生物学事实。**

- 触水瞬间髋-足前倾角 **30.5° ± 6.4°**，11 次全部在 19–38°，没有一次接近 0。
- 从触水到最大压缩，跗跖骨**变平 7.7°**（9/11 次为负），前倾角不减反增。

这和我们仿真里看到的完全一致（天鹅几何 26.2°→26.8°，生成器 13.4°→13.8°）。
**回应 Bigoni 的话就是**：垂线对齐是静态站立的特征；落震瞬间水鸟的腿是斜的，
而且在压缩过程中会继续变斜。我们的 A0（13.4°）反而比真鸟更接近竖直。

## 3. 这份数据给不了什么

原视频全部来自 Instagram / TikTok / YouTube Shorts 的**慢动作**片段，且无标定物。
按髋点位移反解出的下沉速度只有 0.0–0.3 m/s，物理上不可能 —— 慢放倍率未知，
速度量无法反解。**这份数据只能用几何量，不能用来定 v₀ 或 v_x。**
（速度比 v_z/v_x 理论上对慢放免疫，但逐帧算出来从 −0.6 到 +131 全是散点，
说明相机未标定 + 鸟斜向出画的透视误差压过了信号，同样不可用。）

## 4. 对实验计划的影响

- **E28 从"待办"变成"已完成"**，成本 0，结论上面三条。
- **决策 ② 定案**：`q1_0` 保持常数 50°，不升维、不做垂线反解。E26 降级为一次
  27–55°（实测跨度）的敏感度扫描，只为报告"在生物学跨度内 peak_g 变化 X%"。
- 图 `F_duong_e28.png` 可直接进 PPT，是目前唯一一张"外部实验数据 vs 我们的建模约定"的图。
