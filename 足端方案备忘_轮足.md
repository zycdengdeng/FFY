# 足端方案备忘 · 轮足两配置（2026-09-17 讨论记录）

> 背景：v2.5 实测——被动无抓地的仿鸟腿只支持垂直着陆（Fr 上界=0）。
> 水平着陆能力要么靠爪（锚住水平力），要么靠轮（旁路水平速度）。
> 本页记轮的两个候选配置。**全部纯机械、无传感器无电，仍属被动设计。**

---

## 结论先行（2026-09-17 数据补充）

用 P9a 的 9,600 个存量探针重判：**放宽滑移限额救不回水平着陆**。
滑移闸完全拆掉，Fr=0.5 可行率也只从 0.6%→0.8%，Fr≥1 仍≈0。
原因：Fr>0 时行程闸(smax)与滑移闸同时大规模超限（84–87% vs 87–92%）——
侧滑过程中腿被持续压着拖，行程跟着爆。
**"橡胶垫+允许滑行"的滑停式着陆不成立。水平着陆必须爪或轮。**

---

## 方案 A（+C）：行程触发 + 阻尼延时刹车

**原理**　腿压缩时行程拉动钢索 → 钢索不直接拉刹车，中间串一个小阻尼器
（硅油活塞，和"门弛器/缓降器"同原理）→ 刹车在触地后延时 0.3–0.5 s 才咬合。

**为什么要延时**　冲击峰值在触地后 ~100 ms 内结束（t_settle≈0.2–0.3 s）。
延时让轮子在载荷最大的窗口自由滚（水平速度走轮子，不产生水平冲击力），
等垂直载荷稳定后再刹停。

**参数（1 个）**　阻尼时间常数 τ。取 τ ≥ 0.3–0.5 s。

**优点** 结构最简单，行为确定。
**缺点** τ 是死的：地面软硬/下沉速度变了，最佳延时也变，它不会跟着变。

---

## 方案 B+C：反离心刹车 + 单向轴承　★推荐优先做原理样机

**原理**　把离心离合器反着用。轮毂里装离心块+弹簧：
- 轮子转得**快**（刚触地、地速高）→ 离心力把刹车片**顶开** → 不刹，自由滚
- 转速降到阈值以下 → 弹簧赢 → 刹车片**渐进咬合** → 越慢刹得越紧 → 停

**"无源 ABS"的白话解释**　汽车的 ABS（防抱死）是电子系统：轮子快锁死时自动松刹车，
防止轮胎变成滑块失去抓地。我们这个机构用纯物理实现同样效果——
高速时离心力自动"松刹车"，所以**高速下轮子永远不会被锁死**，
不需要任何传感器、控制器、电源。刹车力随速度自动调节，故称"无源 ABS"。

**参数（2 个）**　① 咬合阈值转速（由离心块质量+半径定）② 弹簧预紧（定最终刹车力矩）。

**优点** 自调节：不同地速/地面下行为都合理；没有"定时"概念。
**缺点** 比 A 多几个运动件；离心块需配平。

**原理样机路径**　自行车飞轮花鼓 + 3D 打印离心块 + 带式刹车，估 30–80 g/轮（5 kg 样机可接受）。

---

## C：单向轴承（两方案通用底座）

只许前滚、不许后滚（自行车飞轮现成件）。作用：斜坡落地不倒溜；
也是 B 方案的安装载体。

---

## 待定参数与安全界

| 量 | 定法 |
|---|---|
| A 的延时 τ | ≥ 冲击稳定时间，取 0.3–0.5 s，落震仿真可给每工况的 t_settle |
| B 的阈值转速 | 对应地速 ~0.3–0.5 m/s（低于此速刹停不构成冲击） |
| 刹车减速度上限（防翻） | a_x < g·(前支点到质心水平距离)/(质心高度)，一行静力公式，选弹簧预紧时算 |
| 翻车仿真（E29 三相图） | **不做**（Fr=0 下翻车非主导，E28 真鸟视频支持锁俯仰近似）；与轮子一起 v3 立项 |

---

## 口径备忘（开会用）

- 轮子是**被动机构**，不是主动着陆：无感知、无控制律、无电。
- 类比话术：鸟落水用滑水耗散水平速度，我们用滚动耗散——机构纯被动。
- 三重证据链（爪/轮的必要性）：μ_crit 扫描 · Fr 可行性曲线 · 滑移限额敏感性重判。


---

# ★ W0 冒烟结果回填（2026-09-17 · 云端 exudyn 实测，代码 src/stage10_v2/wheel_probe.py）

1. **方案 A 降级为不推荐**：延时刹车在 Fr=0 下有原则性缺陷——冲击峰值窗口内轮子自由，
   等于把抓地能力让掉。实测自由/弱刹车轮在 Fr=0 直接 deep_sink 硬失效
   （轮滚走→腿放平→机体塌）。**B+C 是唯一同时保住两种着陆的架构。**
2. **单向轴承方向定了（仿真答的）**：Fr=0 垂直落轮心滚 **−x**（−69mm），
   Fr>0 前向落滚 **+x**（+418mm）——方向相反 → **许 +x 前滚、锁 −x**，
   一个轴承同时买到"前向自由滚 + 垂直落免费抓地"。与 v2.5 裸足 P9a 的
   foot_dx<0 独立互证。
3. **刹车力矩是必须解出来的设计变量**：τ_max=8 N·m 没锁住轮（Fr=0 也失效），
   P9w 探针扫 {4, 8, 16} 定档。
4. **翻倒判据装不进单点平面模型**：放开俯仰即翻 90°。翻倒需要前后支撑基长
   （轮距/脚趾长），是 v3 真建模任务——真鸟恰好用脚趾前后长度稳俯仰。
5. 校准：locked0 受控对照与 rigid 逐位一致（构建器验证通过）；
   真实轮径 30mm + 轮重 40g 本身付约 +10% 峰值过载代价。

**下一步**：al7075 收尾后 A100 跑 `p9w_probe.py --workers 128`（~15 min）→
Fr 上界重定标 + τ_max 定档 + 方向复核。


---

# ★★ P9w 定标结果回填（2026-09-18 · 两轮探针 + 本地 P9w-2）

## 结论四条（v3 立项的定量依据）

1. **轮子旁路水平冲击成立**：Fr=2 前向着陆（≈4 m/s），冲击窗内 peak 2.6 g、
   前滚约 0.4 m —— 对照裸足 Fr=2 全灭。峰值载荷结论可信（峰值发生在窗口早期）。
2. **B（反离心刹车）单独在任何 Fr 下都长期不稳**，两个机理都实测到了：
   ① 挣脱失稳——冲击力矩瞬间超过 τ_max 后，轮越转刹车越松（反离心本性），
   τ 从 4 加到 16 N·m 无效；② 复滑——前滚刹停后，腿压缩的 −x 趋势重新接管，
   无止回则反向跑飞。第一轮 A100 上 Fr=2 的 12 个"存活"是 0.3 s 时间窗截尾假象，
   1.2 s 窗口下全部暴露。**B+C 缺一不可从架构偏好升级为实测结论。**
3. **C（单向轴承）理想咬合下，低 Fr 轮足 = 刚性橡胶足**（locked 近似）：
   Fr=0 18/18 仿真有效（peak 11.2 g 探针无 gcap 闸）、Fr=1.0 18/18（peak 4.2 g）。
   ⚠ 但 Fr=1.0 滑移中位 139 mm —— 会被 0.5·L1 滑移闸判死：
   **μ=0.4 的地面上锁死轮也滑**，低速斜落仍需胎面高 μ（橡胶 ~1.0）或爪辅助。
4. **模型债（v3 必修）**：单向离合的用户函数实现数值脆
   （轮惯量 ~2e-5 kg·m² + 强反向阻尼/棘轮刚度 → 求解器首步发散；
   "记忆最大角"式棘轮会被牛顿试探迭代污染，属实现禁忌）。
   正确路径：互补约束 / 事件切换关节。另：轮足会滚远，接触地面 quad 需 ±30 m
   （±3 m 时滚出边界产生 2.4 m "下陷"伪影，已修）。

## 探针口径备忘
- 低 Fr 用 locked（棘轮咬合上界），高 Fr 用 brake（棘轮自由）——分段理想化；
  真棘轮瞬态留给 v3 互补约束模型。
- 探针"有效"= 仿真守卫通过（collapse/deep_sink），不含 v2.5 六闸判据。
- 代码：src/stage10_v2/wheel_probe.py（含平滑刹车律、反向阻尼式单向、大地面）、
  w0_smoke.py、p9w_probe.py（A100 版）。

---

## M1 结果（2026-09-18）：单向离合器实现完成 + 一个作废声明

### 1. 先坦白：此前 P9w 第一轮（12/270）数字全部作废

`wheel_probe.py` 的接触调用漏传了 `quadPoints` —— `quad = ±30m` 变量定义了但没进
`CreateSphereQuadContact`，实际地面是 exudyn 默认的 **1×1 m 单位方块**（x∈[0,1]，机器恰好站在其边缘）。
凡是往 −x 滑动或弹跳的机器都直接掉出世界（sink 数千 mm 的 deep_sink 全是这个）。
A100 上那轮 12/270、"全部 Fr=2" 的筛选结果建立在坏地面上，**不作为任何结论的依据**，M3 重跑。
（W0 的 locked0≡rigid 对照恰好没滚出边缘，结论侥幸成立，但也会在 M3 里顺手复验。）

### 2. 单向离合器（C）的最终实现：休眠弹簧闩锁

三条路线试错后收敛：
- ✗ 力回调里做带状态的最大角棘轮 —— Newton 试探迭代会污染状态，禁用；
- ✗ 中途激活 GenericJoint 转动约束 —— 位置级约束锁的是装配参考角，瞬间回弹爆炸，禁用；
- ✓ **休眠 TorsionalSpringDamper**：常态 k=c=0；preStep 检测到相对转速
  `ω_wheel − ω_tarso < −0.5 rad/s` 时，读该弹簧自己的连续转角（跨圈连续，已单独验证 20 rad 精确），
  设 `offset=当前角` 后上刚度 K=40、阻尼 0.05 —— 零力咬合，无数值冲击。一次性闩锁，记录 latch_t。

### 3. 修复后验证矩阵（真实可行设计 m=4.73 / v0=1.37 / kc=8.7e5，锁俯仰，12/12 通过）

| mode | Fr=0 | Fr=0.5 | Fr=2.0 |
|---|---|---|---|
| rigid（裸足基线） | 2.15g / 漂 56mm | 1.66g | 1.56g / 滚 799mm |
| locked（轮锁死） | 3.79g | 1.94g | **1.54g ≈ rigid** ✓ |
| brake（仅 B） | 1.27g / **倒退 −1018mm** | 2.59g | 1.47g / 滚 3507mm |
| bc（B+C） | 2.56g / **+116mm，闩锁 4ms 咬合** | 同 brake（闩锁未触发） | 同 brake（闩锁未触发） |

读法：
- **B 单独在 Fr=0 倒退奔逃 1 m**（腿压缩反扭矩倒转轮子）—— 之前的推断被定量证实；
- **B+C 把倒退压到 +116 mm**，闩锁只在轮子试图反转时咬合，正向自由滚完全不受影响 —— 单向轴承的两个半边（正向自由、反向锁止）都验证了；
- Fr=2 滚出 3.5 m 未停住是反离心刹车"高速不咬、低速才咬"的设计特性，**停车距离是 M3 的标定输出**，不是缺陷。

### 4. 两个默认值备忘

- `pitch_free`：轮足模式默认放开俯仰，单点接触必翻（pitch→90°，机构倒下时轮心摆到地面以下数米，
  之前误读为"穿地"）。M1 验证一律 `pitch_free=False`；放开俯仰 = M2 轮距的问题域。
- 闩锁刚度 K=40 N·m/rad 是数值稳定上限附近的验证值；咬合后轮子会在簧上弹性回摆（|ω| 峰 ~98 rad/s），
  相当于真实棘轮的齿隙柔性，位移结果不受影响。

**M1 ✅ → 下一步 M2：前后双轮距 + 放开俯仰 + 翻倒判据（E29 复活）。**

---

## M2 结果（2026-09-18）：双腿轮距版建好，两条硬物理教训

`eval_wheel2`（wheel_probe.py 内）：前后双腿 + 机体俯仰自由 + 翻倒判据（max|pitch|>40° = tipover，E29 复活）。每腿按 m/2 定尺，足端接触点对称于质心 ±wb/2。

**教训一：腿的摆放不能以髋对称。** 这套设计髋在足前方 ~245 mm（Σlᵢcosαᵢ），
髋对称 ⇒ 两足全在质心后方，支撑段不包住质心，Fr=0 也必前翻（实测 90°）。改为足端对称。

**教训二：双腿必须镜像（前腿原向、后腿绕竖直面镜像）。** 同向双腿的水平分力不抵消，
合力作用在地面高度、质心在 ~0.39 m ⇒ 恒定俯仰力矩驱动摇摆发散，Fr=0 也翻。
镜像后 locked Fr=0 俯仰精确 0.0°。真机对应：前后起落架八字对撑，和真飞机/四足动物站姿同构。

### 本地冒烟（chosen 设计 4.73 kg，τ=8, r_w=30mm）

| | Fr=0 | Fr=0.5 | Fr=2.0 |
|---|---|---|---|
| locked（撬式基线）wb=0.5 | ok 6.2g | ok 4.0g | **翻倒 19g** |
| locked wb=0.7 | ok | ok 4.0g | **翻倒 25g** |
| bc（轮足）wb=0.5 | 翻倒 | 求解器死（重试机制已加） | ok 2.0g |
| bc wb=0.7 | **ok 1.8g** | **ok 2.2g** | **ok 2.0g，滚 4.2m** |

**v3 论点在小样本上成立**：撬式在 Fr=2 摩擦抱死→前空翻（≥19 g）；轮足同工况 2.0 g 滚出去。
轮距是决定性参数（静态翻倒角 atan(wb/2h)，wb=0.3 只有 21°）。
质心高度 h≈0.39 m 是新的一阶量，进 M3 相图。

### M3 · p9w3_probe.py（A100 待跑）

矩阵：Phase0 = M1 回归 12 例；Phase1 = m{4.7,12} × wb{0.3–0.9} × Fr{0–5} × [bc: τ{2–16} × r_w{20–45mm}]，locked 做基线臂，共 ~844 例，32 workers 约 10 分钟。
产出：翻倒相图（wb–Fr，locked vs bc）、τ/r_w 甜点、停车距离、Fr 上界（v2.5 裸足 Fr≥4 全崩的对照）。

---

## M3 结果（2026-09-19）：P9w-3 全标定完成 → v3 盒子定案

A100 跑完 844 例（529 s，32 workers）：Phase0 M1 回归 **12/12**；主矩阵 832 例，
失败仅两类——翻倒 341、求解器死 1（bc/wb0.7/Fr0，重试机制没救回的孤例）。
相图：`figs_v25al/fig4_翻倒相图.png`。

### 三条主结论

**① 轮足把 Fr 包线从 ≤1 推到 ≥5（矩阵上限）。**
v2.5 裸足 Fr≥4 全崩、推荐 Fr=0；撬式（locked）基线只有 wb=0.9 才在 Fr≥1 存活，峰值 2.3–2.9 g；
轮足 B+C 从 wb=0.5 起全 Fr 存活，峰值 **5 kg 级 1.5–2.0 g、12 kg 级 1.3–1.4 g**（≈撬式一半）。
水平动能被滚动旁路，这是 v3 的核心数字。

**② 刹车要弱不要强。** τ_max=16 N·m 多翻 99 例（咬合太猛，俯仰中位 20.8° vs τ=2 的 3.9°）；
τ ∈ [2,4] 通过数最高且峰值最低。r_w 20–45 mm 不敏感。
直觉反转：单向锁（C）负责"站住"，刹车（B）只需要温和收尾。

**③ 停车距离本轮测不了，这是诚实的边界。** 通过样本停住率 10/465，不是刹车失效，而是：
反离心刹车只在 ω<ω_th=20 rad/s（v≈0.6 m/s）咬合，整个矩阵 **ω_th 没扫**；
模型 **无滚动阻力**（高速段纯惯性滑行）；T=1.2 s 窗口截断。
Fr≥3 存活样本滚距中位 8.4 m 是"1.2 秒内滚了多远"，不是停车距离。

### v3 盒子（进 M4 工厂的设计维与边界）

| 维 | 盒 | 依据 |
|---|---|---|
| wb 轮距 | **[0.5, 0.9] m** | 0.3 两模式全灭；0.5 起 bc 全 Fr 存活 |
| τ_max | **[2, 6] N·m** | 16 翻倒率高；2–4 甜点 |
| r_w | [0.02, 0.045] m | 不敏感，留给结构/重量权衡 |
| ω_th | **新增设计维**，扫 [5, 60] rad/s | 停车行为由它主导，本轮未扫 |
| Fr 工况 | [0, 5] | 包线实测 ≥5 |
| 质心高度 h | 记录量（本轮 ≈0.39 m） | 翻倒角 atan(wb/2h)，进翻倒判据 |

### M4 前必须补的模型项

1. **滚动阻力**（μ_roll≈0.01–0.02 或等效轮毂阻尼）——没有它停车距离永远是无穷；
2. T 拉到 2.5–3 s 或用末段减速度外推停车距离，作为软闸指标；
3. 翻倒判据从 max|pitch|>40° 细化为 " tip 且未恢复"（当前口径把恢复的摆动也判死，偏保守，可接受）。

**M3 ✅ → M4：轮足工厂（设计 10→14 维：+r_w, τ_max, ω_th, wb），基线=v2.5 al7075 裸足。**

---

## M4 开工（2026-09-19）：一个推翻 M1–M3 的符号发现 + v3 物理定案

### ⚠ 头等大事：刹车力矩符号一直是反的（M1–M3 数字部分作废）

写 `physics_v3` 做台架自检时发现：exudyn 的 `TorsionalSpringDamper.springTorqueUserFunction`
返回值在施加到物体时**取反**（内部按"弹簧储存力矩"约定）。台架实测（自转轮 100 rad/s，
纯粘性力矩，1 s）：`return +c·ω` → ω 衰减到 86.07（理论 86.07，误差 0）；`return −c·ω` → 116（发散）。

M1–M3 的 `wheel_probe` 刹车全写的 `−tanh(ω)·τ`，**那其实是驱动力矩**——轮子被加速、从不真刹车。
- M3「轮足滚 8 m 停不下、Fr 包线推到 ≥5」是这个 bug 的直接产物：轮子自由滚+被驱动，
  水平动能没被吸收，当然停不下也不翻。**那张相图作废（第二次，前一次是 quadPoints）。**
- M1 单腿 bc「Fr=0 倒退 116 mm」结论方向仍对（闩锁挡反转是几何事实），但幅度需复核。

修正：`+tanh(ω)·τ`。`physics_v3.eval_v3` 与 `wheel_probe` 均已改，附台架注释防复发。

### 正确物理下的真实图景（chosen 腿，双镜像腿，已修符号）

| Fr | wb=0.7 | wb=0.9 停车距离 | 俯仰max |
|---|---|---|---|
| 1.0 | **collapse（腿被刹车反扭矩压塌）** | **停在 0.37 m** | 14.6° |
| 2.0 | collapse | **停在 1.67 m** | 15.4° |

**真结论（比 M3 假象更硬也更有价值）：**
1. **轮足真能把车刹停**——Fr=2 停在 1.67 m，这是有限实测停车距离，不再是 inf；
2. **需要宽轮距**：wb=0.9 稳，wb=0.7 被刹车反扭矩压塌腿（collapse 成为新主失败模式）；
3. 停车距离随 Fr 单调增（0→0.37→1.67 m），物理合理；
4. τ 在 [1,6] 不敏感（wb=0.9）；俯仰稳定在 15° 附近。

轮足的**真正矛盾**浮现：要停车必须刹车，一刹车就有指向高质心的前翻/压腿力矩。
这才是 v3 要解的问题，M3 的"轮足碾压裸足"是符号 bug 造的海市蜃楼。

### physics_v3 定案（M4 工厂物理层）

- 14 维设计 = v2.5 十维腿 + [wb, τ_max, r_w, ω_th]（`WheelPrior`，与 BioPrior 同接口）
- 8 维条件 = v2.5 六维 + [pitch_cap∈{15,25,40}°, log Lstop∈[3,15]m]（事后抽签，同 gcap/smax）
- 双镜像腿 + 俯仰自由 + B+C 轮 + **滚动阻力 τ_rr=C_RR·N·r_w·tanh(ω/0.5)**（C_RR=0.015 本工作设定）
- 判据 `feasible_v3`：gcap(a_res)/smax/slenderness/massbudget/rebound + **tipover(抽签角)** + **stop(停车距离≤Lstop)**；去掉 slip（轮子本就滚）
- 质量预算 0.08（轮式口径，高于裸腿 0.06）；轮组质量模型见文件头
- 单次评价 ~10–14 s（俯仰自由+双腿+T=2s 停车窗口）

### 下一步

1. **v3_grid_probe.py 上 A100 重跑**（正确符号相图，~300 点，含裸足基线臂）→ 定 v3 盒子真实边界（wb 下界大概率上移到 0.8）；
2. 据此调 `WheelPrior` 的 WB_RANGE，再跑 M4 工厂（factory_v2 加 --v3 分支或新建 factory_v3）；
3. M5 三方对比图（轮足 vs 裸足 vs 油气支柱）用修正后数字。

### 复核勘误（2026-09-19，Fable 复检 Opus 的 M4 工作）

核心结论**全部复核通过**：台架三方对照（内置阻尼 = 用户函数 +c·ω = 理论 86.07 精确；−c·ω 发散）
确证符号约定；physics_v3 生效性（C_RR 线性响应、停车距离有限化）确证。两处勘误：

1. **Opus 的"修正后单腿矩阵"表作废**（brake Fr=0 −1018 mm 等 12 个数字与修正前逐位相同）——
   那次验证进程加载的是本地残留的 M1 旧文件，不是修正版。修正版实测：brake Fr=0 漂移
   **−54.5 mm**（vs free +1.7 mm，函数确认生效）。repo 里的 wheel_probe.py 本身是对的。
2. **"wb=0.7 被刹车反扭矩压塌腿"归因错**。正式判据下该点是 **tip_kill（前翻 90° 后回落）**；
   collapse 标签只在诊断时关掉硬杀（TIP_KILL=179）才出现，0.79 m 的"行程"是翻转几何不是腿压缩。
   真实叙事：**刹车抓地 → 纵向载荷前移 → 前翻**——这与"要停车就得刹车、刹车就有前翻力矩"
   的主矛盾一致，wb=0.9 时俯仰稳在 15° 且停车 0.4–1.7 m 的结论不变。

教训入册：同名模块的旧副本残留在 sys.path 里是"验证通过但没验对东西"的经典陷阱；
以后本地验证一律打印 `module.__file__`。

---

## v3_grid 定案（2026-09-19）：正确符号相图 → v3 盒子锁定，M4 工厂就绪

A100 324 例（287 s）：276 ok / 36 tip_kill / 0 solver（重试机制未触发）。
图：`figs_v25al/fig5_v3定案.png`。裸足基线臂高 Fr 的 "except" 实为 **deep_sink**
（eval_v2 返回 fail 后取 peak_a 的 KeyError，归因成立、标签粗糙）。

### 四条定案结论

**① wb=0.7 是断崖，0.8 起全 Fr 存活。** tip_kill 36 例中 33 例在 wb=0.7
（刹车抓地→前翻 90°）；wb=0.8 仅 τ=6 的 3 个高 Fr 点翻；wb=0.9 零翻倒。

**② 甜点与 M3 假象完全反转：τ≈4、ω_th=40 才是甜点。**
τ=4/ω_th=40：24/24 过、20 停、Fr≥2 停车中位 **4.7 m**、俯仰 15°。
假刹车时代的"τ 要弱"作废——弱刹车（τ≤2 或 ω_th=10）停车中位 37–117 m，Lstop 预算内根本停不住。
**ω_th 是停车主杠杆**（40 vs 10 差 20 倍），τ=6 开始翻（21/24）——停车-俯仰权衡曲线见图 (c)。

**③ 停车距离定量（wb≥0.8，best-of）：**
5 kg 级 Fr=1/2/3/5 → 0.2/1.0/3.0/8.9* m；12 kg 级 → 0.4/2.2/5.6/17.9* m（* 外推）。
Lstop 抽签 [3,15] m 下，Fr≤3 全预算可达，Fr=5 看预算上半段。

**④ 同材料对照：Fr≥2 轮足 1.5 g vs 裸足 2.0 g；裸足 5 kg@Fr=5、12 kg@Fr≥3 直接 deep_sink 崩。**
轮足包线实测到 Fr=5（矩阵顶），且这次是**带真刹车、能停下**的存活。

### 盒子更新（physics_v3 已改）

wb [0.50,0.90]→**[0.75,0.95]**；ω_th [5,60]→**[15,60]** 对数；τ [2,6]、r_w [0.02,0.045] 不变
（r_w 复核不敏感：停车中位 1.15–1.56 m）。

### M4 工厂（factory_v3.py，已交付）

骨架照抄 factory_v2：WheelPrior 14 维、eval_v3、KEYS_V3（30 项含 pitch/stop/latch）、
Fr∈[0,5]（20% 精确取零）、npass=1、走法束加 **fr 走法**（8:6:4:4:3）。
gcap/smax/pitch_cap/Lstop 仍为事后抽签，不进仿真。
默认 375 块 × 96 设计 = 36k 次仿真，128 workers 约 1.5–2 h。本地冒烟 21/24 ok。

**下一棒**：工厂跑完 → dataset_v3/训练侧（条件 6→8 维改造）→ 重训 cVAE → M5 三方对比。

---

## M4 工厂验收 + 训练侧交付（2026-09-19 深夜）

### 工厂验收（36,000 次仿真，A100）

85.9% ok；fail：tip_kill 12.0%、collapse 1.5%、solver 0.6%、deep_sink 2 例。
ok 样本：停住 55.5%、a_res P50/P90 = 5.2/13.8 g、俯仰 P50/P90 = 3.2/16.5°。

**一处口径修正（重要）**：拿 v2.5 的 smax 抽签范围（8–40 mm，裸足货舱冲程口径）判 v3，
smax 单闸杀 77%、总可行率仅 5.7%——尺子错配而非设计差。v3 双腿+俯仰机器的下沉
P5–P95 = 8–210 mm，**SMAX_RANGE_V3 由数据分位定为 [20,150] mm**（滑跑构型的下沉预算
由起落架几何决定）。修正后总可行率 16.3%（抽样）/ 13.5%（dataset_v3 全量），
与 v2.5 al 臂 15.2% 同量级。分 Fr 可行率：0 档 38% → Fr≥3.5 档 1.3%（高 Fr 真难但有信号）。

### 训练侧（dataset_v3.py + e5_loop_v3.py，已交付并全量验收）

- 条件 8 维：[log10 m, v0, log10 kc, gcap, smax, Fr, pitch_cap, log10 Lstop]；
  抽签 gcap U[4,25]g · smax U[20,150]mm · pitch_cap {15,25,40}° · Lstop logU[3,15]m
- 判据 feasible_mask_v3：v2.5 五闸（去 slip）+ 翻倒 + 停车
- dataset_v3 全量产出：**训练 5217 / 验证 999 / 测试 1176 对**，25 路径束，按 bid 切分
- dataset_v3 刻意自包含（纯 numpy，agroup_io 老教训：不拖 exudyn 链，本地机器可验收）
- e5_loop_v3：考卷**不再冻结 Fr=0**（每题抽签一个 Fr 存进 exam.json，冻结由缓存保证）；
  zdim 3→4（设计 14 维）；预算 ~8–10 h @128 workers（eval_v3 单次 14 s，是 v2.5 的 10 倍）
- 本地验证：dataset_v3 迷你+全量两级；训练闭环 fit→sample→expand→contract 合成数据全通

**下一棒**：e5_loop_v3 过夜跑 → 出 gap/可行/覆盖轨迹 → M5 三方对比图 + Week9 汇报。
