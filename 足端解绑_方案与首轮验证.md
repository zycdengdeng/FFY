# 足端解绑(v2.3)· 方案与首轮验证(2026-09-03)

## 一 · 问题:一个逻辑上的混杂,不是数值问题

`r_foot = 0.20 × L₁` 把**腿长**和**接地面积**绑成同一个变量。
四臂消融里 none 臂(b=0)按定义"腿长不随质量涨",于是**自动**获得一只
不随质量长大的小脚(半径恒 14.6 mm,而 bio 在 30 kg 时是 34.0 mm)。
软地面上必然陷穿 → deep_sink → 模型失效。

**这个劣势是绑定造成的,与被检验的异速律假设无关。**
不管效应多大,只要绑着,"腿长的作用"和"脚大小的作用"就永远分不开 ——
所以这不是靠测一测就能解决的,必须解绑。

## 二 · 为什么不用岩土承载力公式

原计划是 `r_foot = √(mg/(nπ·q_allow))`,按接地压强定尺。查下来这条路走不通:

| 依据 | 30 mm 圆形基础上的 q_allow |
|---|---|
| 建筑规范(IBC 1806 / CABO,砂类) | 3000 psf ≈ **144 kPa** |
| Terzaghi 圆基公式 q_ult = 0.3·γ·B·N_γ(无黏聚力砂) | **≈ 3 kPa** |

**相差两个数量级。** 规范表是给**米级**基础的,我们的脚只有 3 cm;
而 Terzaghi 的尺寸效应项 ∝ B,小基础几乎没有承载力。
在没有小尺寸压入实测数据之前,任何 q_allow 取值都是编的 —— 不能拿规范表充依据。

## 三 · 采用的方案:按「保持接触模型有效」定尺

关键认识:**deep_sink 是模型有效性失效,不是物理失效。**
对模型失效的正确回应不是让它污染统计,而是**把脚做大到模型在设计载荷下仍然成立**。

```
F      = m · GCAP_REF / n_legs          设计载荷(单腿),GCAP_REF = 10 g
δ      = F / k_c                        该载荷下的侵入深度
r_foot = clip( FOOT_SF · δ / 0.9,  12 mm,  60 mm )
```

- 只用**已有数据**(k_c),不引入无法取值的新常数
- 四臂在同一 (质量, 地面) 下拿到**完全一样的脚** → 混杂消失
- 上下限都是**绝对值,不与 L₁ 挂钩** —— 否则混杂会从裁剪那里回来
- 附带一个正确的性质:软地面自动得到大脚(湿沙 30 kg → 60 mm;硬地 → 12 mm)

**FOOT_SF = 3.0 是实测标定的,不是拍的。** 首轮用 1.5,草地 12 kg 给出 10.0 mm
(撞下限),比旧口径的 23.7 mm 还小,废题率反升到 33–83%。改 3.0 给 19.6 mm,
与旧口径同量级 —— 原因是随机探针里有大量峰值远超 10 g 的坏设计,
动态侵入约为按 GCAP_REF 估的静态值的 3 倍。

## 四 · 首轮验证(P8,小样本 8 探针,仅定性)

**判据①:混杂是否消失 —— 四臂废题率的极差**

| 工况 | m | 绑定 | 解绑 |
|---|---|---|---|
| 硬地 | 12 / 30 | 0% / 0% | 0% / 0% |
| 草地 | 12 / 30 | 12% / **75%** | 0% / **12%** |
| 湿沙 | 12 / 30 | **50% / 75%** | **0% / 0%** |

**软地面重端的混杂基本消除。** 草地 30 kg 残留 12%,是样本量太小(8 探针)。

**判据②:代价 —— bio 臂自己的可行率(判得了的样本)**

| 工况 | m | 绑定 | 解绑 | Δ |
|---|---|---|---|---|
| 硬地 | 30 | 12% | 38% | **+25%** |
| 草地 | 30 | 88% | 62% | **−25%** |
| 湿沙 | 12 / 30 | 86% / 67% | 75% / 50% | −11% / −17% |

**代价是真实的:软地面上 bio 的脚被做得更大,接触几何变了,可行率下降 10–25%。**
硬地反而变好(脚变小,行程利用更有效)。这不是坏消息 ——
旧口径下 bio 的高可行率有一部分来自"脚恰好够大",现在这部分优势被剥离出来单独计价了。

## 五 · 状态与待办

- 已实现:`physics_v2.foot_radius(mode, ...)`,`scen["foot_mode"]` 选择
  `"leg"`(v2.2 及以前,默认,老结果可复现)或 `"bearing"`(v2.3)。
- 验证脚本:`src/stage10_v2/p8_foot_decouple.py`。
- **首轮只有 8 探针,仅定性。** 进汇报前需 64 探针 × 5 个质量点重跑。
- 若采纳 v2.3,**数据工厂需整体重跑**(约 5 小时):所有现有数字都会动。
  但产品线结论(5 kg 规格书、P4c 架构、P6 屈曲、P7 Zener)不依赖 r_foot 的取法,
  受影响的主要是**四臂消融在软地面上的那几个数**。

## 六 · 仍然存在的已知不一致(本轮不修,记档)

现有模型里 **k_c 与 r_foot 无关**,是按地面直接给的常数。
但物理上刚性圆形基础在弹性半空间上的接触刚度 **K ∝ B**(Boussinesq),
即脚越大接触越硬。解绑后脚的尺寸变化范围变大(12–60 mm),
这条不一致的影响也随之变大 —— 应作为下一轮的候选修正。
