# 生成阶段实验方案 v1(草案)——条件逆向生成:工况 → 起落架设计族

> 2026-08-11 草案,待讨论。定位:课题的 AI 主菜第一步,前期全部成果(判官/先验/边界)在此就位。

## 0. 一句话定义

训练一个条件生成模型:**输入着陆工况,直接生成一族可行的多段式起落架设计**——
把蓝本"每个新工况都要重跑 9 次仿真 + 贝叶斯优化"的流程,摊销成"训练一次,之后任意工况零仿真出设计"。

## 1. 问题设定

| 项 | v1 取值 | 说明 |
|---|---|---|
| 条件 c | (m, v0, g_cap, stroke_max) | 质量、下沉速度、过载上限、行程上限(舱位约束) |
| 设计 x | (L1, r2, r3) | 几何三参数;弹簧由配簧规则自动填 |
| 可行判据 | peak_a≤g_cap 且 stroke≤stroke_max 且不塌陷 | 由 Exudyn 判 |
| 优劣 | 前沿距离 + η | 多指标字典(报告追加 7) |
| 域 | 鸟尺度、固定姿态 | 7 维教训:姿态维度 v2 再进 |

条件范围:m∈[1,12]kg,v0∈[0.5,2]m/s,g_cap∈[4,15]g,stroke_max∈[8,40]mm。

## 2. 对比方法(谁和谁打)

| 方法 | 角色 | 部署时每个新工况的仿真开销 |
|---|---|---|
| A. GPR+BO(蓝本式) | 主基线 | 9 次(其预算) |
| B. 标度律暖启动+局部微调 | 物理基线 | 3–5 次 |
| **C. cVAE 条件生成(v1 主推)** | 我方 | **0 次**(摊销) |
| D. 条件 diffusion+代理梯度引导 | v2 升级 | 0 次 |

v1 选 cVAE 不选 diffusion 的理由:设计空间才 3 维、训练数据量中等,小模型先跑通闭环;
diffusion 留给 v2(设计空间扩到刚度/控制调度后,流形变复杂才值得)。

## 3. 训练数据自产管线(仿真器是数据工厂)

1. LHS 采样 ~500 个工况 c;
2. 每个工况:LHS 80 设计 → Exudyn → 可行集 + Pareto 前沿段(带 η 标签);
3. 训练对 = (c, 前沿上的 x)——一个工况配多个正解,天然是"生成族"监督;
4. 数据增强:利用已证明的 v0 乘性缩放不变性(数学恒等式)做免费扩增。

成本估计:500×80=4 万次仿真 × ~2s ÷ 8 并行 ≈ **3–4 小时 CPU**,A100 节点顺手跑,零 GPU。

## 4. 评测协议(怎么算赢)

留出 20 个从未见过的工况,分两组:**内插**(范围内新组合)与**外推**(边界附近)。

| 指标 | 含义(白话) |
|---|---|
| 可行率 | 生成 100 个设计,仿真验证多少个真能用 |
| 最优性差距 | 生成的设计离该工况真前沿多远(超体积比) |
| 族多样性 | 是否铺满前沿段,还是缩成一个点(mode collapse 检查) |
| **摊销效率(头条指标)** | 同样 9 次仿真预算:BO 只服务 1 个工况,生成模型已服务全部工况 |

## 5. 实验矩阵

| # | 实验 | 回答什么 |
|---|---|---|
| E1 | C vs A vs B,20 留出工况 | 生成是否达到/超过逐工况优化的质量 |
| E2 | 消融:生成绝对值 vs 生成"相对标度律基线的残差" | 物理先验在生成层还灵不灵(残差思想二次验证) |
| E3 | 紧约束压力测试(stroke_max 调小到飞机舱位量级) | 约束满足能力,鸟解不适用区能否外推 |
| E4 | 设计空间加入 κ、髋刚度比(5 维) | 生成是否能利用"生物没有的自由度"超越生物解 |

## 6. 前期成果如何进场

- **标度律** → E2 的物理基线/残差锚点;
- **生物窄带**(r2 1.49–2.09, r3 0.84–1.28)→ 生成先验的支撑域;
- **多指标字典** → 打分卡;**物理残差代理** → 数据工厂加速(小样本段代打 Exudyn);
- **蓝本 GPR+BO** → 正式基线,公平复现其 9 样本协议。

## 7. 里程碑

| 周 | 交付 |
|---|---|
| 1 | 数据工厂跑通,4 万仿真入库(A100 节点 CPU) |
| 2 | cVAE 训练 + E1/E2,初版对比表 |
| 3 | E3/E4 + 实验报告(样式沿用多保真报告) |

## 8. 风险与诚实预判

1. **3 维几何生成可能"太容易"**——multi_metric 已发现该设计空间近似 1 维流形,
   cVAE 可能轻松拟合。这不是坏事(闭环先跑通),但**论文级价值要靠 E4 和 v2 扩维**
   (刚度/控制调度/段数)才能体现——写论文时叙事按"框架+可扩展性"讲;
2. 族生成的 mode collapse → 备选:混合密度网络 / 每工况多头输出;
3. 外推区(超出训练工况范围)可行率可能骤降 → 用标度律基线兜底(混合部署);
4. E4 若发现"非生物自由度大幅超越生物解"——这本身就是一个发现(生物受材料约束,
   工程不受),正反都有话说。

## 9. 待用户拍板的三件事

1. E4 的扩展维度选 κ+髋刚度比,还是直接上"段数可变"(3 段 vs 4 段)?后者更"多段式"但工程量大;
2. v1 用 cVAE 先跑通,还是直接 diffusion(更时髦,答辩好讲,但 3 维上杀鸡用牛刀)?
3. 数据工厂放 A100 节点还是本地 4060 机器过夜跑?(纯 CPU,两边都行)
