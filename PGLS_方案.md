# PGLS 方案 —— 把 HWI × 腿长这条关系做成可发表的强度(2026-09-03)

> 目的:回答"r = −0.61 是真实的适应性关联,还是近缘种共享祖先性状造成的假象"。
> 这是《生物学结果纪要》欠账 #1,也是这条结论唯一的实质性阻塞项。

---

## 一 · 为什么非做不可

普通 OLS 假设各物种是**独立**样本。但近缘种共享演化历史 —— 两只雨燕的翼型和腿型
都像,不是因为"短腿导致高 HWI",而是因为它们的共同祖先就那样。
这会**虚增自由度、低估标准误**,把噪声看成显著。

我们已有的间接证据表明系统发育信号确实存在:

| 聚合层级 | 单元数 | r(HWI,u \| m) | t |
|---|---|---|---|
| 物种级 | 9146 | −0.609 | −73.3 |
| 属级 | 870 | −0.642 | −24.7 |
| 科级 | 154 | −0.471 | −6.6 |
| **目级** | **30** | **−0.355** | **−2.0** |

**衰减但不塌** —— 这是"真信号 + 系统发育结构"的典型模式,而非纯假象。
但目级 t 已到 −2.0(p≈0.05 边缘),**审稿人一定会问**。PGLS 是唯一的正规答案。

---

## 二 · 要做什么(方法)

### 模型

```
u_i = β₀ + β₁·HWI_i + β₂·log₁₀(m_i) + ε_i ,   ε ~ MVN(0, σ²·V(λ))
```

`V(λ)` 是由系统树导出的**期望协方差矩阵**:对角为根到叶的路径长,非对角为
两物种**共享祖先路径**的长度。**λ 是 Pagel's lambda**,取 [0,1]:

- **λ = 0** → V 退化为单位阵 → PGLS 等价于普通 OLS(性状与系统发育无关)
- **λ = 1** → 布朗运动模型(性状完全按系统树漂变)
- λ 由**最大似然**与 β 一同估计,**不是人为设定**

**判读**:
- 若 β₁ 在 PGLS 下仍显著为负 → 关系是适应性的,**结论成立**
- 若 β₁ 变不显著且 λ ≈ 1 → 关系主要由系统发育解释,**结论须撤回或大幅弱化**
- λ 的估计值本身就是可报的结果("腿长残差的系统发育信号 λ = 0.xx")

### 稳健性(必做,不是可选)

1. **树的不确定性**:Jetz 树是 10,000 棵后验样本。**在 100 棵树上各跑一次**,
   报 β₁ 的分布,而不是单棵树的点估计。这是该领域的标准做法。
2. **两套骨架**:Jetz 的树有 Hackett 与 Ericson 两个骨架版本,各跑一遍。
3. **子样本**:全样本(9146)之外,单独跑水鸟 + 鸡形目 + 猛禽的子集,
   看我们关心的类群内部是否也成立。
4. **对照模型**:同时跑 λ=0(=OLS)与 λ=1(=布朗),用 AIC 比较,
   证明 ML 估计的 λ 确实更优。

---

## 三 · 数据从哪来(**这一步你得在能联网的机器上做**)

沙箱到不了 `data.vertlife.org` / figshare(代理 403)。**所以树必须由你下载。**

### ⚠️ 2026-09-03 修订:不能用 subsets 工具

原方案写的是走 `birdtree.org/subsets/` 上传名单换树。**查证后作废**:

> "The upper limit for the tool is 2,500 species." —— birdtree.org/subsets/

我们有 **8831 种**,超限 3.5 倍。**分批也不行**:每个 job 是从后验里独立随机抽树,
A 批的第 1 棵和 B 批的第 1 棵不是同一棵树,拼不起来。

### 正确路线:下全量树 + 本地剪枝

BirdTree 发布的不是一棵树,是 **10000 棵**(物种间亲缘关系本身有不确定性,
约 1/3 物种没有基因数据、位置靠分类学插入,所以发布的是一个后验分布)。
按 1000 棵一个 zip 分成 10 个包。**我们只要 100 棵,下 1 个包就够。**

```
birdtree.org  →  Downloads
  →  "Full trees (9993 species), Hackett backbone"   [Stage 2]  → 下 1 个 zip
  →  "Full trees (9993 species), Ericson backbone"   [Stage 2]  → 下 1 个 zip
```
链接会跳到 `data.vertlife.org` 的 JS 文件浏览器。两个骨架各下一个包,传到 A100。

**为什么要两个骨架**:Hackett 与 Ericson 是两套深层骨架(目与目之间怎么分叉),
出自两篇不同的基因组研究,学界至今没有定论。两套都跑、结论一致,才敢往论文里写。

### 名称匹配:用 AVONET 官方的 BirdTree 表,别手工对同义名

AVONET 原始发布(figshare, Tobias et al. 2022 补充数据)里有三张按不同分类命名的表:

| 表 | 分类系统 | 用途 |
|---|---|---|
| AVONET1_BirdLife | BirdLife | — |
| AVONET2_eBird | eBird | — |
| **AVONET3_BirdTree** | **Jetz 2012** | **← 下这张,名字直接对上树的 tip label** |

我们现在用的是 traitdata 的 `scientificNameStd`,与树的 tip label 不完全一致,
硬匹配预计漏 5–15%。下 AVONET3_BirdTree 做一次 crosswalk,匹配率可到 ~99%,
省掉最脏的一块工作量。

### 退路:Open Tree of Life

若上面都拿不到:OTL 合成树可用 `rotl` (R) 取,但**没有枝长**,
需 `ape::compute.brlen`(Grafen 变换)人为赋值 —— **强度明显下降**,论文里须声明。

---

## 四 · 用什么算

### ⚠️ 2026-09-03 修订:改用 phylolm,不用 corPagel 跑全量

原方案首选 `nlme::gls` + `corPagel`。**在 n=8831 上算不动**:
它要显式构造并分解 8831×8831 的协方差阵(单棵 0.62 GB),
每次 ML 迭代一次 Cholesky,一棵树十几分钟到半小时 → 200 棵跑不完。

改用 **`phylolm`**(Ho & Ané 2014, *Syst Biol* 63:397):
直接在树结构上做线性时间的剪枝算法,**不构造 n×n 矩阵**,一棵树几秒。
同样是高引标准工具,审稿人没有障碍。

```r
install.packages(c("ape", "phylolm"))
library(ape); library(phylolm)

trees <- read.nexus("BirdTree_Hackett_set1.nex")     # 1000 棵,抽 100
dat   <- read.csv("outputs/pgls/pgls_data.csv", row.names = "tip_label")

fit_one <- function(phy) {
  phy <- keep.tip(phy, intersect(phy$tip.label, rownames(dat)))   # 剪枝到我们的种
  d   <- dat[phy$tip.label, ]
  m   <- phylolm(u ~ HWI + log_m, data = d, phy = phy, model = "lambda")
  c(beta_HWI = coef(m)["HWI"], lambda = m$optpar, p = summary(m)$coefficients["HWI", 4])
}
res <- t(sapply(trees[sample(length(trees), 100)], fit_one))
```

**交叉验证(必做)**:在**单棵树**上再用 `nlme::gls` + `corPagel` 跑一遍,
证明两个工具给出一致的 β₁ 与 λ。这一句写进论文方法,堵住"换了个软件所以结果不同"的质疑。

### Python 路线

`phylolm` 在 Python 无等价物。可用 `statsmodels` 手写 GLS(由树算 V(λ),
Cholesky 白化后 OLS,再对 λ 一维似然搜索),但 n=8831 下与 corPagel 同样慢。
**只在 R 装不上时才走。**
---

## 五 · 分三步做,每步都有中止条件

| 步 | 做什么 | 产出 | 中止条件 |
|---|---|---|---|
| **P1** | 导出物种名单 → 取树 → **做名称匹配** | `tree_match_report.txt`:匹配上多少、漏了谁 | 匹配率 < 80% → 先修名称(同物异名表),别硬跑 |
| **P2** | 单棵树跑通全流程,得 β₁ 与 λ | `pgls_single.json` | λ ≈ 1 且 β₁ 不显著 → **立刻停**,回来重新定位结论 |
| **P3** | 100 棵树 × 2 骨架 + 子样本 + AIC 对照 | `pgls_full.json` + 森林图 | — |

**P2 是关键闸门。** 先花半小时看单棵树的结果,再决定要不要投入 P3。

---

## 六 · 预期与预案

**我的预判:β₁ 会保持显著为负,但 |β₁| 比 OLS 小、λ 估计在 0.6–0.9 之间。**
理由:属级聚合(870 个准独立单元)r 仍达 −0.642,说明信号在系统发育结构之上还有余量。

**若预判错了(β₁ 不显著)**,结论改写为:

> 腿长与飞行效率的负相关**主要由系统发育结构承载** —— 即这是一个深层的、
> 在鸟类演化早期就已分化的形态权衡,而非各支系内部反复独立演化的适应。
> 对工程的启示不变(腿是飞行的死重),但**不能宣称是"适应性优化"的证据**。

**这仍然是可写的结论**,只是措辞要弱得多。所以 P2 出来之前,
**论文 A 的主结论先不要定稿**。

---

## 七 · 工作量估计

| 步 | 人工 | 机时 |
|---|---|---|
| P1 名称匹配 | 30 min(若用 AVONET3_BirdTree 表;手工对同义名则 2–4 h) | 分钟 |
| P2 单树跑通 | 1 h | 分钟 |
| P3 100 树 × 2 骨架 | 1 h | **~10 min**(phylolm 线性算法,不构造 n×n 矩阵;若用 corPagel 则 >100 h,不可行) |

**⚠ 内存**:全样本 9146 种的协方差矩阵单棵约 0.7 GB(float64)。100 棵不能同时加载,
必须逐棵算完即弃。若内存吃紧,可先在**属级代表种**(870 种)上做,矩阵降到 6 MB。

---

## 八 · 下一步

**卡在你这边的只有一件事:下树。**

1. `birdtree.org` → Downloads → Hackett 全量树,下 **1 个 zip**(1000 棵);Ericson 同样 1 个
2. figshare 下 AVONET 补充数据里的 **AVONET3_BirdTree** 表(做名字 crosswalk)
3. 两样传到 A100

树到手我写 P2(单树跑通,**关键闸门**)和 P3(100 树 × 2 骨架)的 R 脚本。

**P1 已完成**:`outputs/pgls/species_for_tree.txt`(8831 个 tip_label,0 重名、0 待核对)、
`outputs/pgls/pgls_data.csv`(tip_label / u / HWI / log_m)。
