# 生成阶段实验报告(E1 起,滚动更新)

> 配套《生成阶段实验方案_v1.md》。代码 `src/stage7_generative/`。

## E1 · 条件生成 vs 三基线(2026-08-11,A100)

**设置**:数据工厂 500 工况 × 80 设计 = 4 万次 Exudyn(零失效);训练对 60,250
(工况×约束场景→前沿设计族);cVAE(z=2, 隐层 64×2, β=0.02 预热)训练 300 epoch,
KL≈1.0 无后验坍缩;评测 20 个**留出工况**,每工况新采 150 设计做参考前沿,Exudyn 实判。

**结果**(最优差距 =(选手可行内最优 peak_a − 参考前沿最优)/参考;负值=超过参考采样):

| 选手 | 部署仿真 | 中位差距 | 均值差距 | 失败 | 附加 |
|---|---|---|---|---|---|
| **cVAE 生成** | **0 次** | **2.6%** | 2.9% | 0/20 | 可行率 96%,前沿覆盖 69% |
| LHS 随机 9 | 9 次 | 9.1% | 21.0% | 3/20 | |
| 标度律暖启动+扰动 | 9 次 | 30.4% | 41.7% | 5/20 | |
| BO-9(蓝本式 GPR+EI) | 9 次 | **−0.3%** | −0.3% | 0/20 | 质量冠军 |

**四条结论**:

1. **摊销命题成立**:零仿真、中位 2.6%、零失败,碾压同预算随机基线(9.1%+3 翻车)。
   部署经济学:BO 成本 ∝ 工况数 × 9;生成模型训练一次(4 万仿真,离线),之后边际成本 0。
2. **BO-9 是 3 维空间的质量上限**(−0.3%)——印证方案 §8 "3 维太容易"预判。
   论文口径:**cVAE 以 0 次仿真达到 BO 花 9 次仿真的 ~97% 质量**,非"打败 BO"。
   AI 价值的主战场在扩维(E4:刚度解耦;v2:控制调度)——BO 在高维/混合空间会失速,
   摊销优势则随工况数线性放大。
3. **暖启动崩溃有信息量**(30%/5 翻车):标度律公式的 L1 不依赖 v0,而评测域 v0∈[0.5,2]
   (冲击能量 16×跨度)+ 任意 (g_cap, stroke_max)——单点物理公式接不住全条件依赖,
   **这正是"学习条件映射"相对"物理经验式"的增量**。注意:这不否定标度律本身
   (其推导域是过载约束+速度-体重耦合,见主报告追加 5/6),是"出域使用"的结果。
4. **族生成健康**:覆盖参考前沿行程跨度的 69%,无 mode collapse(KL≈1.0 佐证);
   若需更宽的族:β 0.02→0.01 或 z 2→4。

**诚实边界**:参考前沿本身是 150 点采样估计(非真值),负差距即"超过参考采样"的正常现象;
所有对比是相对档位,不是绝对最优性证明。

**产物**:`outputs/gen_data/factory.jsonl`(4 万仿真库,A100)、`outputs/gen_model/cvae.pt`、
`outputs/gen_eval/eval_results.json`。

## 下一步(按方案矩阵)

- **E2 消融**:生成"相对标度律基线的残差" vs 直接生成绝对值——残差思想在生成层是否依然有效;
- **E3 紧约束压力测试**:stroke_max 压到飞机舱位量级,鸟解不适用区的外推能力;
- **E4 刚度解耦扩维(重点)**:κ_踝/κ_膝/κ_髋 + 阻尼比进设计空间(3→7 维),
  预期 BO-9 失速、生成模型价值凸显;同时回答"工程能否用生物没有的自由度超越生物解"。

## E4 · 刚度解耦扩维:3→7 维(2026-08-11,A100)

**设置**:设计空间扩为 (L1, r2, r3, κ踝, κ膝, κ髋, ζ阻尼比),κ 范围覆盖旧默认 (4,4,16,0.03)
并两侧展开;数据工厂 500 工况 × 120 设计 = 6 万仿真;cVAE z=3;评测协议同 E1
(20 留出工况,裁判 300 设计参考前沿),工况/约束场景与 E1 同分布。

**结果**:

| 选手 | 3维中位差距(E1) | **7维中位差距** | 7维均值 | 7维失败 |
|---|---|---|---|---|
| **cVAE 生成(0 仿真)** | 2.6% | **38.8%** | 57.4% | **0/20**(可行率 98.5%,覆盖 63%) |
| BO-9(9 仿真) | −0.3% | **36.5%** | 41.8% | 2/20 |
| LHS-9 | 9.1% | 100% | 93.6% | 5/20 |
| 标度律暖启动 | 30.4% | 74.6% | 82.4% | 3/20 |

**四条结论**:

1. **BO 失速,预判应验**:−0.3%→36.5%+2 翻车——9 次仿真在 7 维摸不到方向。
   "逐工况优化的价值随维度骤降"获得第二数据点;
2. **cVAE 相对地位反超**:与 BO 同档(38.8 vs 36.5)但零仿真、零翻车、可出族。
   3 维="省钱的次优",7 维起="同档免费还稳"——生成式方法的主场从这里开始;
3. **★ 维度红利实测(E4 科学问题的答案)**:7 维参考前沿的可达峰值显著低于 3 维
   (测试工况 ref 中位 ~54→~37 m/s²,部分场景至 1.7–2.1g)——**解耦刚度解锁了
   配簧规则够不到的性能,工程自由度确实能大幅超越生物约束下的解**。
   (注:两轮测试场景为同分布抽样,非逐一配对,降幅为量级判断);
4. **诚实归因:瓶颈在老师不在学生**。训练前沿来自每工况 120 设计,裁判用 300——
   7 维里 120 铺不满,老师自身离真前沿就有距离;学生可行率 98.5%、零翻车,模型健康。

**下一步(E5,自提升循环)**:cVAE 生成候选 → Exudyn 验证 → 好设计并回训练集 → 重训,
循环 2–3 轮(每轮千次级仿真)。预期把差距从 ~39% 大幅压缩;论文叙事="条件生成 + 主动学习闭环"。

**产物**:`outputs/gen_data7`(6 万仿真库)、`outputs/gen_model7`、`outputs/gen_eval7`。
