# 实验计划 v2.4 · 会议反馈清算 + 水平速度进条件向量

> 输入来源：① Hsia × Bigoni 第一次会议（2026-09-09）的三条外部意见；
> ② 《v2.1 待办清单》里尚未处理的欠账；③ 一条我在读代码时发现的判据漏挂。
>
> 排序原则沿用 `run_v23_main.sh`：**先便宜后昂贵、先决定性后补充**。
> 核心纪律：**A 组和 B 组的结果决定 C 组怎么设计，不许先跑 C。**

---

## 0 · 这一版要回答的三个问题

把会议反馈翻译成可证伪的问题：

| | 问题 | 来自 |
|---|---|---|
| **Q1 几何** | 触地姿态站不站得住？`q1_0 = 50°` 该不该是常数？ | Bigoni 14:30–19:00 |
| **Q2 方向** | 加入水平触地速度后，现有结论会不会翻？ | Bigoni 17:02 + Hsia 16:15 |
| **Q3 非循环** | 管径 D 的标度是不是自己长成了生物的样子？ | Shizhuo 45:45 的总表 |

Q3 是这次唯一**可能直接产出论文级新结论**的一条，而且几乎不花机时，所以排第一个做。

---

## 1 · 分组与依赖

```
A 组（零新增仿真，半天）
  E22 D/L 标度检验 ──────────────┐
  E23 κ/τ 盒壁饱和体检 ──────────┤
  E24 回弹软闸重打分 ────────────┤→ 决定 C 组的盒子/判据怎么设
  E25 后验塌缩量化 ──────────────┘

B 组（新仿真，不重训，1 天）
  E26 q1_0 扫描 ─────────┐
  E27 垂线约束参数化 ────┤→ 决定 q1_0 的归宿，再动工厂
  E28 Cranfield 阶段对照 ┘

C 组（改物理 + 重跑工厂 + 重训，主体）
  P9  足端摩擦探针 ──→ 不通过就停，别跑后面
  v2.4 数据工厂（条件 5→6 维）→ 重训 ×2 种子 → E18b / E20 / E21 全套重跑
```

---

## A 组 · 零新增仿真

> 全部用 `outputs/v23_e20_r12` 和 `v23_e20_r12_s1` 已有产物做后处理。
> 本地就能跑，不占 A100。**建议今天全做完。**

### E22 · D/L 标度律 vs 生物实测 ★最高性价比

**动机**　Shizhuo 45:45 的总表里有一组 **GROUND-foot · bird tarsometatarsus**，正是我们的 L₁：

| 生态类群 | α (d/L ∝ L^α) | n |
|---|---|---|
| 重体型飞行者（鸭/雉/鹤） | **−0.38** | 63 |
| 潜水取食者（鸬鹚/鹭/鲣鸟） | **−0.57** | 19 |
| 涉禽 | −0.12 | 24 |
| 远洋翱翔 | +0.02 | 25 |

我们的 `size_segment` 是**纯力学**地从应力+屈曲反推 D 的，**没有用过任何生物的 D 信息**。
所以"我们的 α 是否等于鸟的 α"是一个**真正非循环**的检验 —— 和 κ 一样干净，
不会重蹈"幂律写进参数化"的覆辙。

**做法**
1. 从 `e20_bio_raw.npz` 取每个工况可行集里每条设计的 `D_mm[3]` 和 `seg_len[3]`
2. 对跗跖骨段（L₁）回归 `log10(D/L) ~ log10(L)`，得 α_model 及 95% CI
3. 分工况（硬地/草地/湿沙）分别做，看 α 是否随地面变化
4. 三段骨都做一遍，L₂/L₃ 和鸟的股骨（+0.12）、肱骨（−0.13）对照

**判据**
- `|α_model − α_bird| < 0.10` 且 CI 不跨 0 → **强结论，直接进论文和下次汇报**
- 差很多 → 也是结论，指向 Bigoni 33:04 说的"鸟骨是桁架、你的是薄壁圆管"

**必须同时报告的风险量**　D 受 `D_MIN = 4 mm` 和 `D_MAX_RATIO = 0.25` 上下夹逼。
如果大量样本贴在这两个边界上，α 就是被夹出来的、不是力学长出来的。
**必须报告被夹住的样本比例**，超过 20% 这条结论就不能要。

**产出**　`src/stage6_surrogate/e22_dl_scaling.py` → 一张图 + `e22_dl_scaling.json`
**机时**　0

---

### E23 · κ / τ 盒壁饱和体检

**动机**　待办 C4 说 τ 和 κ踝 贴着下界，但依据只有"5 个设计、单次生成"，
清单里自己写着"这是旗标不是结论"。而 C 组要重设盒子，必须先把这件事做实。

**做法**　对 r12 两个种子的**全部可行设计**，每一维算归一化位置
`u = (x − lo)/(hi − lo)`，画 9 维的分布直方图；统计 `u < 0.05` 或 `u > 0.95` 的比例。

**判据**
- 任何一维贴壁比例 > 20% → **该维在 C 组必须放宽**，并记录放宽前后的对比
- 全部 < 5% → 盒子够宽，答辩时可以直接把这张图甩出去

**产出**　`e23_box_saturation.py` → 9 联图
**机时**　0

---

### E24 · 回弹软闸补挂 ★这是个漏挂的判据

**发现**　`physics_v2.feasible_v2` 里只有四条判据：

```python
if r["peak_a"] > gcap:      bad.append("gcap")
if r["leg_stroke"] > smax:  bad.append("smax")
if r["struct_over"]:        bad.append("slenderness")
if r["mass_over"]:          bad.append("massbudget")
```

**没有回弹**。而 `hf_exudyn._metrics` 从头到尾都在算 `rebound` 和 `n_bounce`
（第 107 / 112 行），只是从没被用过。《回弹判据调研》v3 修订已经把判据定死了：

> 软闸为唯一判罚：**回能比 = rebound ÷ (v₀²/2g) ≤ 5%**；足端离地只记录不判罚。

所以这不需要讨论，照着实现即可。

**做法**　用已存的 `rebound` 对全部 r12 结果重打分，统计：
1. 现有"可行"设计里有多少会被软闸打掉
2. 打掉的集中在哪些工况（预期：高速 + 低阻尼）

**判据**
- 打掉 < 5% → 无关紧要，写进报告，C 组顺手把闸接上
- 打掉 > 20% → **现有可行率是虚高的**，必须在 C 组重训前接上，否则训练目标就是错的

**产出**　`e24_rebound_gate.py` + 修改 `feasible_v2`（加 `--reb-cap` 参数，默认 0.05）
**机时**　0

---

### E25 · 后验塌缩量化

**动机**　待办 E1：同条件 216 次采样，7 维中位 CV 只有 0.21% —— 解码器忽略了 z，
实际是个确定性回归器。这不影响 E5 自提升（探索来自 LHS），但**影响所有"生成设计族"的表述**。

**做法**　在 r12 模型上复测，每个工况采 216 次，报告 9 维各自的 CV 分布。

**判据**　这不是要修，是要**定口径**。确认塌缩后：
- PPT / 论文里所有"生成一族设计""多个解"的说法全部改写
- 改成"条件到设计的确定性映射"，并如实说明这是当前实现的性质
- 会议上赵老师 12:16 已经替你说过一次「it is not necessarily the best one, it is one solution」
  —— 口径要和这句话对齐

**产出**　`e25_posterior_collapse.py`
**机时**　0（纯推理）

---

## B 组 · 新仿真，不重训

> 复用现有 cVAE 的输出设计，只改物理场景重摔。不动训练。

### E26 · q1_0 扫描 ★Bigoni 主线

**动机**　`SCEN_BIRD_X` 里 `q1_0 = 50°` 是写死的场景常数，不在 9 维设计向量里，
但它决定整条腿的倾斜。三个证据指向它偏小：

| 来源 | 值 |
|---|---|
| 现值 | 50° |
| 我量 Bigoni 那张骨骼图的跗跖骨 | **≈ 71°** |
| 反解"髋在足正上方" | **63–76°** |

现状后果（我已用 `hist_swan.npz` 算过）：髋部水平偏出 100–173 mm，
**且压缩过程中偏移几乎不变、倾角反而略增**（13.4°→13.8°，26.2°→26.8°）——
这条腿不会自己转正，因为模型里没有水平力去转它。

**做法**　取 cVAE 在 6 个代表工况的输出设计，固定 9 维不变，只扫
`q1_0 ∈ {40, 50, 63, 71, 80, 90}` 外加一档**"由垂线约束逐条反解"**，
每档记录 `peak_a / leg_stroke / 髋-足水平偏移 / D_mm / 可行性`。

**判据**
- 峰值或行程对 q1_0 的敏感度 > 20% → **q1_0 必须升为设计变量或被约束固定**，不能留作常数
- < 5% → 它只是画图问题，改个值标个箭头就完事

**预期方向（要先想到，免得被结果吓到）**　q1_0 变大 → 腿更竖直 → 可用行程变小 → 峰值变高。
所以"垂线对齐"很可能是**牺牲性能换生物一致性**。如果是这样，这本身就是个值得报告的 trade-off。

**产出**　`e26_q1_sweep.py`
**机时**　小（6 工况 × 7 档 × 若干设计）

---

### E27 · 垂线约束参数化

**动机**　三个角其实不独立。给定 (r₂, r₃, θ_A, θ_K)，"髋在足正上方"这个约束
**唯一确定** q1_0：

```
L₁[cos a₁ + r₂·cos(a₁ + 180° − θ_A) + r₃·cos(a₁ + θ_K − θ_A)] = 0
```

这是一个**降维**：与其把 q1_0 加成第 10 维，不如把它由约束解出来。
更生物、更少一维、而且直接回应了 Bigoni。

**做法**　在 E26 基础上，比较两种参数化下的**可行域体积**：
(a) q1_0 自由（第 10 维）　(b) q1_0 由垂线约束反解

**判据**　约束版可行率损失 < 10% → **采纳约束版**，写进 `bioprior.expand`

**产出**　`e27_vertical_constraint.py`

---

### E28 · 与 Cranfield 四阶段的姿态对照

**动机**　赵老师 23:30 那张 "Flexion & Extension Dynamics" 给了绿头鸭着水四阶段，
触水时刻 (d) 的定性构型是 **TMPJ 在 ITJ 前方、FT 朝前**。
我们的触地构型落在哪一段，是可以直接算的。

**做法**　把生成设计的触地构型按同样的三点前后关系分类，看落在 (a)–(d) 哪一段。

**判据**　落在 (d) → 下次汇报可以**用赵老师自己的数据回应 Bigoni**：
"我们的触地姿态与你们测的鸭子第四阶段一致，Bigoni 教授当时按从左往右的运动方向读图，
所以看成了第一阶段。"

**⚠ 需要先问赵老师**　那份 Cranfield 数据的关节角定义、ITJ/TMPJ/FT 的标注约定、
以及原始数据能不能拿到。**这是 B 组唯一有外部依赖的一条。**

---

## C 组 · 改物理 + 重跑工厂（主体）

### P9 · 足端摩擦探针（把门的那一步）

**角色**　等同 `run_v23_main.sh` 里的 `0/5` —— 先花小机时确认新物理数值上稳，
不稳就别跑后面几小时。

**改动**　`hf_exudyn.py`：足端接触加切向库仑摩擦；初速度加水平分量 v_x。

**做法**　64 个探针设计 × 5 个质量 × v_x ∈ {0, 0.5, 1, 2, 3} m/s × μ ∈ {0.3, 0.6}

**判据（任何一条不过就停）**
1. solver 失败率 ≤ 现有水平（待办 B3 说小质量+硬地本来就有失败，不能更糟）
2. v_x = 0 时结果与现有 v2.3 逐条一致（**回归测试，必须过**）
3. 水平滑行距离量级合理（不出现足端穿透或无限滑移）

---

### C1 · 水平触地速度进条件向量

**条件向量 5 → 6 维**：`[log10 m, v0, log10 kc, g_cap, s_max]` **+ v_x**

**判据体系要跟着改**　现在判的是垂直峰值。加了 v_x 之后应该判**合加速度**
（我估过：我们的腿 μ≈0.35 时合加速度 +6%；滑橇橡胶 μ≈0.8 时 +28% —— 滑橇在这一项上吃更大的亏，
这对第 11 页那个对比是**有利**的，值得算清楚）。

**⚠ 两个必须你来定的开放问题**

1. **v_x 的范围**。VTOL 侧风/漂移是 0–3 m/s；固定翼滑跑是 15–30 m/s。
   **这两个量级差十倍，我强烈建议不要塞进同一个条件向量** ——
   条件空间被拉伸十倍，采样密度会被稀释，很可能两头都学不好。
   建议 v2.4 只做 **0–3 m/s**，滑跑着陆作为 v3 单独立项（那时多半还得上轮子）。
   这也和你在会上说的口径一致："目前只比较垂直降落的情况"。

2. **μ 从哪来**。打印尼龙–混凝土有手册值（0.3–0.4），
   草地/湿沙**没有可引的数**。又是一个"本工作设定"，
   而 A0 页的来源表里这类空格已经有两个了（κ 和 τ）。
   要么找岩土文献补，要么老实标"量级估计"。

---

### C2 · 数据工厂重跑 + 重训

依赖 P9 三条判据全过。沿用 `run_v24_main.sh`，五段结构照抄 v2.3，条件维度 +1。
两个种子（规矩：训练类指标 ≥2 种子才报）。

**顺带一起处理的欠账**（都要改盒子，必须合并在同一次重跑里，不要分两次）：

| 待办 | 内容 | 是否并入 |
|---|---|---|
| E23 结果 | κ/τ 盒壁放宽 | **是**（如果贴壁 >20%） |
| E24 结果 | 回弹软闸接进 `feasible_v2` | **是** |
| E27 结果 | q1_0 由垂线约束反解 | **是**（如果可行率损失 <10%） |
| B1 | σ 由残差直接算（0.0784 → 0.0819） | **是**，一行的事 |
| A1/A2 | ζ→τ 改名、n 统一 213 | **是**，无损 |
| C1（待办） | 三维条件先验 p(L1,r2,r3\|m) | **待定，见下** |

---

### C3 · 下游全套重跑

`E18b 走廊` / `E20 生成走廊` / `E21 真鸟对比`，外加把 **E22 的 D/L 检验**做成常规产物。

---

## D 组 · 明确排除（防止范围蔓延）

| 项 | 来源 | 为什么不做 |
|---|---|---|
| 主动着陆 / 触地前姿态调节 | 赵 20:33 的观察 | 是控制问题，要在 exudyn 里加时变约束；且 Airbus 说工程上不安全（21:09）。**v3** |
| 猫 / 袋鼠等跨物种 | Bigoni 55:47 | 需要新的数据管线。**v3** |
| 鸟骨桁架截面 | Bigoni 33:04 | **等 E22 的 D/L 结果出来再决定值不值得** |
| 滑跑着陆（v_x 15–30 m/s） | — | 量级不兼容，见 C1 开放问题 1 |

---

## 排期建议

| 天 | 内容 | 机时 |
|---|---|---|
| D1 | A 组全部（E22–E25） | 0，本地 |
| D2 | B 组（E26–E28）+ P9 探针 | 小 |
| D3 | 根据 A/B 结果定盒子与判据，改 `bioprior` / `feasible_v2` / `hf_exudyn` | 0 |
| D4– | C 组主体：工厂 → 重训 ×2 → 下游 | **上次 `run_v23_main.sh` 的时长 × 约 1.2** |

> **需要你填**：上次 `run_v23_main.sh` 全跑一遍实际多久？我按此估 v2.4 的窗口。
> 日志在 A100 上（本地 `logs/` 目录不存在，只下载了 outputs）。

---

## 现在需要你拍板的四件事

1. **着陆方向约定** —— 落震图左右镜像 + 加运动方向箭头？（我建议两个都做）
2. **q1_0 的归宿** —— 升为第 10 维设计变量 / 由垂线约束反解 / 保持常数但改成 ~65°？
   （我建议先看 E26 敏感度，再在"约束反解"和"保持常数"之间选，**不建议加第 10 维**）
3. **v_x 范围** —— 0–3 m/s（我的建议），还是要覆盖滑跑？
4. **待办 C1（三维条件先验 r₂/r₃ 随 L₁ 漂移）要不要并进这次工厂重跑？**
   - 并入：省一次昂贵重跑；代价是这次改动变多，出问题不好归因
   - 不并：归因干净，但半年内多半要再重跑一次工厂
   - 我倾向**并入**，因为它的函数形式已经完全确定（`r2(L1) = r2_ref·(L1/L1_ref)^(p−1)`，
     p−1 = −0.123 / −0.234），不是探索性改动，归因风险可控

---

## 脚本清单（供 A100 部署）

```
src/stage6_surrogate/
  e22_dl_scaling.py          # A组·D/L 标度 vs 生物            [新建]
src/stage10_v2/
  e23_box_saturation.py      # A组·盒壁饱和                    [新建]
  e24_rebound_gate.py        # A组·回弹软闸重打分              [新建]
  e25_posterior_collapse.py  # A组·后验塌缩                    [新建]
  e26_q1_sweep.py            # B组·q1_0 扫描                   [新建]
  e27_vertical_constraint.py # B组·垂线约束参数化              [新建]
  e28_cranfield_stage.py     # B组·四阶段姿态对照              [新建，依赖赵老师数据]
  p9_foot_friction.py        # C组·摩擦探针                    [新建]
  physics_v2.py              # feasible_v2 加回弹软闸           [改]
  bioprior.py                # σ 修正、q1_0 约束、盒子放宽      [改]
  factory_v2.py              # 条件 6 维、v_x 采样              [改]
src/stage6_surrogate/
  hf_exudyn.py               # 足端切向摩擦、水平初速、新指标   [改]
run_v24_main.sh              # 五段结构照抄 v2.3               [新建]
```
