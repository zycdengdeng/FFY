# 实验计划 v2.5 · 会议反馈清算 + 水平速度进条件向量

> **v2.5 相对 v2.4 的变化**（2026-09-10）
> - **E28 已完成**，赵老师的 Cranfield 数据就在 `FFY/duong_data`，不再是外部依赖。
>   结果见 `E28_Cranfield对照_结果.md` 和 `F_duong_e28.png`。
> - 据此**决策 ② 定案**：`q1_0` 保持常数 50°，不升维、不做垂线反解。E26 降级、E27 改性质。
> - 决策 ① 采纳（镜像 + 箭头都做）、决策 ④ 采纳（待办 C1 并入本次重跑）。
> - **决策 ③ 定案：走方案 C** —— 主工厂 `Fr ∈ [0,2]`，P9b 探针把 Fr 推到 15 只为定位崩溃点。
>   条件向量第 6 维放 **Froude 数 Fr**，不是 v_x。见「方案 C 总述」。
> - **A 组 + B 组已在 A100 全部跑完**（2026-09-10）。E22–E28 七条实验全部有结果，
>   原始产物在 `reports/ab_group/`。下面各条已改写成结论。
> - 🔁 **决策 ② 翻案**：`q1_0` 从"保持常数 50°"改为**升为第 10 维设计变量**，
>   但**必须与足端摩擦（P9）绑定**，否则生成器会跑到会打滑的姿态。理由见 E26。
> - **`run_v23_main.sh` 的真实时长拿到了：822 分钟（13.7 小时）**，见排期一节。
> - **E22 的生物侧数字已核实**（逐行读了会议帧 t_00-45-45.jpg），`e22_bio_ref.json` 已填好。
>   口径确认是 **d/L ∝ L^α**（细长比），不是 D ∝ L^α。它仍**不是零成本**（D 没落盘，要本地重评）。
> - **会议文字稿一直都在**：`Hisa x Davide 会议\第一次会议.txt`（48 KB，825 行，01:07:00）。
>   本版引用的每个时间点都已回原文核对，见下面各条。

> 输入来源：① Hsia × Bigoni 第一次会议（2026-09-09）的三条外部意见；
> ② 《v2.1 待办清单》里尚未处理的欠账；③ 一条我在读代码时发现的判据漏挂。
>
> 排序原则沿用 `run_v23_main.sh`：**先便宜后昂贵、先决定性后补充**。
> 核心纪律：**A 组和 B 组的结果决定 C 组怎么设计，不许先跑 C。**

---

## 0 · 这一版要回答的三个问题

把会议反馈翻译成可证伪的问题：

| | 问题 | 来自 |
|---|---|---|
| **Q1 几何** | ~~触地姿态站不站得住？~~ **✅ E28 已答：站得住，`q1_0=50°` 保持常数** | Bigoni 14:30–19:00 |
| **Q2 方向** | 加入水平触地速度后，现有结论会不会翻？**范围已定：Fr∈[0,2]** | Bigoni 17:02 + Hsia 16:15 |
| **Q3 非循环** | 管径 D 的标度是不是自己长成了生物的样子？ | Shizhuo 45:45 的总表 |

Q3 是这次唯一**可能直接产出论文级新结论**的一条，而且几乎不花机时，所以排第一个做。

---

## 1 · 分组与依赖

```
A 组（本地后处理，不占 A100）
  E22 D/L 标度检验 ── 脚本就绪，待跑（要重评，非零成本）+ 待核实生物侧
  E23 κ/τ 盒壁饱和 ── ✅ 盒子不动，前沿有偏置
  E24 回弹软闸重打分 ─ ✅ **打掉 30.2%，软闸必须接**
  E25 后验塌缩量化 ── ✅ 未塌缩，待办 E1 关闭

B 组（新仿真，不重训，1 天）
  E26 q1_0 敏感度（28-55°）─┐ 脚本就绪；几何部分 ✅ 已出（垂线要 63°，在实测跨度外）
  E27 竖直化的结构收益 ─────┘ 复用 E26 缓存，零新增仿真
  E28 Cranfield 对照 ✅ 已完成 → 已定案 q1_0 = 50° 常数

C 组（改物理 + 重跑工厂 + 重训，主体）
  P9a 准入体检（Fr 0–2）──→ 四条不全过就停，别跑后面
  P9b 崩溃阶梯（Fr 2–15）─→ 不设通过与否，产出就是"在哪一档崩"
  v2.5 数据工厂（条件 5→6 维，第 6 维 = Fr）→ 重训 ×2 种子 → E18b / E20 / E21 全套重跑
```

---

## A 组 · 本地后处理（不占 A100）

> 数据源是 `outputs/v23_data_bio/factory.jsonl`（500 块 × 120 = 240,000 个样本）
> 和 `outputs/v23_e5_bio/cvae_r12.pt`，不是 `v23_e20_r12`（后者只存了 e20 走廊的产物）。
>
> 共用模块 `agroup_io.py` 刻意**不 import physics_v2 / factory_v2** —— 那条链会拉进 exudyn，
> 而 A 组是纯后处理，应该在任何一台只有 numpy 的机器上都能跑。
> 常量在 `selfcheck()` 里与真模块比对，防止两边悄悄漂开。
>
> **E23 / E24 / E25 已经跑完**（几秒到一分钟）。E22 要重评，见下。

### E22 · D/L 标度律 vs 生物实测　✅ **已完成，结论：不一致，而且不一致本身是结果**

**数据**　从 `factory.jsonl` 按质量对数轴分层抽 3,000 个可行设计重跑 `eval_v2`，
取回 `D_mm` / `seg_len` / `governs`。脚本 `e22_dl_scaling.py`。

**① 夹逼诊断：0.0%** —— 三段合计只有 0.1% 触到 `D_MAX_RATIO`，`D_MIN` 一个都没碰到。
**指数完全可用。** 支配判据：`bending+axial` 99%、`buckling` 1%、`OVER-SLENDERNESS` 0%。

**② 口径的坑（这条必须写清楚，否则会得出相反结论）**

| 口径 | tarso | tibio | femur | 合并三段 |
|---|---|---|---|---|
| `log D ~ α log L`（全样本） | +0.699 | +0.628 | +0.657 | **+0.437** |
| `log D ~ α log m`（全样本） | +0.383 | +0.376 | +0.366 | +0.375 |
| `log (D/L) ~ α log L`（全样本） | −0.301 | −0.372 | −0.343 | **−0.563** |
| **`log (D/L) ~ α log L`（跨质量分箱）** | **+0.058 ± 0.081** | **+0.018 ± 0.081** | **−0.001 ± 0.040** | — |

两个坑：

- **「合并三段」是统计假象，不能用。** 逐段是 −0.30/−0.37/−0.34，合并却成 −0.563 ——
  三段骨头长度量级差很多，混在一起回归得到的是**组间斜率**（Simpson 悖论那一类），不是物理。
- **对 L 直接回归也不对。** 我们的 L 有两个来源：体重，以及 `u_L` 给的 ±2.5σ 设计自由度；
  而生物那张图是**跨物种**回归，L 的差异**只来自体重**。两者问的不是同一个问题。
  所以加了「跨质量分箱」口径：按体重分 12 箱、每箱取中位再回归，只保留跨质量那一份变化。
  **比生物只能用这一组。**

**③ 结论：我们的定尺是等比的，鸟不是。**

跨质量口径下 α ≈ **0**（+0.058 / +0.018 / −0.001）—— 也就是 **D 和 L 同速增长，细长比不随体重变**。
对照 `L ~ m^0.361` 与 `D ~ m^0.383`，两者几乎相等，自洽。

生物侧（重体型飞行者，鸭/雉/鹤，n=63）跗跖骨是 **−0.38**：**大鸟的骨头相对更细长**。

**差 10.6σ，不一致。** 这不是失败，这正是"非循环检验"该有的样子 ——
`size_segment` 从来没用过生物的 D 信息，它长出来的形状和鸟不一样，
说明**鸟的骨形不是由我们这套落震定尺准则决定的**。

**④ 机制上说得通，而且能落到会上那句话**

会上 47:02–47:27，Hsia 问 Shizhuo「what does negative half mean」，
答的是 *"bending stiffness, if it's constant loading… and buckling"* ——
**α = −1/2 是定载荷下的 Euler 屈曲**。对照我们的：

| | 载荷 | 支配判据 | α |
|---|---|---|---|
| 鸟（生物图） | 与体重弱相关？ | 接近定载荷屈曲 | −0.38 ~ −0.57 |
| **本模型** | **F ∝ m**（验收把 peak_g 压在上限附近，所以力正比于质量） | **弯曲+轴压强度 99%，屈曲仅 1%** | **≈ 0** |

**两处都不同：载荷是否随质量放大、以及由强度还是屈曲定尺。** 这是一句可证伪、可写进论文的话。
下一步要做的不是"把 α 掰到 −0.38"，而是查清楚是哪一处造成的差异。

**⑤ 顺手记一笔**　`eval_v2` 其实返回了 `D_mm` / `seg_len`，只是 factory 没落盘。
**v2.5 的 `KEYS_V2` 要把这两项加上**，以后这条实验就真是零成本。

**产出**　`e22_dl_scaling.py` + `e22_bio_ref.json` → `e22_dl_scaling.json` + 两联图
**机时**　3,000 次重评，A100 上约 2 分钟

---

### E23 · κ / τ 盒壁饱和体检　✅ **已完成，结论：盒子不动**

**做法**　对 `v23_data_bio/factory.jsonl` 全部 240,000 个样本、40,509 个（去重）可行设计，
在 u 空间统计各维分布。脚本 `e23_box_saturation.py`。

**结论一：全部可行设计在盒里基本均匀。** 九维贴壁率（u<0.05 或 u>0.95）都在 5% 基线附近，
最高 6.5%，远低于 20% 的触发阈值。**盒子没有把可行域切掉，v2.5 不放宽。**

**结论二：但 Pareto 前沿系统性地压在一侧。**

| 维 | 全体中位 u | 前沿中位 u | 物理量 |
|---|---|---|---|
| τ (ζ) | 0.486 | **0.206** | 0.025 s（盒 0.005–0.10） |
| κ 踝 | 0.506 | **0.321** | 3.1（盒 0.75–8.0） |
| θ_A | 0.474 | **0.344** | 129°（盒 113–160°） |
| L1 | 0.582 | **0.650** | 比异速中位长 14% |

**待办 C4 可以关掉，但要换个说法**：它用 5 个设计看到的是"前沿偏爱下界"，
这是对的；错的是把它说成"盒子不够大"。放宽盒子只会把前沿往外挪，
不会让更多设计变可行。

**这条偏置本身是结果**：落震最优偏爱**低阻尼、软踝、更屈的踝角、更长的腿**。
接得上 E28 —— 生成器的最优踝角 129° 比真鸟落水实测的 140°±14° 更屈，
腿比异速中位长 14%，都在生物学跨度内但方向系统。

**产出**　`e23_box_saturation.py` → `e23_box_saturation.json` + 九联图
**机时**　0（本地几秒）

---

### E24 · 回弹软闸补挂　🔴 **已完成，结论：必须接，训练目标现在是错的**

**发现**　`physics_v2.feasible_v2` 里只有四条判据 —— gcap / smax / slenderness / massbudget，
**没有回弹**。而 `hf_exudyn._metrics` 从头到尾都在算 `rebound`（第 107 行）和
`n_bounce`（第 112 行），《回弹判据调研》v3 也早把判据定死了：
回能比 R = rebound / (v₀²/2g) ≤ 5% 为软闸，足端离地只记录。

**重打分结果**（脚本 `e24_rebound_gate.py`，240,000 样本）

```
现判据下可行        109,040  (45.4%)
其中回能比 > 5%      32,940  (占可行的 30.2%)
进训练集的前沿点     13,095，其中被打掉 3,099 (23.7%)
可行样本回能比分位   P50=0.000  P75=0.068  P90=0.138  P95=0.185  P99=0.289
足端离地占可行样本   71.0%
```

**30.2% > 20%** —— 现有"可行"里有三成会弹起来，**进训练集的 Pareto 前沿里有 23.7% 会弹**。
cVAE 现在学的目标里混了近四分之一会弹的设计。
**必须在 v2.5 重训前把软闸接进 `feasible_v2`，这不是可选项。**

打掉比例随质量单调上升：2 kg 约 22%，30 kg 约 37% —— 重机器更容易弹，
与"腿相对更细、储能占比更高"一致。

足端离地 71% 这个数太大，值得在论文里单列一句：接触不是单次的。

**产出**　`e24_rebound_gate.py` → `e24_rebound_gate.json` + 两联图；
`physics_v2.feasible_v2` 加 `--reb-cap` 参数（默认 0.05）**[待改]**
**机时**　0

---

### E25 · 后验塌缩量化　✅ **已完成，结论：没塌缩，待办 E1 关闭**

**做法**　四个互相独立的诊断，都不需要仿真（脚本 `e25_posterior_collapse.py`，模型 `cvae_r12.pt`）。

```
① 编码器每维 KL：z0 0.220 / z1 0.224 / z2 0.599 nat，总 1.043
   死维 0/3，active units 3/3
② 同条件采 216 次：σ_z 中位 = 盒宽的 5.9%
③ σ_z / σ_data 中位 = 0.21（只保留数据自身多样性的 21%）
④ 复现 E1 口径（物理 x 空间 CV）：9 维中位 4.65%，E1 报的是 0.21% —— 差 22 倍
```

**待办 E1 的塌缩结论在 v2.3 r12 上不复现。** E1 测的是 v2 的 7 维模型，
此后换过先验、加过姿态两维、重训过多轮。这条待办关掉，但把这个数写进报告，
别让旧结论留在文档里。

**口径还是建议保守，但理由换了** —— 从"塌缩"换成"覆盖不足"：
多样性只有数据自身的 21%，模型在往条件均值收（cVAE 的常态，不是缺陷）。

> ✓ 给定工况，模型给出满足验收条件的仿生设计；采样能给出若干变体，
> 但**不宣称覆盖了全部可行解，也不宣称其中有最优解**。
> —— 与赵老师 12:16 原话一致：*it is one solution, not necessarily the best one*。

要跟着改的地方：PPT 第 9 页的措辞；论文里凡是出现 p(u|c)「分布」「采样」「多样性」的句子；
E20 走廊图的图注（走廊宽度来自**条件在变**，不是同条件采样）。

**产出**　`e25_posterior_collapse.py` → `e25_posterior_collapse.json` + 两联图
**机时**　0（纯推理）

---

## B 组 · 新仿真，不重训

> 复用现有 cVAE 的输出设计，只改物理场景重摔。不动训练。

### E26 · q1_0 敏感度　🔁 **已完成，结论翻案：升为第 10 维，但必须和摩擦绑定**

**做法**　6 个代表工况 × 24 个 cVAE 设计 = 144 条腿，固定 9 维不动，
只扫 `q1_0 ∈ {28, 33, 38, 43, 48, 50, 55, 62, 70, 80}°`，共 1,584 次落震，全部成功。

**① 判据：中位 69.7% ≫ 25%**

生物学跨度（27.6–54.8°）内 peak_g 的相对变化：中位 **69.7%**、P90 84.9%、最大 212%，
六个工况分别 61.2 / 73.6 / 69.6 / 67.0 / 69.3 / 75.0%，**没有一个低于 60%**。

> **决策 ② 翻案。** E28 说 50° 在生物学上站得住（实测 43°±9°），这没错；
> 但"生物上合理"不等于"对性能不重要"。**q1_0 必须进设计向量。**

**② 响应形状：强非线性，而且最好的一档在最低角**

```
逐档中位（每个设计先对自己 50° 的值归一化）
  28.0°   0.42×   ██████████████
  33.0°   0.72×   █████████████████████████
  38.0°   0.93×   ████████████████████████████████
  43.0°   0.97×   █████████████████████████████████   ← 真鸟均值 42.6°
  48.0°   0.99×
  50.0°   1.00×   ← 模型常数
  55.0°   1.01×
  62.0°   1.01×   ← 垂线对齐
  70.0°   0.99×
  80.0°   0.93×
```

`d(peak_g)/d(q1_0)` 中位 **+0.167 g/度，144 个设计 100% 为正**。
总变化 140%，其中 90% 发生在 **28–43°**，之上是平台。

这解释了①（跨度变化大）和"50°→垂线档只变 0.6%"为什么不矛盾：**50° 已经在平台上。**
也就是说 —— **现在的常数 50° 正好选在最差的那一段，28° 的峰值只有它的 42%。**

**③ 但这个收益多半拿不到 —— 全部落在足端会打滑的区间里** ★

把腿近似成沿"足-髋连线"受力的二力杆，足端需要的摩擦系数 ≈ tan(前倾角)：

| q1_0 | 前倾角 | μ_需要 | peak(相对 50°) | 打印尼龙–混凝土 μ≈0.30–0.40 |
|---|---|---|---|---|
| **28°** | 35.1° | **0.70** | **0.42×** | **滑** |
| 33° | 30.1° | 0.58 | 0.72× | **滑** |
| 38° | 25.1° | 0.47 | 0.93× | **滑** |
| 43° | 20.1° | 0.37 | 0.97× | 临界 |
| 48° | 15.1° | 0.27 | 0.99× | 安全 |
| **50°** | 13.1° | **0.23** | 1.00× | 安全 |
| 62° | 1.2° | 0.02 | 1.01× | 安全 |

**峰值过载的收益全部落在会打滑的区间里。**
要在 28° 拿到 0.42×，需要 μ ≥ 0.70；打印尼龙对混凝土只有 0.30–0.40。
而**现在的常数 50°（μ_需要 = 0.23）恰好在这套材料站得住的最倾斜姿态附近** —— 当初选它是撞上的。

两条推论：

1. **q1_0 升为第 10 维必须和足端摩擦一起做**（P9 / C1）。
   现在的模型里机体被 `CreatePrismaticJoint(axis=[0,0,1])` 锁成只能竖直平动，
   水平力由约束反力**免费**承担，所以生成器会一路跑到下界 28°，
   产出一批**在真实地面上会打滑**的设计。这不是可选项，是前提。
2. **真鸟能用 30.5° 前倾（E28 实测），是因为它们落在水面/软地、而且有脚趾可以抓**，
   不受 μ≈0.35 的约束。这条生物-工程对照值得单独写一段 ——
   它同时解释了"为什么我们不能照抄鸟的姿态"。

> ⚠ tan(前倾角) 只是量级估计：关节有扭簧，足端力并不严格沿足-髋连线。
> **P9 要直接测水平/竖直地反力的峰值比，把这一栏换成实测。**

**产出**　`e26_q1_sweep.py` → `e26_q1_sweep.json`（含 `curve_median` / `mu_required`）+ 三联图
**机时**　1,584 次落震

---

### E27 · 竖直化的结构收益　✅ **已完成，结论：关掉**

**性质**　E28 + E26 几何已经否掉了"髋在足正上方"的生物学依据，
原方案（垂线反解 q1_0、给 bioprior 降一维）撤销。剩下的是纯力学问句：
竖直化能不能靠轴压替代弯曲把腿减轻。复用 E26 缓存，零新增仿真。

**结果：不能，而且是纯亏。** 从 50° 走到垂线档（q1_0\* 中位 63.1°，前倾 13.1° → 0°）：

| | 变化 | 好的方向 |
|---|---|---|
| 腿质量 | **+4.9%**（四分位 +2.9 ~ +7.7%） | ↓ |
| 最大管径 | +6.1% | ↓ |
| 峰值过载 | +0.6% | ↓ |
| 可用行程 | −0.4% | ↑ |
| 通过验收 | 80% → **76%** | ↑ |

弯曲应力占比 87.5% → 87.0%，**几乎没动**。分段看，只有跗跖骨的弯曲占比随竖直化明显下降
（95.6% @28° → 74.2% @80°），胫跗骨和股骨反而回升。

**原因说得清**：`size_segment` 里的 D 主要由**关节峰值力矩**定，
而关节力矩来自弹簧刚度 × 转角，不是来自髋-足偏移的静力臂。
把腿摆竖直并不会显著卸掉弯矩，反而因为行程变小、力变大，管径还粗了。

**这条彻底关掉。** 回应 Bigoni 时可以直接给这张表：他的直觉在静力学上成立，
但在落震这个工况下，弯矩由关节刚度主导，不由姿态主导。

**⚠ 同一个边界**　水平力目前由 prismatic 约束免费承担。v2.5 加 v_x 与摩擦后受力图会变，
**这条要重新测一次**（和 E26 的 ③ 是同一件事）。

**产出**　`e27_vertical_structural.py` → `e27_vertical_structural.json` + 三联图
**机时**　0

---

### E28 · 与 Cranfield 数据的姿态对照　✅ **已完成，机时 0**

**数据到手**　就是 `C:\Users\ZihanWANG\Desktop\FFY\duong_data` ——
Duong Vu (Cranfield MSc CSTE, 2025) 的 DeepLabCut 项目，
12 次水鸟落水（鸭 3 / 雁 3 / 鹈鹕 3 / 天鹅 3），30 fps 逐帧 16 关键点。
`Duck_1_final` 剔除（压缩后髋点被跟到足下方，跟踪失败），有效 11 次。
角度由本工作从原始关键点独立重建，与其 `kinematic_processed_dataset.csv`
交叉核对：ITJ 均值差 1.8°、KNEE 均值差 2.4°，约定一致。

**骨骼命名对应**　`ankle→foot` = 跗跖骨 = 我们的 **L₁**；`ITJ_angle` = **θ_A**；`KNEE_angle` = **θ_K**。

**结论三条**

1. **两个角度盒被真实落水完全覆盖，一次没漏。**
   θ_A 实测 140° ± 14°（113.5–159.9）vs 盒 `(113, 160)`；
   θ_K 实测 135° ± 12°（118.1–156.8）vs 盒 `(118, 157)`。
   ⚠ **已从代码确证不是独立验证。** `bioprior.py` 第 50 行的注释白纸黑字写着：
   > `# 1) 姿态进设计向量(度):范围 = Duong 12 段着水视频 water_contact 帧实测全距。`

   所以 `THA_RANGE` / `THK_RANGE` 就是从这份数据定的，**不能写成"外部数据独立验证了我们的先验"**。
   能写的是：盒边界有明确的实验出处（可引 Duong 2025），且 11 次落水无一越界。

2. **`q1_0 = 50°` 站得住。** 跗跖骨与水平面夹角实测 **43° ± 9°**（27.6–54.8°），50° 在 +0.8σ 内。
   > 修正旧结论：早先我从那张鸟骨骼**示意图**量到 ≈71° 并说"髋在足正上方"，那是**站立**姿态，作废。

3. **"髋在足正上方"不是触地时的生物学事实。**
   触水瞬间髋-足前倾角 **30.5° ± 6.4°**，11 次全在 19–38°，无一接近 0；
   从触水到最大压缩，跗跖骨**变平 7.7°**（9/11 次为负），前倾不减反增。
   与我们仿真结论一致（天鹅几何 26.2°→26.8°，A0 13.4°→13.8°）。
   **回应 Bigoni**：垂线对齐是静态站立的特征；落震瞬间水鸟的腿是斜的、
   而且在压缩中继续变斜。我们的 A0（13.4°）反而比真鸟更接近竖直。

**这份数据给不了什么**　原视频全部是 Instagram / TikTok / YouTube Shorts 的**慢动作**片段，
无标定物。按髋点位移反解的下沉速度只有 0.0–0.3 m/s，物理上不可能 —— 慢放倍率未知。
速度比 v_z/v_x 理论上对慢放免疫，但逐帧算出来从 −0.6 到 +131 全是散点
（相机未标定 + 斜向出画的透视误差压过信号）。
**只能用几何量，不能用来定 v₀ 或 v_x。**

**产出**　`fig_duong.py` → `F_duong_e28.png`（三联图，可直接进 PPT，
是目前唯一一张"外部实验数据 vs 我们的建模约定"的图）+ `E28_Cranfield对照_结果.md`

---

## C 组 · 改物理 + 重跑工厂（主体）

### 方案 C 总述（决策 ③ 已定）

主工厂只做 **Fr ∈ [0, 2]**（近垂直着陆）；同时用 P9 探针把 Fr 一路推到 **15**，
**唯一目的是定位现有接触模型在哪一档崩**。两件事共用同一套代码、同一次改动，
探针的机时是主工厂的零头。

**条件向量 5 → 6 维**：`[log10 m, v0, log10 kc, g_cap, s_max]` **+ Fr**

不是 v_x 本身。定义：

```
Fr = v_x / sqrt(g · L_ref(m))
L_ref(m) = L1_med(m) · (1 + r2_med + r3_med) = L1_med(m) · 3.850
L1_med(m) = 10^(0.479 + 0.391 · log10(m_g)) / 1000     # m，即 bioprior 的 u_L = 0
r2_med = 1.790,  r3_med = 1.060                         # bioprior 盒中点
```

`L_ref` 只依赖 m，而 m 已在条件向量里，所以 **Fr 可由条件本身算出**，
不引入设计变量、不破坏 cVAE 的条件结构。

三个好处：与质量解耦（直接服务"给固定翼和旋翼一个通解"的说法）；
以后扩范围是**拉宽区间**而不是**换参数化**，学到的表示仍然有效；
和已有的 `κ = k/(mgL)`、`g_cap` 一样是无量纲量，整套条件口径统一。

已实现：`src/stage10_v2/froude.py`（`fr_to_vx` / `vx_to_fr` / `l_ref` / `sample_fr`，
从 `bioprior.BioPrior(arm='bio')` 取 `l1_center`，导入不到时字面量兜底；
`python froude.py` 打表，`python froude.py --check` 自检往返换算与采样比例）。

**Fr 换算成 m/s（供你和别人解释时用）**

| m (kg) | L_ref (m) | √(gL_ref) | Fr=1 | **Fr=2（主工厂上界）** | Fr=4 | Fr=9 | Fr=15 |
|---|---|---|---|---|---|---|---|
| 1 | 0.173 | 1.30 | 1.3 | **2.6** | 5.2 | 11.7 | 19.5 |
| 5 | 0.324 | 1.78 | 1.8 | **3.6** | 7.1 | 16.1 | 26.8 |
| 11.07 | 0.442 | 2.08 | 2.1 | **4.2** | 8.3 | 18.8 | 31.3 |
| 25 | 0.608 | 2.44 | 2.4 | **4.9** | 9.8 | 22.0 | 36.6 |
| 45 | 0.765 | 2.74 | 2.7 | **5.5** | 11.0 | 24.7 | 41.1 |

注意 Fr = 2 对应 **2.6–5.5 m/s**，比原来一刀切的 3 m/s 更宽，而且重机器给得更多 ——
这是物理上对的：30 kg 的飞机以 3 m/s 漂移，比 1 kg 的以 3 m/s 漂移温和得多。
CW-15 / CW-40 这一类复合翼的全部着陆工况（本来就垂直落地）加侧风漂移，都在这个盒里。

---

### P9 · 足端摩擦探针（把门的那一步）　—— 拆成 P9a / P9b

**改动**　`hf_exudyn.py`：足端接触加切向库仑摩擦；初速度加水平分量 `v_x = Fr·√(gL_ref)`。

**新增指标**（P9b 要用，P9a 顺带产出，都很便宜）

| 指标 | 定义 | 作用 |
|---|---|---|
| `a_res` | max √(a_x² + a_z²) | 取代 `peak_a` 成为 `gcap` 的判据量 |
| `slip` | 足端接触点水平总位移 | 探针的发散检测 |
| `pen` | 足端最大穿透深度 | 探针的穿透检测 |
| `tip_margin` | 合力矢量落点相对支撑多边形边界的裕度 | 翻倒（nose-over）的量化 |
| **`mu_demand`** | **max ‖F_x‖ / F_z（地反力峰值比）** | **E26 ③ 那张 μ 表的实测替代，决定 q1_0 的可用下界** |
| `E_res` | 末态总能 / (初动能+重力功) − 1 | 数值守恒体检 |

> ★ **P9 的地位变了。** 原来它只是"确认新物理数值上稳"的探针；
> E26 出结果之后，它同时是 **q1_0 能不能升为第 10 维的前提**：
> 没有摩擦约束，生成器会一路跑到 28°，产出在真实地面上会打滑的设计。
> 所以 P9a 多加一条判据（下面第 5 条），并且必须在工厂重跑前完成。

#### P9a · 主工厂的准入体检（必须全过，否则停）

64 个探针设计 × 5 个质量 × **Fr ∈ {0, 0.5, 1, 1.5, 2}** × μ ∈ {0.3, 0.6}
× **q1_0 ∈ {28, 38, 50}°**（把 E26 的三档姿态带进来）

1. **回归测试（最重要）**：`Fr = 0` 时逐条复现现有 v2.3 结果，`peak_a` 相对偏差 < 0.5%。
   这一条不过，说明改动动了不该动的地方，直接回滚。
2. solver 失败率 ≤ 现有水平（待办 B3 说小质量+硬地本来就有失败，不能更糟）。
3. `pen` < 足垫厚度的 10%；`|E_res|` < 2%。
4. `slip` 有界且随 Fr 单调 —— 不出现无限滑移。
5. ★ **`mu_demand` 的实测值与 E26 的 tan(前倾角) 估计同量级**（差 <30%）。
   对得上 → 那张 μ 表可以直接用来定 q1_0 的下界；
   对不上 → 以实测为准，重画那张表再定盒子。

#### P9b · 崩溃阶梯（探针，不进工厂）

同一批设计，**Fr ∈ {2, 4, 6, 9, 12, 15}**，μ ∈ {0.3, 0.6}。
**这一步不设通过/不通过，它的产出就是"在哪一档崩"本身。**

逐档记录四个数：solver 失败率、`pen` 超标率、`|E_res|` 超标率、`tip_margin < 0` 的比例。

两种结局都有用：

- **在某一档（比如 Fr ≈ 5）开始崩** → 这是论文里一句有分量的**方法边界**：
  "本文的点接触 + 库仑摩擦模型在 Fr ≳ 5 失效，滑跑着陆需要另一套接触模型和轮系拓扑"。
  同时它直接论证了为什么滑跑必须单独立项，而不是含糊带过。
- **一路到 Fr = 15 都数值稳** → **v2.6 直接把区间拉宽重跑，参数化一行不用改**。
  这就是上面用 Fr 而不是 v_x 的价值兑现的地方。

另外无论哪种结局，`tip_margin` 随 Fr 的下降曲线本身就值得画一张图 ——
它是"为什么高速滑跑需要轮子"最直观的证据。

**机时**　P9a 与原计划同量级；P9b 是 6 档 × 2 μ × 64 设计的单次落震，
比 P9a 还小，**但要注意高 Fr 时单次积分时长会拉长到 1–3 s**，按 10× 估。

---

### C1 · 数据工厂的 Fr 采样与判据改动

**采样分布**（关键，别用均匀）

```
40% 的样本  Fr = 0  精确取零
60% 的样本  Fr ~ U(0, 2]
```

保留 40% 的 `Fr = 0`，是为了让 **v_x=0 的基线在新数据集里仍然足够密**，
这样 v2.5 的结果才能和 v2.3 逐条对比 —— 否则加了一维之后哪儿变了都说不清。

**设计向量 9 → 10 维：加 `q1_0`**

盒取 E28 实测跨度 **[28, 55]°**，但**由摩擦判据把住下界** —— 见下表新增的一行。
不加这条闸，E26 已经证明最优会一路压到 28°，而那里 μ 需要 0.70。

**判据体系跟着改**

| 原来 | 现在 |
|---|---|
| `peak_a > gcap` | **`a_res > gcap`**（合加速度） |
| — | **新增 `tip_margin < 0` 为硬闸**（翻倒直接判死） |
| — | **新增 `mu_demand > μ_地面` 为硬闸**（足端打滑判死）★ 这条是 q1_0 升维的前提 |
| `leg_stroke > smax` | 不变 |
| `struct_over` / `mass_over` | 不变（但侧向力要进 `size_segment` 的弯矩项） |
| — | `rebound` 软闸（E24 的结论接进来） |

**为什么这对第 11 页那个对比是有利的**　合加速度 = √(a_x²+a_z²)，
水平分量正比于 μ。我们的腿 μ≈0.35 → 合加速度约 +6%；
滑橇橡胶脚垫 μ≈0.8 → 约 +28%。**滑橇在这一项上吃更大的亏**，值得算清楚再写进 PPT。

**⚠ 还剩一个开放问题：μ 从哪来**
打印尼龙–混凝土有手册值（0.3–0.4），草地/湿沙**没有可引的数**。
又是一个"本工作设定"，而 A0 页的来源表里这类空格已经有两个了（κ 和 τ）。
要么找岩土文献补，要么老实标"量级估计"。

---

### C2 · 数据工厂重跑 + 重训

依赖 P9 三条判据全过。沿用 `run_v24_main.sh`，五段结构照抄 v2.3，条件维度 +1。
两个种子（规矩：训练类指标 ≥2 种子才报）。

**顺带一起处理的欠账**（都要改盒子，必须合并在同一次重跑里，不要分两次）：

| 待办 | 内容 | 是否并入 |
|---|---|---|
| E23 结果 | κ/τ 盒壁放宽 | **否** —— 贴壁率 ≤6.5%，盒子不动（已定案） |
| E24 结果 | 回弹软闸接进 `feasible_v2` | **是，且是硬要求** —— 打掉 30.2%（已定案） |
| ~~E27~~ | ~~q1_0 由垂线约束反解~~ | **撤销**（E28 + E26 几何否掉，且 E27 实测为纯亏） |
| E26 结果 | q1_0 升为第 10 维，盒 [28,55]°，**由 `mu_demand` 闸把住下界** | **是，已定案**（敏感度 69.7%） |
| B1 | σ 由残差直接算（0.0784 → 0.0819） | **是**，一行的事 |
| A1/A2 | ζ→τ 改名、n 统一 213 | **是**，无损 |
| C1（待办） | 三维条件先验 p(L1,r2,r3\|m) | **是** —— 你已拍板并入 |

---

### C3 · 下游全套重跑

`E18b 走廊` / `E20 生成走廊` / `E21 真鸟对比`，外加把 **E22 的 D/L 检验**做成常规产物。

---

## D 组 · 明确排除（防止范围蔓延）

| 项 | 来源 | 为什么不做 |
|---|---|---|
| 主动着陆 / 触地前姿态调节 | 赵 20:33 的观察 | 是控制问题，要在 exudyn 里加时变约束；且 Airbus 说工程上不安全（21:09）。**v3** |
| 猫 / 袋鼠等跨物种 | Bigoni 55:47 | 需要新的数据管线。**v3** |
| 鸟骨桁架截面 | Hsia 转述 Bigoni，33:08–33:41 | **等 E22 的 D/L 结果出来再决定值不值得** |
| 滑跑着陆（Fr ≈ 9–15） | — | **改由 P9b 探针只摸边界，不进工厂**。真做要等 v2.6，且多半要先上轮系拓扑 |

---

## 排期建议

| 天 | 内容 | 机时 |
|---|---|---|
| ~~D1~~ | ~~A 组 E22–E25~~ | ✅ 全部完成 |
| ~~D2~~ | ~~B 组 E26–E28~~ | ✅ 全部完成 |
| **D3** | **改代码**：`hf_exudyn` 加摩擦/水平初速/新指标、`physics_v2` 加三条闸、`bioprior` 加第 10 维、`factory_v2` 条件 6 维 + Fr 采样 + `KEYS_V2` 补 D_mm | 0 |
| **D4** | **P9a 准入体检**（含新增的 `mu_demand` 判据）—— 不全过就停 | 小 |
| D4.5 | **P9b 崩溃阶梯**（可并行，结果不挡工厂） | 小 |
| D5– | C 组主体：工厂 → 重训 ×2 → 下游 | **≈ 18 小时**（见下表） |

**✅ 真数拿到了**（`logs/v23_main_20260903_101829.log`）：

| 段 | 累计（分钟） | 本段耗时 |
|---|---|---|
| 0/5 P8 足端解绑验证 | 4 | **4 min** |
| 1/5 数据工厂（12 万次落震） | 66 | **62 min** |
| 2/5 训练集 | 66 | <1 min |
| 3/5 自提升闭环 ×40（seed 0） | 389 | **323 min** |
| 3/5 自提升闭环 ×40（seed 1） | 709 | **320 min** |
| 4/5 两种子比对 | 709 | <1 min |
| 5a E18b 四臂走廊 | 806 | **97 min** |
| 5b E20 生成走廊 | 821 | **15 min** |
| 5c E21 真鸟 vs 生成 | 822 | 1 min |
| **合计** | **822** | **13.7 小时** |

**自提升闭环占 78%**（643/822 分钟）。这个比例决定了以后所有"要不要并进这次重跑"的取舍
—— 边际代价基本就是训练那一段。

我之前按 v2.2 日志估的是 10–11 小时，**低了约 25%**，主要是低估了自提升闭环。



---

## 决策状态

| | 事项 | 状态 |
|---|---|---|
| ① | 着陆方向约定 | ✅ **镜像 + 运动方向箭头，两个都做** |
| ② | `q1_0` 的归宿 | 🔁 **改为升第 10 维**，盒 [28,55]°，**与 `mu_demand` 摩擦闸绑定**（E26 敏感度 69.7%） |
| ③ | `v_x` 范围 | ✅ **方案 C**：主工厂 Fr∈[0,2]（≈2.6–5.5 m/s），P9b 探针推到 Fr=15 |
| ④ | 待办 C1 三维条件先验 | ✅ **并入本次工厂重跑**（"不惜代价"） |

---

### ③ 为什么是 C（决策留痕）

**15–30 m/s 不是"同一条轴上更大的数"，是另一种物理。**

| | Fr ≲ 2（VTOL 漂移 / 侧风） | Fr ≈ 9–15（真滑跑） |
|---|---|---|
| 足端 | 静摩擦锁住，接触仍是弹簧 | **持续滑动**，库仑摩擦 + 磨损生热 |
| 仿真时长 | ~100 ms | **1–3 s，单样本贵 10–30 倍** |
| 主导失效 | 峰值过载、行程不够 | **翻倒、侧滑、脚垫磨穿** |
| 需要的构型 | 现在这条三段腿 | **多半得上轮子或低摩擦滑靴**，换拓扑不是换参数 |

所以不是"条件空间要不要拉宽"，而是**滑跑需要一套不同的判据和多半不同的拓扑**。
硬塞进同一次工厂重跑，两头都不到位，而且 `Fr=0` 的回归基线会被稀释。

**C 的做法是把这件事拆成"现在做"和"现在只探"**：主工厂老实覆盖近垂直着陆，
探针花零头机时把边界摸出来。代价是这一版论文里不能说覆盖了固定翼滑跑；
收益是能说清楚**为什么不覆盖**，而且 v2.6 要扩时参数化一行不用改。

**对外口径**：这一版是完整的**垂直/近垂直着陆通解**（旋翼 + 全部复合翼固定翼），
滑跑是明确标注的下一站，不是含糊带过的缺口。

---

### 两个悬案都结了

- **会议文字稿一直都在。** `Hisa x Davide 会议\第一次会议.txt`（48 KB，825 行，01:07:00，
  medium.en），还有 `.srt` / `.json`。转写其实在 09-10 07:00 就跑完了。
  本版引用的每个时间点都已回原文核对：12:16 / 14:30 / 15:08 / 16:15 / 17:02 / 17:26 /
  20:33 / 21:09 / 23:31 / 33:39 / 45:46 / 47:02 / 55:47 全部对得上。
  唯一要改的归属：**33 分那段"鸟骨是桁架"是 Hsia 在转述 Bigoni**，不是 Bigoni 自己说的。
- **E22 的生物侧已核实**，见 E22 条目。
- **运行时长**：脚本自带日志，见上面「排期建议」里的取法与估算表。

---

## 脚本清单（供 A100 部署）

```
src/stage10_v2/
  agroup_io.py               # A组共用读盘与判据（只依赖 numpy） [✅ 已完成]
  e22_dl_scaling.py          # A组·D/L 标度 vs 生物            [✅ 已完成，待跑]
  e22_bio_ref.json           # A组·生物侧指数（**待核实后填**） [✅ 模板已建]
  e23_box_saturation.py      # A组·盒壁饱和                    [✅ 已跑出结果]
  e24_rebound_gate.py        # A组·回弹软闸重打分              [✅ 已跑出结果]
  e25_posterior_collapse.py  # A组·后验塌缩                    [✅ 已跑出结果]
  e26_q1_sweep.py            # B组·q1_0 敏感度 + 垂线闭式解     [✅ 已完成，几何已出]
  e27_vertical_structural.py # B组·竖直化的结构收益（复用E26）  [✅ 已完成，待 E26 缓存]
  fig_duong.py               # B组·E28 对照                    [✅ 已完成]
  p9a_friction_gate.py       # C组·准入体检 Fr 0-2 + 回归测试   [新建]
  p9b_breakdown_ladder.py    # C组·崩溃阶梯 Fr 2-15            [新建]
  froude.py                  # Fr <-> v_x 换算、L_ref(m)、工厂采样 [✅ 已完成]
  physics_v2.py              # feasible_v2：回弹软闸(E24 已确认必须) + a_res / tip_margin [改]
  bioprior.py                # σ 修正、三维条件先验（盒子**不放宽**，E23 已定）[改]
  factory_v2.py              # 条件 6 维、Fr 采样(40% 取零)、KEYS_V2 加 D_mm/seg_len [改]
src/stage6_surrogate/
  hf_exudyn.py               # 切向摩擦、水平初速、a_res/slip/pen/tip_margin/E_res [改]
run_v25_main.sh              # 五段结构照抄 v2.3               [新建]
```
