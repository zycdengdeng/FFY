# 版本冻结 · v1「质量无关版」

> 冻结标签 `v1.0-mass-invariant` · 冻结脚本 `freeze_v1.sh` · 验尸脚本 `src/stage9_domain/e14_mass_probe.py`
>
> **为什么要单独冻结**:v1 存在一处**已定位、已量化、不影响主结论**的建模局限——
> 归一化响应对质量 m 严格不变。下一版(v2)将从物理层面改掉它,两版数据不可混用。
> 本文件的作用是:若干个月后回到 v1,不需要重读代码就能知道它是什么、能用来说什么、不能说什么。

---

## 一、v1 是什么

| 项 | 内容 |
|---|---|
| 物理内核 | Exudyn 平面三连杆落震(踝/膝/髋扭簧+阻尼,罚函数地面接触) |
| 设计变量 x(7 维) | L1(跗跖长 mm)、r2(胫/跗跖)、r3(股/跗跖)、κ_踝、κ_膝、κ_髋(无量纲刚度)、ζ(阻尼比) |
| 工况 c(4 维) | m∈[1,12] kg、v0∈[0.5,2.0] m/s、g_cap∈[4,15]g、s_max∈[8,40] mm |
| 生成模型 | cVAE:enc[x7+c4]→z3,dec[z3+c4]→x7,64×2,β=0.02 线性预热 |
| 训练机制 | E5 专家迭代 + 真值裁判(Exudyn)+ 三道防线(防近亲繁殖/防考题泄露/防池子发霉) |
| 评测协议 | 冻结考卷 76 题;参考前沿 = 每题 300 个新鲜 LHS 设计的最优可行峰值;gap=(生成最优−参考)/参考;gap≥100% 记崩盘 |
| 尺寸化规则 | `models.bird_size_x`:k_关节=κ·m·g·L_leg,kc=4000·m·g,cc=0.01·kc,杆件质量=5%·m |

## 二、v1 的头条结果(全部有效,见 §四)

| 实验 | 结论 | 目录 |
|---|---|---|
| E5c 专家迭代 85 轮 | **官方 5.9%**(验证集选种 seed 1),8 种子 6.8±0.9 pp,**零崩盘**;BO-9 基线 36.5%。设计池 954,240 | `outputs/gen_e5c` |
| E6 多指标(+η+回弹软门 v3) | **2.1%**,零崩盘;BO-9 33.4%,24 次崩盘 | `outputs/gen_e6c` |
| E7 同预算(各 9 次仿真) | 抛光臂 3.0% / 不抛光 5.5% / BO-9 36.5% | `outputs/gen_e7` |
| E8 结构定尺 | 腿质量 0.5–1.5% 机体质量;支柱式溢价 1.10–1.20×;SEA 49–155 J/kg;髋关节力矩为膝/踝的 3–4 倍 | `outputs/gen_e8c` |
| E11 仿生先验消融 | 对 wide:宽盒峰值好 17% 但重 4.54×(每换 1% 峰值付出约 21% 质量);对 shift:仅"位置正确"值 10 pt gap,两盒同样可造(0.7% vs 0.8% 质量分数) | `outputs/gen_abl{,_wide,_shift}` |
| E12 可行性分类器 | AUC 0.9991,ECE 0.003(注:测试仅 1,488 样本 / 20 工况;未单列近边界精度) | `outputs/gen_e12` |
| E13 环境鲁棒性 | **零结果**:gen 与 bo9 同样稳(保留率 92.8% vs 93.0%,配对中位 +0.0 pt);但 gen 的 P90(3.55g)优于 BO 的标称(4.01g),且 BO 在 5/76 题上完全失败 | `outputs/gen_e13` |
| E14 质量验尸 | 见 §三 | `outputs/gen_e14` |

## 三、已知局限:归一化响应对 m 严格不变

### 机理
运动方程中每一项力都正比于 m:

```
重力      m·g
关节扭矩  k·Δθ = κ·m·g·L_leg·Δθ
地面接触  kc·δ = 4000·m·g·δ
惯量      杆件质量 = 5%·m
```

于是 `a = F/m` 中 m 上下约掉。**结论:peak_a[g]、stroke、η 与 m 无关,只有绝对力 F_peak = m·a 正比于 m。**

### 证据(直接仿真,同一设计扫 m)

| m (kg) | 1 | 5 | 12 |
|---|---|---|---|
| peak_a | 10.191 g | 10.191 g | 10.191 g |

`e14_mass_probe.py` 把这条固化成三项可复现体检:A 解码器灵敏度(0 仿真)、B 跨质量移植交叉表、C 不变性验尸。

### 第二处独立缺陷:数据里根本没有 p(x|m)
`data_factory.py` 先 LHS 撒工况 (m, v0),再对**每个工况用同一个几何盒子**独立 LHS 撒设计。
两次采样互相独立 ⇒ 训练集中 m 与形态的互信息为 0。即使物理上 m 有影响,模型也无从学起。

**两处缺陷互相独立,必须同时修**:只修数据,得到的是"按先验背书的形态"而非"物理驱动的形态";
只修物理,先验里仍没有生物学的尺度律。

### 第三处:kc = 4000·m·g 的身份混淆
它同时扮演两个角色——(a) 防穿透的数值罚刚度;(b) E13 中被 ±30% 扰动的"真实地面刚度"。
真实地面刚度是地形属性,不随飞行器质量变化。v2 中必须拆开。

## 四、哪些结论有效 / 哪些必须收回

**有效**(都是固定物理下的相对比较,与 m 是否为死维无关):
- 专家迭代相对 BO 的样本效率优势(E5/E6/E7)
- 仿生先验消融的两个对照结论(E11 + E11b)
- 可行性分类器的可学习性(E12)
- 鲁棒性零结果与尾部优势(E13)
- 结构定尺链条与工程基准(E8)

**必须收回或改写**:
- ❌ "多工况条件生成" → v1 实为**单参数工况(v0)+ 两个约束(g_cap, s_max)**,m 维是死的
- ❌ E6 按质量分层的统计(结构性无效,应改按 v0 与约束松紧分层——数据已在 `e6_results.json`,纯切片即可)
- ❌ 任何"跨尺度泛化/大鸟小鸟共享设计律"的表述
- ⚠️ E13 中对 kc 的扰动解释需重述为"数值罚刚度的敏感性",不是"地形变化"

## 五、如何回到 v1

```bash
git checkout v1.0-mass-invariant           # 代码
tar xzf archive/v1.0-mass-invariant/core_results.tar.gz    # 小件结果
cat archive/v1.0-mass-invariant/HEADLINE.json              # 头条数字速查
# 完整性校验
awk -F'\t' 'NR>1{print $3"  "$1}' archive/v1.0-mass-invariant/MANIFEST.tsv | sha256sum -c -
```
大件(工厂 jsonl、设计池)未打包,留在 A100 原路径,清单与校验和见 `BIG_FILES.tsv`。

## 六、v1 可直接复用到 v2 的资产

| 资产 | 说明 |
|---|---|
| 专家迭代框架 `e5_loop.py` | 与物理无关,直接复用 |
| 三道防线、冻结考卷协议、多种子纪律 | 直接复用 |
| 结构定尺链 `e8_struct.py` | v2 中从"事后分析"升级为"在线可行性判据" |
| 回弹判据 v3、多指标评测 `e6_exam.py` | 直接复用 |
| 生物数据(AVONET 101 种 / Watanabe 91 种) | v2 中从"全局边界盒"升级为"质量条件先验 p(x\|m)" |
| BO-9 / CMA-ES 基线实现 | 直接复用 |
