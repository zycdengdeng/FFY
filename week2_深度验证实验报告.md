# Week 2 · 深度模型验证实验报告(VGGT-Omega 镜像验证)

> 2026-08-14 · swan01(150 帧 @25fps, 768×432)· A100 · 配套图:`outputs/swan01/stage3b_对比图.png`、`stage3b_depth.png`

## 1. 实验目的

回答一个决定技术路线的问题:**用前沿单目深度模型直接估计深度、还原鸟腿三维,走不走得通?**

设计为「镜像验证」:我们的 Stage 3 拿**骨长恒定**当约束反解深度;本实验反过来,拿 VGGT-Omega(CVPR 2026,多帧联合推理的几何基础模型)**独立**估出的深度算逐帧 3D 骨长,再用**骨长是否恒定**当免费真值给深度打分——骨头物理上不变长,故 3D 骨长的变异系数(CV)越低,深度越可信。合格线定为 CV < 10%。

## 2. 实验设置

- 模型:VGGT-Omega-1B-512(官方权重 `vggt_omega_1b_512.pt`),多帧联合推理,内参由 pose_enc 解码;
- 输入:swan01 着陆窗口 150 帧;关键点用 Stage 2 DLC 输出(kp.json);
- 采样防渗色:深度只在 SAM2 掩码内、关节邻域(±4px)取中位数——防细腿像素染上背景水面深度;
- 两种推理方式:分块(48 帧/重叠 8,块间中位数比值对齐尺度)与整段一次(排除分块接缝混淆);
- 指标:远端两根骨(ankle–mtp 跖骨段、mtp–toe 足段)3D 长度的逐帧 CV。

## 3. 结果

| 方法 | ankle–mtp CV | mtp–toe CV | 判定 |
|---|---|---|---|
| 纯 2D(基线) | 21.3% | 15.5% | — |
| **骨长恒定反解(Stage 3)** | **7.5%** | **4.7%** | ✅ 当前最优 |
| VGGT-Omega 深度(分块 48/8) | 27.7% | 27.0% | ❌ 不合格 |
| VGGT-Omega 深度(整段一次) | 33.7% | 33.6% | ❌ 不合格 |

**结论:深度直算不及格——不但远超 10% 合格线,还比不用深度的纯 2D 更差;整段推理排除了分块接缝的嫌疑(反而更差),判决坐实。**

## 4. 归因分析

1. **目标太细太小**:整根跖骨在模型的场景坐标里仅 ~0.01 单位、在 512px 输入里仅几十像素宽。关节两端深度取样各带一点噪声,骨长(对深度差敏感)就被放大摧毁——深度误差量级 ≥ 骨长信号本身。
2. **触水段深度打滑**:骨长曲线的 2 倍尖峰集中在 f70–100(触水溅水、运动模糊段),水花使深度在腿上大幅漂移。
3. **场景尺度不稳**:分块推理时块间尺度一路漂到 0.81,说明模型对该场景的全局尺度本身不稳定;但整段一次推理更差,证明问题根源不在分块,而在**腿部逐点深度精度不足**。
4. 权重加载干净(无 missing/unexpected),排除"没跑对模型"。

## 5. 结论与意义

- **负结果,但有价值**:用 CVPR 2026 最新几何基础模型实测,**细腿小目标场景的深度直算不可用**(CV 27–34%)。这不是"我们没试深度",而是"试了、量化了、不行"。
- **技术路线由此定案**:三维还原必须走**约束优化**(骨长恒定 + 生物先验,CV 5–7%),深度模型可留作粗尺度参考,不作测量。
- **写入论文的位置**:方法选型/消融一节——为什么用约束优化而非端到端深度,本实验就是定量论据。
- 工具已入库可复现:`src/stage3b_depth/depth_validate.py`(extract GPU / analyze CPU),产出 `depth.npz`、`bone3d_table.csv`、`depth_validation.json`、`stage3b_depth.png`。

## 6. 遗留与下一步

- 可选补强:把骨长误差拆成「像平面分量 vs 深度分量」,进一步量化 z 噪声占比(analyze 加个诊断即可);
- 若未来要再试深度路线,方向是:更高输入分辨率 + 腿部裁剪放大(zoom-in)推理 + 时序滤波,但预期收益有限;
- 主线继续:多物种(Duong 数据)运动学 + 预判式主动控制叙事。
