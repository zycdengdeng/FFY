# 第三周实验报告 · 从文献输入到 v2 重建

**周期**:2026-08-13(第二周汇报后)→ 2026-08-18
**一句话**:四篇文献给了我们一把尺子,用它量出课题有一处**根本性建模缺陷**——
体重在方程里被人为消掉了;我们把它验尸坐实、把旧版本完整冻结、然后一次改到位,
并用两道验收门确认新版本成立。

---

## 〇、摘要(只读这一页也够)

| 环节 | 结论 |
|---|---|
| 文献输入 | 夏老师三篇给**方法学范式**与**尺度效应**;GeoPT 给**理论定位**与**修法方向** |
| 发现的缺陷 | 三处**互相独立**:①每个力都正比于 m ⇒ 响应对 m 严格不变;②工况与设计两次独立采样 ⇒ 训练集里 m 与形态互信息为 0;③接触刚度身份混淆 |
| 验证(E14) | 解码器对 m 的灵敏度只有最强维的 **9%**;跨质量移植交叉表**每行水平**;响应沿 m 的相对极差 **2.6×10⁻⁹**(机器精度) |
| 决策 | v1 就地完整冻结(标签 `v1.0-mass-invariant`),v2 从物理层面一次改到位,不打补丁 |
| v2 验证 | G1:质量效应 **36.7–88.0%**(判据 10%);G2:8 轮 **40.8% → 3.3%**,可行率 79%→86% |
| 副产物 | 能力图显示可行域在 (地面刚度, 体重) 平面上是**一条脊**,不是单调面 |

---

## 一、起点:第二周结束时我们手里有什么

E5 专家迭代跑到 85 轮、官方 5.9%(BO-9 基线 36.5%),E6/E7/E8/E11/E12/E13
一整套配套实验,以及一次外部复核后主动降调的 7 处过度声称。

**表面上一切正常**:gap 曲线漂亮、可行率 99%、零崩盘、多种子稳定。
这一点本身后来变成了本周最重要的教训——**缺陷从任何常规指标上都看不出来**。

---

## 二、文献输入:四篇论文各给了什么

> **先把因果链说准**:缺陷不是论文"发现"的,是我们在整理 E12/E13 时自查发现的
> (分类器的可行域图在质量方向完全平直,反常)。文献的作用是三件事:
> **给它一个名字、给它一个理论位置、指出修法方向。** 没有这三样,
> 我们大概率会把它当成"影响很小"的小瑕疵按下不表——事实上第二周的报告里
> 已经写了"不立即修",本周推翻了这个判断。

### 2.1 夏老师推荐的三篇

| 文献 | 我们拿走的东西 |
|---|---|
| **Lei, Linghu, Pan, Hsia**, *Hard-to-Soft Part Ratios in Natural Crawlers Inform Hybrid Robotic Design*, **EML** 79 (2025) 102394 | ① **方法学范式**:跨物种汇编一个设计比值 → 机制特征窄带 → 关联性能 → 工程准则。我们的 214 种腿长标度律 + 91 种比例窄带天然同款,论文可平行引用为先例;② **尺度效应**:步频 ~ 体重^(−1/6)——**这条直接刺中我们**:一篇讲仿生设计比值的文章都把尺度效应写在核心结论里,而我们的"多工况"里体重是死的;③ "机器人可超越生物限制"为 E4 维度红利背书 |
| **Duffy, Biggins, ..., Hsia, ...**, *Nematic Design for Shape Morphing*, **Annu. Rev. Control Robot. Auton. Syst.** 9 (2026) | 实现路径工具箱(可编程各向异性变形、控制前沿)。**克制使用**:只进"工程实现展望",并写明时间尺度不匹配(LCE 秒级 vs 落震毫秒级,差 3 个量级),只能做触地前的慢构型调整 |
| **Pan, Liu, Lei, ..., Hsia**, *Bioinspired Mechanisms and Actuation of Soft Robotic Crawlers*, **Adv. Sci.** 12(16) 2416764 (2025) | 驱动六类对比表 = 未来半主动起落架的选型依据(毫秒级 → 电/磁驱动,气动做慢调整,光/化学出局);"机器人比自然慢 1–2 个量级"的 gap 叙事可作引言引文 |

**三篇的共同信息**:仿生不是抄形态,是抄**"按机制/按尺度配置"的能力**。
这正好是我们缺的那一块。

### 2.2 GeoPT(Wu, Guo, Li, Dou, Long, He, Matusik, **ICML** 2026)

**它做什么**:神经模拟器的物理标签极贵(单样本 6.1×10⁴ CPU·小时),但几何是
web 规模现成的。做法是给几何**合成动力学**(随机速度场),把预训练输入空间
从 G 提升到 (G, V),使之与下游任务对齐。

**与本课题的关系是"逆向但同理"**:

| | GeoPT | 本课题 |
|---|---|---|
| 什么便宜 | 几何 | **物理**(Exudyn 约 1 s/次) |
| 什么贵 | 物理标签 | **生物几何**(214 种,不可增) |
| 提升(lifting)方向 | 合成**动力学** | 合成**几何**:用标度律把 214 个离散 (m_i, x_i) 提升成连续流形 x(m, u) |

**真正有用的是它的 Remark 4.2**:

```
slice( E_V [ F_lifted(x; G, V) ] ) = F_native(x; G)
```

即:只在原生空间预训练,学到的是**沿被忽略维度取期望**的塌缩表示,信息有损。

**这就是我们那个 bug 的精确名字**:因为响应流形沿 m 是平的,我们的 cVAE 学到的是
E_m[·]——一个沿质量维塌缩的表示。它同时给了两样东西:
1. 一个**可引用的名字**(degenerate / reduced representation);
2. 一个**可引用的理由**说明为什么需要 condition-path 数据:i.i.d. 的 (x_i, c_i)
   只能学到 c 的切片期望,同一个 x 在多个 c 上的配对才监督"张成的流形"。

它的**负迁移**结论(只用几何预训练反而更差)还加固了 E11 的立论:
**先验加在错误的轴上会有害**——这正是我们的消融要测的。

---

## 三、确认的三处缺陷(互相独立,必须同时修)

| # | 缺陷 | 位置 | 后果 |
|---|---|---|---|
| **B1** 物理 | 每个力都写成 m 的正比量:k=κ·m·g·L、k_c=4000·m·g、杆件质量=5%·m | `models.bird_size_x` | a=F/m 中 m 上下约掉 ⇒ 归一化响应(峰值[g]、行程、η)对 m **严格不变** |
| **B2** 数据 | 先 LHS 撒工况 (m,v0),再对每个工况用**同一个几何盒子**独立 LHS 撒设计 | `data_factory.main` | 两次采样互相独立 ⇒ 训练集里 m 与形态的互信息 = 0,即使物理上 m 有影响也无从学起 |
| **B3** 建模 | k_c = 4000·m·g 同时扮演"防穿透的数值罚刚度"与"E13 里被扰动的真实地面刚度" | `bird_size_x` + `e13` | 真实地面刚度是地形属性,不随飞行器质量变化;E13 的"地形扰动"解释站不住 |

**为什么必须同时修**:只修 B2 得到的是"先验背书的形态"(物理上 m 仍无影响,
评审一眼看穿);只修 B1 则先验里仍然没有生物学的尺度律。这条判断决定了
"不打补丁、一次改到位"的路线。

---

## 四、验证:E14 三项验尸,把"我算过"变成证据

`src/stage9_domain/e14_mass_probe.py`,总仿真量 < 700 次。图 `outputs/gen_e14/fig_e14.png`。

### A 解码器灵敏度(**零仿真**,8 个种子)

固定同一批隐变量,单独把某一维工况从下界推到上界,量生成设计移动了多少:

| 工况维 | 相对灵敏度(中位) | 8 种子范围 | 推满时 L1 的变化 |
|---|---|---|---|
| **m (1→12 kg)** | **0.091** | 0.081 – 0.109 | **−0.13 mm** |
| v0 (0.5→2.0 m/s) | 0.875 | — | +18.39 mm |
| g_cap (4→15 g) | 0.687 | — | −17.65 mm |
| s_max (8→40 mm) | 1.000 | — | +35.74 mm |

**体重翻 12 倍带来的形态变化,只有行程要求变化的 1/268。**
8 个独立种子全落在 0.081–0.109 的窄带内,不是训练噪声的偶然。

### B 跨质量移植

为 m_i 生成并选优的设计,拿到 m_j 下实摔:**交叉表每一行完全水平**
(相对极差 10⁻⁸ 量级)。五个质量下生成的其实是同一条腿
(L1 = 101.39 / 101.69 / 101.94 / 101.74 / 101.78 mm,极差 0.54%)。
**"专属设计优势"全部 ≤ 0**(−4.58, −2.26, −3.53, −1.92, 0.00 pp):
为某个质量专门生成的设计,从来不是那个质量下最好的。

> 这一项解决了 A 项留下的疑问:解码器确实还有 0.09 的残余灵敏度,
> 但 B 项证明**那 0.13 mm 位移不携带任何信息**——不必纠结残余项的来源。

### C 不变性验尸(24 个随机设计 × 5 个质量,与模型无关)

| 量 | 沿 m 的相对极差(中位) |
|---|---|
| peak_a | 2.6 × 10⁻⁹ |
| stroke | 1.7 × 10⁻¹³ |
| η | 2.9 × 10⁻⁹ |

样例设计逐质量峰值 11.219977 g 五个质量一位不差;
F_peak/F_peak(1 kg) 实测 [1, 2, 4, 8, 12],与 ∝m 的理论**逐位吻合**。

**这不是"影响很小",是数学上恒等。**

### 这三项在论文里的用法

它们不是补充材料,而是**方法论的负对照**:一个条件维在物理上被人为消去时,
条件生成模型会在该维上学到什么(答案:什么都学不到,且**从外表完全看不出来**
——gap 曲线、可行率、崩盘数全部正常)。

---

## 五、决策与冻结

**决策**:v1 的方法论结论全部有效(专家迭代样本效率、仿生消融、可行性分类、
鲁棒性零结果——它们都是固定物理下的相对比较),失效的只有涉及跨质量泛化的表述。
因此:**v1 就地完整冻结,v2 另起,两版不可混用。**

冻结产物 `archive/v1.0-mass-invariant/`:代码快照(8 MB)、结果小件包(66 MB)、
每个文件的 sha256 清单、自动发现的头条数字索引(26 个目录 / 44 个 json)、
运行日志。配套 `版本冻结_v1_质量无关版.md` 写明它是什么、能用来说什么、
不能说什么、怎么回来。

**必须收回的表述**:"多工况条件生成"(v1 实为单参数工况 v0 + 两个约束)、
E6 按质量分层的统计(结构性无效)、任何跨尺度泛化的说法。

---

## 六、v2:一次改到位,并用两道门验收

### 6.1 P1 物理去归一化(三处)

1. **接触改为绝对介质属性并升为工况维**:`k_c` [N/m] 由地形决定、与 m 无关;
   阻尼比由 k_c 单调导出。一举三得:破坏不变性、修好 B3、把课题真正的场景
   (软介质/着水)纳入模型。
2. **杆件质量由结构定尺导出**(复用 E8 链条),不再是 5%·m。
3. **结构判据上线成在线可行性**:应力、欧拉屈曲、**几何上限 D ≤ 0.25×段长**、
   质量预算——全是不随 m 缩放的绝对量。
   > 几何上限是关键:没有它,应力和屈曲永远可以靠把管加粗化解,平方立方律咬不到人。

**刻意不改**:κ = k/(m·g·L_leg)、ζ、r2、r3 继续用无量纲形式。它们是良构的
无量纲群,改成绝对单位只会让 1 kg 与 12 kg 的取值范围失配。
**不变性应当由绝对量打破,而不是把好的参数化拆掉。**

### 6.2 P2 生物先验条件化 p(x|m)

```
log10(L1[mm]) = a + b·log10(m[g]) + σ·u_L        u_L ∈ [−2.5, +2.5]
```
AVONET 214 种水鸟拟合:a = 0.4790,**b = 0.3911**,r² = 0.754;
残差 σ 由回归恒等式反推 = 0.0784。网络输入仍是 [0,1]⁷ 且与 m 无关,
`expand/contract` 往返误差 2.2×10⁻¹⁶,**cVAE 结构一行不用改**。

**消融随之升级**:从"选盒子"改成"选标度指数",四臂在几何平均质量 3.464 kg 处
强制同点(73.0 mm),盒子形状/散布完全相同,只差一个标量斜率:

| 臂 | b | 出处 | L1@1 kg | L1@12 kg |
|---|---|---|---|---|
| **bio** | 0.391 | AVONET 实测 | 28.6–70.5 | 75.6–186.4 |
| geo | 1/3 | 几何相似 | 30.7–75.8 | 70.4–173.4 |
| elastic | 1/4 | McMahon 弹性相似 | 34.1–84.0 | 63.5–156.4 |
| none | 0 | 无尺度知识 | 46.5–114.6 | 46.5–114.6 |

每一个都是文献里有名有姓的零假设 ⇒ **"你是不是挑了个坏盒子"这类质疑结构性消失**。

### 6.3 P3 数据协议:预埋 condition-path

与 Codex 讨论后采纳(但顺序不变:先修物理再埋路径——在 v1 物理上做质量 walk
只会得到一条平线,若再加 path 损失等于**用损失函数把假象钉死**)。
工厂 25% 的块改成路径束:同一批锚点设计沿一条工况线走 5 步。
四类路径中 `m_allo`(固定 u 扫 m,腿长沿异速律同步变)与 `m_iso`(固定物理设计扫 m)
成对存在,直接给出分解:

```
dy/dm|_allo  =  ∂y/∂m|_x   +   (∂y/∂x)·(dx/dm)|_bio
  (总效应)      (m_iso:纯物理)    (差值 = 生物先验的贡献)
```

**切分单元必须是束(bid)而非工况(cid)**——一个束把同一批设计放进 5 个不同 cid,
按 cid 切就是同一个设计同时出现在训练与测试侧,静默泄漏且很难自己发现。

### 6.4 G1 门:质量真的活了

同一设计沿 m 扫,peak_a 的相对极差(判据 ≥ 10%,n=200):

| 地形 | k_c (N/m) | peak_a 沿 m 相对极差 |
|---|---|---|
| concrete | 2.0e6 | 36.7% |
| asphalt | 8.0e5 | 51.4% |
| turf | 2.0e5 | 75.1% |
| wetsand | 5.0e4 | **88.0%** |

**随地面变软单调增强**——因为破坏不变性的机制正是"绝对地面对更重的机体
相对更软"。通道归因(把 v2 的改动逐个退回 v1 写法)锁死了这一点:

| 组合 | peak_a 沿 m 相对极差 |
|---|---|
| A 全 v1 口径 | **0.00%** |
| B 接触绝对化 | 73.2% |
| C 只改杆件质量 | **0.50%** |
| D 全 v2 | 72.2% |

**A 组精确复现 0.00%**:既独立复核了 E14,也是最好的回归测试——退回 v1 参数
就得到 v1 结果,说明 v2 没夹带无关改动。
C 组只有 0.50% 说明杆件质量对**动力学**几乎无贡献,但它**不是白改**:
它的作用在**可行域**而不在响应。

### 6.5 能力图:两个质量通道方向相反

| k_c | 量 | m=1 | m=12 |
|---|---|---|---|
| 2.0e6 | 峰值 (g) | 35.1 | 23.2 |
| 2.0e6 | **腿行程** (mm) | 8.47 | 8.01 (沿 m 仅 **6.1%**) |
| 2.0e6 | 地面下陷 (mm) | 0.18 | 1.42 |
| 2.0e4 | 峰值 (g) | 10.3 | 2.8 |
| 2.0e4 | 腿行程 (mm) | 8.50 | 20.29 |

可行率(g_cap=10 g, s_max=24 mm):

| k_c | m=1 | 2 | 4 | 8 | 12 |
|---|---|---|---|---|---|
| 2.0e6 | 6.7 | 8.3 | 5.0 | 3.3 | 3.3 |
| 1.2e5 | 13.3 | 16.7 | 21.7 | **25.0** | 10.0 |
| 5.0e4 | 23.3 | 35.0 | **51.7** | 26.7 | 13.3 |

**可行域在 (k_c, m) 平面上是一条脊,不是单调面。** 太轻则地面显得硬(g 超标),
太重则陷穿行程预算,最优质量随地面变软先增后减。
这是博士方向那句"可行域及其性能边界如何随尺度与工况连续演化"的实体化。

### 6.6 G2 门:全链条跑通

范围按 pilot 数据定标后(见 §七.3),8 轮自提升:

| 轮 | gap 中位 | 可行率 | 崩盘 | 池 |
|---|---|---|---|---|
| 0 | 40.8% | 79% | 3 | 10,200 |
| 4 | 14.0% | 84% | 2 | 20,780 |
| 6 | **3.2%** | 85% | 1 | 26,070 |
| 8 | 4.9% | 86% | 1 | 31,360 |

**8 轮 40.8% → 3.3%(末 3 轮中位)。** 质量条件先验 → 路径预埋工厂 →
按束防泄漏 → 结构判据在线 → 专家迭代,整条链通。
(v1 需 85 轮到 5.9%,但两者考卷与物理都不同,**不可直接比较**,
只能说改物理没把机制搞坏。)

---

## 七、两处自我更正

> 单列一节,因为这两处恰好说明本周引入的验证机制在起作用——
> **错误是被自己的检查抓住的,不是被评审抓住的。**

### 7.1 塌陷判据 bug:把"陷进去"误判成"腿塌了"

v1 的塌陷判据 `stroke > 0.6·L_leg` 里,`stroke` 是**机体总下沉**。
硬地上它约等于腿的压缩量,所以 v1 一直没出事;软地上它 = 腿压缩 + **地面下陷**。
后果:软地形在 m=8/12 kg 上 200 个设计全军覆没,整栏数据作废。

**修法**:加足端传感器,`leg_stroke = stroke − sink`,塌陷判据与 s_max 都改用它;
并把**模型失效**(`deep_sink`:侵入超过足端半径,罚接触模型出界)与
**物理不可行**(`collapse`)彻底分家。
修完之后才看得见 §6.5 那张表里"硬地上腿行程沿 m 只变 6.1%"的干净结论——
**这两个量之前被一个数糊在一起。**

顺带修了一处 v1 遗留的不自洽:足端半径固定 8 mm,而跗跖长在 33–121 mm 之间变
(同一只脚伺候 3.7 倍的腿)。v2 改为随腿缩放。

### 7.2 "高质量端平方立方律接管"——检查顺序的伪影,已撤回

我曾据 G1 的失效计数写下"slenderness 失效从 m=1 的 0 涨到 m=12 的 67,
平方立方律接管了设计问题"。**这个结论不成立。**

原因:可行性检查**只报第一个**违反的判据(顺序 g_cap → s_max → slenderness)。
低质量端峰值高、g_cap 先失效,把这些设计的结构状态整个遮住;高质量端峰值低、
过了 g_cap 才轮得到结构判据被检查。**看上去的"换手",一半是遮蔽解除。**

独立评估(10,200 次仿真,不看检查顺序):

| 质量档 | slenderness 失效率 |
|---|---|
| 1–2 kg | **61.3%** |
| 4–8 kg | 55.6% |
| 8–12 kg | **50.2%** |

**是轻微下降,不是上升。** 而机制上说得通,并且比原说法更有价值:

```
平方立方律     载荷 ∝ m、截面需求 ∝ m^0.78 ⇒ 管径相对变粗        ← 不利
质量-地面耦合  绝对地面让重机体峰值降 51% ⇒ 载荷少涨            ← 有利
                                        两者几乎抵消,净剩 6% 有利
```

**两个质量通道在结构判据上互相抵消**,是能力图那张脊在结构侧的对应物。
代码已改为返回**全部**违反项,统计不再带顺序伪影。

(slenderness 仍是有效判别器,不是一刀切:按峰值分档,0–8 g 只淘汰 10.1%,
>40 g 淘汰 99.2%。)

### 7.3 pilot 暴露的范围失配,按数据定标

物理很稳(10,200 次仿真 ok 99.5%、solver 0.4%、deep_sink 0.2%),
但**工况范围与设计盒不匹配**:峰值中位 16.0 g,而 g_cap 只抽到 15 g 封顶
——中位设计比最宽松的要求还超标,**28% 的工况零可行**。

按 pilot 数据扫参后定标,两处都有物理依据:

1. `KC_RANGE` 上界 2e6 → **1e6**:足端不是刚体,弹性足垫与地面**串联**,
   有效接触刚度由较软者封顶。
2. `GCAP_RANGE` 上界 15 → **25 g**:[4,15] 是按 v1 那个唯一且偏软的等效地面
   标定的;v2 地形跨到 1e6,硬地上生物设计盒里**没有任何设计**能进 15 g。
   要求区间必须跟着环境区间一起放宽,否则任务本身无解。

定标后逐工况可行率中位 **19.2%**,零可行工况 **0%**。

**另有一条应写进论文的物理下限**:
```
v0 = 1.2 m/s、腿行程 8.4 mm(中位) → a = v²/(2s) = 8.7 g
要压到 5 g,需要行程 14.7 mm
```
**峰值是被行程卡住的,不是被地面卡住的**;而行程受制于 κ 的下限
(κ 是簧扭矩比重力扭矩,太小腿撑不住自重)。
"把腿调软到任意程度"在物理上不成立,这是设计空间的硬边界。

---

## 八、本周产出清单

| 类别 | 产出 |
|---|---|
| 文献 | `文献精读笔记_夏老师三篇.md`;GeoPT 全文文本版入库 |
| 诊断 | `e14_mass_probe.py`(A/B/C 三项)+ `fig_e14_mass.py` + 三联负对照图 |
| 冻结 | `freeze_v1.sh`、`tools/index_results.py`、`版本冻结_v1_质量无关版.md`、`archive/v1.0-mass-invariant/` |
| v2 物理 | `stage10_v2/physics_v2.py`(绝对接触、结构定尺导出质量、在线结构判据) |
| v2 先验 | `stage10_v2/bioprior.py`(四臂标度指数消融) |
| v2 数据 | `stage10_v2/factory_v2.py`(u 空间采样 + 四类路径束)、`dataset_v2.py`(按束防泄漏) |
| v2 循环 | `stage10_v2/e5_loop_v2.py` |
| 验收 | `e15_mass_check.py`(G1 + 通道归因 + 能力图)、`fig_v2_map.py` + 能力图 |
| 一键脚本 | `run_v2_g1.sh`、`run_v2_factory.sh`、`run_v2_e5.sh` |
| 文档 | `质量耦合改造方案_v2.md`(640 行,含五份实施记录) |

---

## 九、下一步

1. **全量重建主臂**:工厂 15–25 分钟 + 40 轮自提升约 2 小时。
2. **四臂标度指数消融**(本次改造的正题):b ∈ {0.391, 1/3, 1/4, 0},约 3 小时。
   要回答的问题是可证伪的——**实测的生物标度指数是否使可行体重范围最大?**
   是则"生物的标度律对着陆任务近乎最优";否则"生物在为涉水/觅食优化,
   着陆不是主约束"。两个方向都是结论。
3. **`m_allo` 尺度极限曲线**:沿保持异速生长的路径把 m 推到可行性失效,
   得到 m*(u_L, b)——某形态型仍能安全着陆的最大体重。这是主图候选。
4. 复跑 v1 的下游实验(E6 分层、E12、E13)于 v2 物理。
5. (记账,不动工)软垫/智能材料作为第 8 维设计变量,与 Lei et al. 的 H2S-R 概念接轨。
