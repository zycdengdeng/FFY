# 独立贡献草案:生物动态耦合——把着陆的时间维度引入仿生起落架设计

> 面向「基于仿生学与人工智能的轻量化起落架生成式设计」课题。本草案陈述相对蓝本(AESCTE-D-26-02311)的独立方法贡献,供开题报告与学位论文使用。核心一句话:**蓝本把水鸟着陆运动压缩成一个静态均值姿态来定几何;本工作恢复着陆的全程时间维度,提炼出「生物着陆律」,并据此揭示被动柔顺范式无法复现的动态签名,从而提出主动/分段柔顺的设计要求。**

---

## 1. 研究缺口:蓝本丢弃了时间维度

蓝本的运动捕捉(DeepLabCut)逐帧输出了关节角时序,但在进入设计时(§2.3.1)只取了**统计均值**作为一个静态触地姿态,再叠加文献骨长比生成几何。全程角时序——腿在下降与缓冲相里如何随时间协调弯曲——在设计阶段被丢弃。换言之,蓝本用「一张定格照片」代表了一个本质上是**动态过程**的着陆。生物腿最独特的价值(多关节的时序协调吸能)因此没有进入设计。

## 2. 方法:全程时序 → 生物着陆律

本工作把 Stage 2/3 的全程关键点序列转化为一条尺度无关的**生物着陆律**:定义腿的竖直构型高度 $h(t)=\big(y_{\text{toe}}-y_{\text{ankle}}\big)/\ell_{\text{bone}}$(骨长归一化,消除绝对尺度与相机距离),取缓冲相(最大伸展→最大压缩)得到归一化压缩进度 $\mathrm{comp}(\tau),\ \tau\in[0,1]$。这条曲线编码了「腿如何分配它的缓冲行程」,是一条可复现、物种可比、可直接作为设计参考的目标轨迹。它只需运动捕捉即可获得,不依赖绝对尺度或力测量,与课题「比例为关键、不求绝对尺度」的定位一致。

## 3. 实证发现:延迟起峰的「慢-快-慢」签名

在样例(swan01)上,生物着陆律呈明显的 **S 形**:压缩进度在 $\tau=0.25/0.5/0.75$ 处约为 $0.34/0.79/0.95$,压缩**速率**的峰值不在触地瞬间,而**延迟到 $\tau\approx0.24$**——起步慢、中段快、末段稳。这是一个具体、可测量的动态特征,而均值静态法对它完全不可见。

## 4. 分析:被动柔顺范式为何够不到——一个可证伪的结论

蓝本(及绝大多数仿生起落架)用**被动弹簧-阻尼**吸能。对一个在触地瞬间以速度 $v_0$ 释放的被动二阶系统,压缩速度 $\dot\delta$ 在 $\tau=0$ 即为最大值($v_0$),其后单调衰减——**压缩速率的峰值恒在 $\tau=0$**,与刚度是否渐进、是否两段无关。因此任何被动弹簧-阻尼**在物理上都无法复现生物律的延迟起峰**。本工作用简化冲击代理做了残差分析:即便把刚度放开为两段渐进 $K(\delta)=K_0(1+\beta_1 s+\beta_2 s^2)$ 并最优拟合,残差仍卡在约 0.057(定刚度 0.064),且最优被动解的速率峰值仍在 $\tau=0$。**延迟起峰是被动范式的物理禁区。**

## 5. 设计含义:仿生起落架需要主动或分段/预载的定时柔顺

生物的延迟起峰意味着鸟在着陆中**主动、定时**地调节腿的抗力(肌肉预张力与时序控制),而非被动地被压缩。要在起落架上复现这一动态,需要引入**主动或半主动元件**(如可调阻尼/磁流变阻尼、预载多段行程、或按行程/时间调度的刚度),而不是单纯优化被动连杆几何。这把设计问题从「选最优的固定几何」升级为「设计一条随时间/行程调度的柔顺律」。**这是蓝本的均值静态框架连提都提不出来的问题。**

## 6. 贡献陈述(三点)

1. **方法**:提出把着陆的**全程时间维度**作为设计信号的流程——从无标记运动捕捉直接提炼尺度无关的「生物着陆律」,补上蓝本丢弃的时间维度。
2. **发现**:在真实水鸟着陆上量化出「慢-快-慢、延迟起峰($\tau\approx0.24$)」的动态签名,并给出其提取与表征方法。
3. **判据**:证明该签名落在被动弹簧-阻尼范式的物理禁区(速率峰值恒在 $\tau=0$),据此提出仿生起落架应引入**主动/分段/预载的定时柔顺**——一个可证伪、可实验检验的新设计要求。

## 7. 验证与后续

在更多视频、物种(鹅/鸭/鹭)与视角上检验签名的**稳健性与物种差异**(延迟量、S 形陡度是否随形态变化);把简化冲击代理换为多体/有限元以确认残差结论;构建一个含预载/半主动元件的机构模型,检验其能否复现延迟起峰,并与被动最优做冲击指标(峰值加速度/jerk)对比。可把生物着陆律作为新字段并入 `MotionChain` 契约,使下游设计直接消费时序目标而非仅均值。

## 8. 诚实边界

当前冲击评估为简化 1-DOF 解析代理(非高保真 FE),用于揭示范式级结论与残差趋势,数值量级为示意;样例为单段单物种、二维矢状面分析;近端关节仍为文献先验(体外不可观测,见《方法学初稿》)。这些不影响第 3–5 节结论的定性成立(延迟起峰与被动禁区是运动学与二阶动力学的一般性质),但定量验证有待 FE / 实验与多样本支撑。

---

*草案 2026-08-06 · 配套演示:`src/stage5_biocouple/biocouple.py` → `stage5_biocouple.png`(左:生物律 vs 被动设计;右:延迟起峰签名)。关联:《方法学初稿》《PROJECT_STATUS》。蓝本:AESCTE-D-26-02311。*
