# 实体化清单 v1 · 从仿真到能摔的样机（2026-09-17）

> 目标：一台 5 kg 级双腿落震样机 + 一套能和仿真对表的落震测试。
> 材料口径：首台样机用 **cfnylon 打印**（这正是保留 cfnylon 口径的原因）；
> al7075 版是对标口径，实体化要 CNC，留到第二台。

---

## 一、还不存在的东西（比采购更关键，按依赖顺序）

### ① CAD 出图 —— 最大缺口
模型输出的是骨架参数（L₁/L₂/L₃、D₁/D₂/D₃、关节位置、姿态角），不是可打印几何。要补：
- 三段薄壁管实体（壁厚 0.1D ≈ 2–3 mm，SLS 可打）
- 关节叉耳 + 销轴孔 + **扭簧安装座**（簧腿卡槽）
- **行程限位**（θ_A/θ_K 的机械止挡：防止过压缩折断，也防止反向翻转）
- 足端接口（可换：橡胶垫版 / 爪版 / 以后轮版，同一接口）
- 与机身假体的连接法兰
选定设计 → 我出参数化骨架图和尺寸表，建模在 Fusion/SolidWorks 里做。

### ② 关节的物理实现 —— 最大技术风险
仿真里关节是**理想线性扭簧 + 线性阻尼**。现实里两条路：
- **A. 扭簧 + 旋转阻尼器**（推荐首选）：刚度阻尼独立可调、都是线性、和模型一一对应。
  换算（5 kg 机，κ 用 Pareto 前沿中位）：
  k = κ·m·g·L_ref → 踝 κ≈3.1 → **k ≈ 49 N·m/rad ≈ 0.85 N·m/°**；
  c = τ·k → τ≈0.025 s → **c ≈ 1.2 N·m·s/rad**。扭簧此量级可定制；
  旋转阻尼器（缓冲铰链/工业旋转阻尼）需选大扭矩型号，这是选型里最难买对的一件。
- B. 橡胶弹性体衬套：一个件同时给 k 和 c，结构简单，但非线性、参数耦合、
  批次散差大 —— 和模型对表困难。留作 B 方案。

### ③ 单关节标定 —— 必须先于整腿打印
簧和阻尼器的标称值误差 ±10–20%，直接组装整腿等于把误差全部叠进去。流程：
1. 先打**一个踝关节标定件**（两根短杆 + 关节）
2. 台架测实际 k（挂重-角度）和 c（自由摆衰减取对数减幅）
3. 实测值回填仿真重跑一遍 → 确认可行性结论不翻
4. 通过后才打整腿
这一步同时是论文的 sim-to-real 第一环，不是浪费。

### ④ 测试协议（和仿真对表的口径）
- 落高换算：v₀ = 1.2 / 1.5 / 2.0 m/s → **自由落高 73 / 115 / 204 mm**（很矮，安全）
- 三种地面对应三档 k_c：**混凝土板 / 草皮块 / 湿沙箱**（工况就齐了）
- 对表四个量：峰值过载 peak_g、腿行程、回弹高度、足端滑移
- 判据沿用仿真：g_cap、s_max、回能比 ≤5%、滑移 <0.5·L₁

---

## 二、采购清单（价格为量级估算，人民币）

### A. 打印与结构
| 项 | 规格 | 数量 | 估价 |
|---|---|---|---|
| 腿段打印外包 | SLS PA12-CF（或 MJF），三段×2 腿 + 备份 | 1 批 | 800–2000 |
| 关节标定件打印 | 同工艺，先行 | 1 套 | 150–300 |
| 销轴 | 45# 钢/304 光轴 φ5–8 mm | 6+ | 50 |
| 法兰轴承/衬套 | 对应销轴径 | 12 | 100 |
| 紧固件 | M3–M5 内六角一盒 | — | 50 |

### B. 关节（技术风险集中地）
| 项 | 规格 | 数量 | 估价 |
|---|---|---|---|
| 扭簧定制 | 按 k 表三档（踝/膝/髋），各 ±0/加强一档 | 6–12 | 300–800 |
| 旋转阻尼器 | 大扭矩型（≥1 N·m·s/rad 档），单向阻尼优先 | 6 | 300–900 |
| 关节标定砝码/挂架 | 或用现成杠铃片 | — | 0–100 |

### C. 足端（按 μ_crit 规格）
| 项 | 规格 | 数量 | 估价 |
|---|---|---|---|
| 橡胶足垫 | 邵氏 60–80A 橡胶板自裁，μ≈0.8–1.2 | 若干 | 50 |
| 爪原型 | 打印齿 + 钢针尖（形锁合验证用） | 2 | 100 |
| 单向轴承 | 自行车飞轮/工业单向轴承（B+C 轮足预研） | 2 | 100 |

### D. 机身假体与落震架
| 项 | 规格 | 数量 | 估价 |
|---|---|---|---|
| 配重 | 杠铃片 1.25/2.5 kg 若干 + 连接板 | — | 200 |
| 落震架 | 4040 铝型材龙门 + 竖直导向（或纯自由落体夹具） | 1 | 400–800 |
| 释放机构 | 电磁铁（12V 吸盘式）或快拆销 | 1 | 60 |
| 地面样件 | 混凝土板 / 草皮卷 / 沙箱（整理箱+沙） | 3 | 200 |

### E. 测量
| 项 | 规格 | 数量 | 估价 |
|---|---|---|---|
| 加速度计 | **±200 g 量程**（如 ADXL375 模块）＋单片机记录 ≥1 kHz | 2 | 150 |
| 高速影像 | 手机 240 fps 起步（行程/滑移/回弹够用）；能借 ≥1000 fps 更好 | — | 0 |
| 标定网格布/标尺 | 视频量位移用 | 1 | 30 |
| （可选）测力台 | 有实验室现成的就用，没有不单买 | — | — |

**合计量级：不含测力台约 3,000–6,000 元**，单件最贵的是打印外包和落震架。

---

## 三、执行顺序（关键路径）

```
al7075 版跑完 → 选定 1–2 个代表设计（5 kg 档）
   → ① 关节换算表 + 扭簧/阻尼器选型下单（周期最长，先动）
   → ② 踝关节标定件打印 + 台架标定 → 实测 k,c 回填仿真复核
   → ③ 整腿 CAD → 打印外包
   → ④ 落震架 + 传感搭建（与②③并行）
   → ⑤ 首摔：混凝土 v₀=1.2（73 mm，最温和工况）→ 对表四量 → 逐级加码
```

**风险提示两条**：
- 打印件疲劳：PA12-CF 反复冲击会累积损伤，备份件要够，每轮摔完目检关节根部；
- 模型缺一样东西：仿真里没有关节摩擦（库仑死区），实物一定有——
  标定时把它测出来，超过弹簧力矩 5% 就要在模型里补一项。

## 四、给厂商打印前的检查单

- [ ] 壁厚 ≥ 打印工艺下限（SLS ≥1.5 mm；我们 0.1D≈2–3 mm，OK）
- [ ] 销孔留公差（打印孔偏小，留 +0.2 mm 或后铰）
- [ ] 簧座/止挡这些小特征做加强，不悬空打印
- [ ] 一次多打一套备份（开机费占大头，边际件便宜）
- [ ] 要求厂商提供材料数据页（实际 σ/E 和我们 70 MPa/6 GPa 的假设对不对）
