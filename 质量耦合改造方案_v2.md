# v2 改造方案:让质量真正进入问题(一次改到位,不打补丁)

> 目标:把"m 是死维"从**建模缺陷**变成**已解决的设计要素**,并顺手把仿生消融
> 升级成教科书级的对照。原则:**不为了让 m 活而乱改参数化,只把写错的物理写对。**

---

## 一、诊断:三处独立缺陷

| # | 缺陷 | 位置 | 后果 |
|---|---|---|---|
| B1 | 每个力都写成 m 的正比量(k=κmgL、kc=4000mg、杆件质量=5%m) | `models.bird_size_x` | a=F/m 中 m 约掉 ⇒ 归一化响应严格不变 |
| B2 | 工况 LHS 与设计 LHS 相互独立 | `data_factory.main` | 训练集里 m 与形态互信息 = 0 ⇒ 无从学起 |
| B3 | kc 同时扮演"数值罚刚度"与"真实地面刚度" | `bird_size_x` + `e13` | E13 的地形扰动解释站不住 |

**必须同时修**:只修 B2 得到"先验背书的形态"(物理上没意义);只修 B1 得到"物理有依赖但先验里没有生物学"。

---

## 二、修法

### P1 · 物理去归一化(改 3 处,是全部改动的核心)

**P1.1 接触改为绝对介质属性,并升为工况维**

```
F_contact(δ, δ̇) = k_c·δ + c_c·δ̇ + ½·ρC_dA·δ̇²        (δ>0)
```
- `k_c` [N/m]、`c_c` [N·s/m]、`ρC_dA` [kg/m] 全部**绝对单位**,与 m 无关
- 硬地:k_c≈2e6,拖曳项=0;软地/湿滩:k_c≈1e4–1e5;**水面着陆**:k_c 小 + 拖曳项主导
- 一举三得:(a) 破坏不变性;(b) 修好 B3,E13 的 ±30% 变成真正的"地形/介质变化";
  (c) 把课题真正的场景(水鸟**着水**)纳入模型,而不是只做硬地落震

*为什么 m 就活了*:接触频率 ω_c=√(k_c/m) ∝ m^(-1/2),而腿的频率 ω_leg=√(κg/L_leg) 与 m 无关。
两者之比在 m: 1→12 kg 上变化 3.46 倍。软端更狠:静沉陷 δ=mg/k_c,k_c=1e4 时 1 kg 沉 1 mm、
12 kg 沉 12 mm——而 s_max 只有 8–40 mm。**重机器在软地上要先赔掉一半行程预算。**

**P1.2 杆件质量由结构定尺导出,不再是 5%·m**

复用 `e8_struct.py` 的链条(关节力矩 → 薄壁管 t=0.1D → 弯曲+轴向应力 + 欧拉屈曲,SF=2):
```
弯曲控制:  D ∝ (m·L)^(1/3)          杆质量 ∝ ρ·L·D² ∝ m^(2/3)·L^(5/3)
屈曲控制:  D ∝ (m·L²)^(1/4)
```
配合 L ∝ m^0.39,**腿质量分数 ∝ m^0.32**(1→12 kg 增长 2.2 倍)——这就是平方立方律。

> 实施细节:一次仿真 → 关节力矩 → 定尺 → 可行性 + 导出质量。是否需要第二遍
> (新质量回代再仿真)用 200 个设计的抽样实测决定;若二次迭代对 peak_a 的影响 < 1%,
> 就只做一遍并把残差写进报告。**不假设,实测。**

**P1.3 结构判据上线成在线可行性**

v1 里应力/屈曲只是事后分析(E8),v2 里进入可行性门:
```
可行 ⇔ peak_a ≤ g_cap  ∧  stroke ≤ s_max  ∧  回弹比 ≤ 5%
       ∧ σ_max ≤ σ_y/2  ∧  P_axial ≤ P_euler/2  ∧  F_peak ≤ F_max
```
`σ_y`、`E`、`F_max` 全是绝对量,**不随 m 缩放** ⇒ 可行域随 m 单调收缩。这是最干净的质量依赖。

**P1.4 保持不变的部分(重要)**

κ = k/(m·g·L_leg)、ζ、r2、r3 **继续用无量纲形式**。它们是良构的无量纲群,
换成绝对单位只会让 1 kg 与 12 kg 的取值范围失配,那是把问题搞坏而不是搞对。
不变性由 P1.1–P1.3 的**绝对量**打破,这才是物理上正确的破缺路径。

### P2 · 生物先验条件化 p(x|m)

把几何三维改成**相对生物学基线的 z 分数**,网络的盒子因此保持固定且与 m 无关:
```
log L1 = a + b·log m + σ_L·u_L          u_L ∈ [-2, +2]
r2     = r̄2(m) + σ_2·u_2                u_2 ∈ [-2, +2]
r3     = r̄3(m) + σ_3·u_3                u_3 ∈ [-2, +2]
```
- (a, b, σ_L) 由 AVONET 214 种水鸟拟合,现有值 **b = 0.39 ± 0.03**
- r̄2/r̄3 先检验 Watanabe 91 种里是否存在质量趋势;无趋势就退化为常数(即 v1 的边缘盒)
- 网络输入 x_net = (u_L, u_2, u_3, κ_踝, κ_膝, κ_髋, ζ),**全部与 m 无关** ⇒ cVAE 结构一行不用改
- 数据工厂改动:先采 (m, v0, 介质),再采 u,最后用 m 展开成物理 x。**约 5 行**

### P3 · 消融升级:从"选盒子"改成"选标度指数"

v1 的 bio/wide/shift 三盒消融最大的软肋是"你是不是挑了个不好的 shift 盒"。
v2 里三臂退化成**同一个 b 的四个取值**,每一个都是文献里有名有姓的零假设:

| 臂 | b | 出处 | 含义 |
|---|---|---|---|
| **bio** | 0.39 | AVONET 实测 | 水鸟腿的正向异速生长 |
| geo | 1/3 | 几何相似 | 等比例放大 |
| elastic | 1/4 | McMahon 弹性相似 | 抗屈曲优先 |
| none | 0 | = v1 的边缘盒 | 无尺度知识 |

四臂**盒子体积、形状、位置全部由同一公式生成**,只差一个标量 ⇒ "精挑细选"的质疑自动消失。
而且这个消融现在回答的是一个真问题:**AI 该不该继承生物的标度律?**

---

## 三、验收门(不通过就不烧算力)

| 门 | 判据 | 成本 |
|---|---|---|
| G0 | v1 验尸:解码器 m 相对灵敏度 < 0.35 且响应沿 m 相对极差 < 1e-6 | ~10 分钟 |
| G1 | v2 反验:同一设计扫 m,**peak_a 相对极差 ≥ 10%**;可行率随 m **单调下降** | ~20 分钟 |
| G2 | 小试:200×60 工厂 + 10 轮 1 种子,按 m 分层的 gap **出现显著差异**;e14 的 A 项里 S(m) 与 S(v0) 同量级 | ~6 小时 |
| G3 | 全量重建 | ~2–3 天 |

**G1 不过就回头调物理参数,不进 G2。** 这是"一次改到位"与"盲目重跑"的分界线。

---

## 四、工作量与顺序

| 步 | 内容 | 我的耗时 | 机器耗时 |
|---|---|---|---|
| 0 | 冻结 v1 + 跑 E14 验尸 | 已备好脚本 | 10 min |
| 1 | 改 `hf_exudyn` 接触律、`models.bird_size_x`、`data_factory` 先验、`e6_exam` 可行性门 | ~3 h | — |
| 2 | G1 反验脚本 `e15_mass_check.py` | ~0.5 h | 20 min |
| 3 | 小试(G2) | — | 6 h |
| 4 | 全量:工厂 500×120 → 85 轮 → 8 种子 → E6/E7/E8/E12/E13 重跑 + 四臂 b 消融 | — | 2–3 天 |

---

## 五、这次改造顺带解决的旧账

- E13 的 kc 扰动获得物理解释(地形/介质变化)
- E6 的质量分层重新有意义(可行域随 m 收缩,分层不再是空的)
- E8 从"事后分析"变成"在线判据",结构线与生成线合并成一条线
- E11 的"精挑细选"质疑被 b 指数消融结构性消除
- "多工况条件生成"的表述第一次名副其实:m、v0、介质刚度、约束松紧四个方向都活

---

# 附:V2 数据采集协议(采纳 condition-path,但只在数据层)

## 六、判断:采纳,顺序不变

Codex 的核心判断正确且必须执行:**先修物理,再埋路径**。补充一条更强的理由——
在 v1 物理上做质量 walk 不只是"得到一条平线没用",而是**有害**:
若再加 path-consistency 损失,等于用损失函数**显式地把 ∂x/∂m = 0 这个人为假象钉死**。
这比"白跑"严重。

### 6.1 v1 里已经做对的一件事

Codex 建议区分 c_phys=(m, v0, 介质) 与 r=(g_cap, s_max, η_min),并指出改 r 不必重仿真。
**这在 v1 中已经是既定设计**:`data_factory.py` 只扫 (m, v0)×设计并存全部指标,
g_cap/s_max 在 `build_dataset.py` 构训练对时才抽签。

由此得到一个**零成本**的机会:**要求空间的路径可以从冻结的 v1 池里直接造,一次仿真都不用跑。**
配合 v0 在 v1 物理里本来就是活维,于是——

> **可以在 v1 冻结池上先把整套 path 机制跑通:数据格式、按束切分、path 损失、
> 防泄漏评测。这件事零算力、零墙钟(我写 v2 代码时机器本来空着),
> 而且能在建 v2 之前就回答"同等仿真预算下,path 数据是否真的强于独立 LHS"。**

如果答案是否,我们就不必建 V2-PathLoss,省下的是数天。这是这轮讨论里 ROI 最高的一步。

## 七、路径协议(两遍,不是一遍)

**Codex 的 60/25/15 有一处结构性问题:边界加密无法与前两者同批产出**——
它需要先有可行性模型才知道边界在哪。所以必须分两遍:

| 遍 | 内容 | 仿真量 | 占比 |
|---|---|---|---|
| ① | 全局块:375 工况 × 120 设计 | 45,000 | 63% |
| ① | 路径束:25 束 × 120 锚点 × 5 工况 | 15,000 | 21% |
| ② | 边界加密:用 ① 训出的 FeasNet 定位 p_feas≈0.5 等值面,附近 100 工况 × 120 设计 | 12,000 | 17% |
| | **合计** | **72,000** | (v1 为 60,000,+20%) |

**路径束的构成**(每束 = 同一批 120 个锚点设计,沿一条工况线走 5 步):

| 束型 | 数量 | 走法 | 用途 |
|---|---|---|---|
| `v0` | 10 | v0: 0.6→1.6,Δ=0.25 | 最强动力学效应,边界穿越最清楚 |
| `m_allo` | 6 | **固定 u_L 走 m**,L1 按 exp(a+b·log m+σu_L) 同步变 | 保持异速生长的工况路径:追踪"同一形态型"随体重的演化 |
| `m_iso` | 4 | **固定绝对 x 走 m** | 反事实对照:纯物理的质量效应 |
| `medium` | 5 | k_c: 1e4→2e6 对数等距 | 硬地↔软地↔水面 |

`m_allo` 与 `m_iso` 成对存在,是因为它们直接给出一个**分解**:
```
dy/dm |_allo  =  ∂y/∂m |_x        +      (∂y/∂x)·(dx/dm)|_bio
   (总效应)      (m_iso:纯物理)         (差值:生物先验的贡献)
```
**这个分解本身就是结果**,不只是训练数据——它量化"生物的标度律在多大程度上补偿了体重增长"。

## 八、把 `m_allo` 从采样技巧升级成主实验

Codex 把 allometry-preserving path 当作更合理的采样方式。我认为它应该是**主图**:

沿 `m_allo` 路径把 m 一直推到可行性失效,得到
```
m*(u_L, b) = 某个形态型仍能在 g_cap 内着陆的最大体重
```
这是一条**尺度极限曲线**。结合 §二 P3 的 b 指数消融,直接提出一个可证伪的问题:

> **实测的生物标度指数 b = 0.39 是否使可行体重范围最大?**

- 若是 → "生物的标度律对着陆任务近乎最优",强结论
- 若否 → "生物在为别的功能(涉水、觅食)优化,着陆不是主约束",同样是结论

无论哪个方向都是结果,且正好是博士方向那句话的实体化:
**可行域与性能边界如何随生物尺度与工程工况连续演化。**

## 九、对 Codex 另外两条建议的修正

### 9.1 三头架构(F 性能 / C 可行 / G 生成):采纳分解,拒绝共享主干

理由是项目特有的:
1. **C 已经存在**且很好(E12 FeasNet,AUC 0.9991),不需要重新接线。
2. **E5 循环里刻意没有代理模型**——真值裁判是 Exudyn。"零在线仿真下比 BO 好 6 倍"
   是这条线最干净的卖点;挂一个共享主干的性能头 F,评审很容易读成
   "又变回代理模型优化了",而且引入 F↔G 的训练耦合,又多一个要消融的东西。
3. Codex 自己的规矩是"别一次同时改物理/先验/采样/架构/损失"——**共享主干就是架构改动**,
   按他自己的规矩,V2-Base 里不该有。

→ **三个独立模型共用同一个池子**,不共享编码器。共享主干推迟到 V2.5,且以 path 数据先证明有效为前提。

### 9.2 消融阶梯:V1-Frozen 不是一个臂

| 名称 | 是否可与其它臂同表比较 |
|---|---|
| V1-Frozen | ❌ 物理不同,只能作叙事参照("我们修了什么"),数字不能进同一张表 |
| V2-Base | ✅ 修正物理 + p_bio(x\|m),普通 LHS + cVAE |
| V2-PathData | ✅ 同物理,路径样本当普通样本用,**同仿真预算** |
| V2-PathLoss | ✅ 同数据,加显式连续性/边界损失 |

公平比较的唯一口径是**同仿真预算**下 Base vs PathData vs PathLoss。

### 9.3 切分纪律:具体会炸在哪里

Codex 的"整条路径必须整体进训练或测试"是对的。**我们这里的具体炸点**是:
E5 的"防考题泄露"防线按 **cid(工况编号)** 切分,而一个路径束把同一批设计放进 K 个不同 cid。
按 cid 切 ⇒ 同一个设计同时出现在训练与测试侧 ⇒ 静默泄漏。

→ **切分单元必须从 cid 改成 bundle_id**;非路径块的 bundle_id = 自身 cid。改动约 10 行,
但不改就会得到一个虚高且很难自己发现的数字。

## 十、与 GeoPT(Wu et al., ICML 2026)的关系

课题组另一条线在读的 GeoPT 与本项目**方向相反但共用同一条理论**,值得在论文里引一次:

| | GeoPT | 本项目 |
|---|---|---|
| 什么便宜 | 几何(web 规模现成模型) | **物理**(Exudyn 约 1 s/次) |
| 什么贵 | 物理标签(单样本 6.1×10⁴ CPU·小时) | **生物几何**(214 种,不可增) |
| 做法 | 合成**动力学** v~Unif(B),把 G 提升到 (G, V) | 合成**几何**:用拟合的标度律把 214 个离散 (m_i, x_i) 提升成连续流形 x(m, u) |
| 尺度 | 1.6 亿网格,Transformer 主干 | 7 维标量,小 MLP |

**共用的理论是它的 Remark 4.2**:
```
slice( E_V [ F_lifted(x; G, V) ] ) = F_native(x; G)
```
即:只在原生空间预训练,学到的是**沿被忽略维度取期望**的那个塌缩表示,信息有损。

这正是 v1 的病理的精确描述:因为响应流形沿 m 是平的,我们的 cVAE 学到的是 **E_m[·]**——
一个沿质量维塌缩的表示。GeoPT 因此给了我们两样东西:
1. **给自己的 bug 一个可引用的名字**(degenerate / reduced representation,沿无监督信号的条件维塌缩);
2. **给 condition-path 一个可引用的理由**:i.i.d. 的 (x_i, c_i) 只能学到 c 的切片期望,
   同一 x 在多个 c 上的配对才监督"张成的流形"而非"切片"。

另外可引用其**负迁移**结论(只用几何预训练反而变差)来加固 E11 的立论:
**先验加在错误的轴上会有害**——这正是我们的消融要测的。

不应借鉴的是它的规模与架构。7 维问题 + 秒级求解器,与 1.6 亿网格 + 6 万 CPU·小时是两个世界;
"把 GeoPT 搬到起落架上"是错的表述,"两者是同一枚硬币的两面"才是。

---

# 实施记录 ①:P1 物理改造完成,G1 门通过(2026-08-18)

代码:`src/stage10_v2/{physics_v2.py, bioprior.py, e15_mass_check.py}`(v1 代码一行未动,
两版可并存复现)。以下数字全部在沙箱实测,不是估算。

## 一、G1 反验:质量活了

12 个随机设计 × 3 地形 × 5 个质量(1/2/4/8/12 kg),每次两遍仿真。

| 地形 | k_c (N/m) | peak_a 沿 m 相对极差 | stroke | F_peak |
|---|---|---|---|---|
| concrete | 2.0e6 | **28.5%** | 4.8% | 228% |
| turf | 2.0e5 | **60.9%** | 44.9% | 192% |

判据是 ≥10%,实测 28–61%。**G1-a 通过。**

可行率(g_cap=10g, s_max=24mm)随 m 的走向:turf 30→30→20→20→10%,**单调下降,G1-b 通过。**
卡人的约束随质量换手——低质量端全是 g_cap,高质量端变成**slenderness**
(m=12 kg 时 slenderness×6 vs gcap×3):**平方立方律确实咬到人了。**

## 二、通道归因:哪一处改动在起作用

把 v2 的改动逐个退回 v1 写法(turf 地形):

| 组合 | k_c | 杆件质量 | 接触阻尼 | peak_a 沿 m 相对极差 |
|---|---|---|---|---|
| A 全 v1 口径 | 4000mg | 5%·m | 0.01k_c | **0.00%** |
| B 接触(刚度+阻尼) | 绝对 | 5%·m | 2ζ√(k_c m) | 76.9% |
| B2 只绝对化刚度 | 绝对 | 5%·m | 0.01k_c | 96.1% |
| C 只改杆件质量 | 4000mg | 定尺导出 | 0.01k_c | **0.38%** |
| D 全 v2 | 绝对 | 定尺导出 | 2ζ√(k_c m) | 77.8% |

三条读数:

1. **A 组精确复现 0.00%** —— 独立于 E14 再次确认 v1 的不变性,同时证明 v2 代码没有
   引入无关改动(退回 v1 参数就得到 v1 结果)。这是最好的回归测试。
2. **质量效应几乎全部来自接触的绝对化**。这符合物理:地面刚度是地形属性,
   v1 把它写成 4000·m·g 等于假定"地面对更重的机体更硬",这是全部人为不变性的主源。
3. **杆件质量导出对动力学几乎无贡献(0.38%)**——因为腿只占机体质量 0.2–4%。
   但它**不是白改**:它的作用在**可行域**而不在响应(见 §一 的 slenderness 换手)。
   论文里要把这句话说清楚,否则会被问"既然只有 0.38%,为什么要改"。

## 三、实测确定的几个工程细节

- **两遍质量回代必须做**:第二遍对 peak_a 的影响中位 2.2%、P90 4.1%、最大 33.6%。
  代价是仿真量翻倍,但 33.6% 的尾巴不能忽略。
- **腿质量分数**:中位 0.88%,范围 0.22–3.96%,与 E8 独立得到的 0.5–1.5% 一致。
- **软端有模型上限**:k_c=1.2e4(泥)时 m≥4 kg 全部失败——动态侵入深度超过足端球
  半径 8 mm,球-面罚接触模型本身失效,不是物理。因此 `KC_RANGE` 收窄到
  **[3e4, 2e6]**(湿沙→混凝土,约 2 个数量级)。泥/水这类大侵入介质需要
  v²拖曳+附加质量的入水模型,列为 **v2.1**,不阻塞主线。
- **几何上限是关键**:`D ≤ 0.25×段长`。没有这条,应力和屈曲永远可以靠把管加粗化解,
  平方立方律根本咬不到人。加上之后高质量端才会出现 slenderness 失效。

## 四、P2 生物先验:四臂已就位

`bioprior.py`,AVONET 拟合 log10(L1_mm) = 0.4790 + 0.3911·log10(m_g),r²=0.754,n=214。
残差 σ 由 (b, ci95, n) 的回归恒等式反推 = **0.0784**(1σ ≈ ×1.20);
条件盒取 ±2.5σ,四臂在几何平均质量 3.464 kg 处**强制同点**(73.0 mm),只差斜率:

| 臂 | b | L1@1 kg | L1@3.46 kg | L1@12 kg | 出实测全距比例 |
|---|---|---|---|---|---|
| **bio** | 0.391 | 28.6–70.5 (44.9) | 46.5–114.6 (73.0) | 75.6–186.4 (118.7) | 11.3% |
| geo | 1/3 | 30.7–75.8 (48.3) | 同上 | 70.4–173.4 (110.5) | 8.9% |
| elastic | 1/4 | 34.1–84.0 (53.5) | 同上 | 63.5–156.4 (99.6) | 5.8% |
| none | 0 | 46.5–114.6 (73.0) | 同上 | 46.5–114.6 (73.0) | 0.0% |

- 网络输入仍是 [0,1]^7 且与 m 无关,`expand/contract` 往返误差 2.2e-16;
  **cVAE 结构一行不用改**。
- "出实测全距比例"是诚实披露的诊断:大质量端样本稀疏,大残差没被采到,
  所以 ±2.5σ 会略微越出 [33,121] mm。**默认不裁剪**——裁剪会让四臂盒子形状不同,
  毁掉消融的干净性。`clip=True` 保留给 rebuttal。
- σ 的反推依赖样本 log10(体重) 的散布估计 0.35。拿到 AVONET 原始表后应直接算残差 σ
  覆盖它;但消融四臂共用同一个 σ,**σ 的误差不影响臂间比较**。

## 五、待办(按顺序)

1. **A100 上放大跑 G1**(沙箱只有 2 核,n=12 太小,concrete 那一栏还非单调):
   `--ndes 60 --workers 128`,顺便把 `--channels` 也放大到 n=40。
2. v2 数据工厂:条件向量扩成 (m, v0, k_c, ζ_c) + 事后抽签的 (g_cap, s_max),
   并按 §七 的协议预埋 25% 路径束。
3. `e5_loop` 接 `bioprior.expand`(改动集中在采样与反归一化两处)。
4. G2 小试 → 全量重建。

---

# 实施记录 ②:A100 放大验证暴露一个 bug 和一条错判据(2026-08-18)

n 从 12 提到 200 之后,G1-a 更强,但两件事必须改。

## 一、G1-a 通过,且随地面变软而增强

| 地形 | k_c (N/m) | peak_a 沿 m 相对极差 | 有效设计 |
|---|---|---|---|
| concrete | 2.0e6 | 36.3% | 198/200 |
| asphalt | 8.0e5 | 50.8% | 198/200 |
| turf | 2.0e5 | **75.8%** | 193/200 |

通道归因(n=120)与沙箱一致:A 全 v1 口径 **0.00%**、B 接触 72.7%、B2 只绝对化刚度 92.2%、
C 只改杆件质量 **0.49%**、D 全 v2 72.0%。**A 组精确复现 0.00% 是最好的回归测试。**

## 二、Bug:软地形下"腿塌了"是误判

v1 的塌陷判据 `stroke > 0.6·L_leg` 里,`stroke` 是**机体总下沉**。硬地上它约等于腿的
压缩量,所以没出事;软地上它 = 腿压缩 + **地面下陷**,于是把"陷进去"误判成"腿塌了"。
后果:wetsand 在 m=8/12 kg 上 200 个设计全军覆没(collapse×200),整栏 n=0。

**修法**:加足端传感器,把两者分开——

| 量 | 含义 | 用途 |
|---|---|---|
| `stroke` | 机体总下沉 | 报告 |
| `sink` | 地面下陷(足球心低于半径的部分) | 报告 + 模型有效性判据 |
| `leg_stroke` = stroke − sink | 起落架自身行程 | **塌陷判据与 s_max 都改用它** |

同时把"模型失效"与"物理不可行"彻底分家:`fail="deep_sink"`(侵入 > 0.9×足端半径,
罚接触模型出界)vs `fail="collapse"`(扣除下陷后腿仍被压塌,是设计不行)。
**这两件事以后绝不会再混在一起。**

## 三、顺带修一处 v1 遗留的不自洽:足端半径

v1 里 `r_foot` 固定 8 mm,而跗跖长在 33–121 mm 之间变——同一个足端伺候 3.7 倍的腿。
v2 改为 `r_foot = 0.20·L1`(蹼足,随腿缩放),`gap0` 同步缩放。修完之后模型有效范围:

| k_c (N/m) | m=1 | 2 | 4 | 8 | 12 |
|---|---|---|---|---|---|
| 2.0e6 | 5.37g / 0.0mm | 5.38 / 0.1 | 5.39 / 0.1 | 5.40 / 0.3 | 5.40 / 0.4 |
| 2.0e5 | 5.37 / 0.3 | 5.36 / 0.6 | 5.30 / 1.2 | 5.17 / 2.4 | 5.04 / 3.5 |
| 5.0e4 | 5.21 / 1.2 | 5.03 / 2.3 | 4.72 / 4.3 | 4.26 / 7.8 | **3.93 / 10.9** |
| 2.0e4 | 4.90 / 2.8 | 4.54 / 5.1 | 4.04 / 9.1 | 3.40 / 15.6 | deep_sink |

(格式:峰值 g / 地面下陷 mm。同一个设计。)
`KC_RANGE` 定为 **[5e4, 2e6]**——在 m∈[1,12] kg 全程有效。

## 四、错判据:"可行率随 m 单调下降"是我写窄了

实测:concrete 单调↓(11.0→5.0%),turf **非单调**(14→16→19→18.5→16.5%),
wetsand 更是先升后崩。原因在上表里看得很清楚——**两个质量通道方向相反**:

```
地面变软 → 重机体被垫得更软(峰值 5.21g → 3.93g,降 25%)   ← 有利
         → 但陷得更深(1.2mm → 10.9mm,吃掉行程预算)        ← 不利
```

于是**可行域在质量方向上出现内部最优**:太轻则地面显得硬(g 超标),太重则陷穿行程预算。
这不是噪声,是阻抗匹配。

**判据据此改写**(G1-b 原文作废):

> 旧:可行率随 m 单调下降
> 新:**可行域随 m 显著改变形状,且卡人的约束发生换手**

实测的换手极其干净(turf,n=200):

| m (kg) | 1 | 2 | 4 | 8 | 12 |
|---|---|---|---|---|---|
| g_cap 失效 | 172 | 167 | 156 | 128 | **87** |
| slenderness 失效 | 0 | 0 | 6 | 32 | **77** |

**低质量端是"加速度太大",高质量端是"管子粗到不成其为杆件"。** 平方立方律接管了设计问题。
这比原来那条单调判据更强,也更是我们想讲的故事——可行域的**演化**,不是可行域的**平移**。

## 五、新增:kc × m 能力图(`--map`)

上面那张表升格成正式产物 `e15_map.json`:6 档地面刚度 × 质量 × 设计,
报峰值/下陷/腿行程的中位与沿 m 的相对极差,以及可行率与"内部最优在哪个质量"。
这就是方案 §五 要的两张主图之一,现在有数据了。

---

# 实施记录 ③:修完 bug 后的定版 G1 结果 + 能力图(2026-08-18)

## 一、G1 定版通过

| 地形 | k_c (N/m) | peak_a 沿 m 相对极差 | stroke | 有效设计 |
|---|---|---|---|---|
| concrete | 2.0e6 | 36.7% | 7.2% | 198/200 |
| asphalt | 8.0e5 | 51.4% | 21.6% | 198/200 |
| turf | 2.0e5 | 75.1% | 45.6% | 179/200 |
| wetsand | 5.0e4 | **88.0%** | 60.2% | 101/200 |

**质量效应随地面变软单调增强**——因为破坏不变性的机制正是"绝对地面对更重的机体
相对更软"。通道归因(n=120)锁死了这一点:A 全 v1 口径 **0.00%**、B 接触 73.2%、
B2 只绝对化刚度 103.6%、C 只改杆件质量 **0.50%**、D 全 v2 72.2%。

约束换手(每格 200 个随机设计里的失效数):

| 地形 | m=1 | 2 | 4 | 8 | 12 |
|---|---|---|---|---|---|
| concrete | gcap 179 | gcap 179 | gcap 178, slen 6 | gcap 175, slen 12 | gcap 174, **slen 18** |
| turf | gcap 172 | gcap 170 | gcap 161, slen 7 | gcap 134, slen 28 | gcap 82, **slen 67** |
| wetsand | gcap 154 | gcap 120 | **slen 58**, sink 12 | slen 55, sink 55 | **sink 99, smax 50** |

低质量端一律是"加速度太大",高质量端换成"管子粗到不成其为杆件"。**平方立方律接管了设计问题。**

## 二、能力图:两个质量通道方向相反(`fig_v2_map.png`)

峰值加速度中位(g),60 个随机设计:

| k_c | m=1 | 2 | 4 | 8 | 12 | 沿 m 极差 |
|---|---|---|---|---|---|---|
| 2.0e6 | 35.1 | 32.1 | 28.9 | 25.4 | 23.2 | 41% |
| 3.0e5 | 24.7 | 21.1 | 17.5 | 13.8 | 12.0 | 73% |
| 5.0e4 | 14.3 | 11.1 | 8.3 | 6.0 | 5.0 | 112% |
| 2.0e4 | 10.3 | 7.6 | 5.4 | 3.8 | 2.8 | **140%** |

**行程的两个来源必须分开看**(这正是那个 bug 的价值所在):

| k_c | 量 | m=1 | 12 | 沿 m 极差 |
|---|---|---|---|---|
| 2.0e6 | 腿行程 | 8.47 | 8.01 | **6.1%** |
| 2.0e6 | 地面下陷 | 0.18 | 1.42 | 211% |
| 2.0e4 | 腿行程 | 8.50 | 20.29 | 114% |
| 2.0e4 | 地面下陷 | 4.52 | 19.20 | 142% |

硬地上**起落架自身行程几乎与质量无关(6%)**——质量效应全在地面下陷里;
软地上两者一起爆。若不分开,就会把"陷进去"记成"腿塌了"(v1 判据的原 bug)。

可行率(%,g_cap=10g, s_max=24mm):

| k_c | m=1 | 2 | 4 | 8 | 12 |
|---|---|---|---|---|---|
| 2.0e6 | 6.7 | 8.3 | 5.0 | 3.3 | 3.3 |
| 1.2e5 | 13.3 | 16.7 | 21.7 | **25.0** | 10.0 |
| 5.0e4 | 23.3 | 35.0 | **51.7** | 26.7 | 13.3 |
| 2.0e4 | 40.0 | **73.3** | 43.3 | 0.0 | 0.0 |

**可行域在 (k_c, m) 平面上是一条脊,不是一个单调面。** 太轻则地面显得硬(g 超标),
太重则陷穿行程预算;最优质量随地面变软而**先增后减**。这就是"可行域及其性能边界
如何随尺度与工况连续演化"的实体。

## 三、必须诚实记下的两点

1. **硬地上随机设计几乎全不可行**(可行率 3–8%,峰值中位 23–35 g vs g_cap 4–15 g)。
   原因是设计盒(κ 范围)承袭自 v1,而 v1 的地面等效 k_c 只有 4000·m·g
   (m=5 时约 2e5),从没见过 2e6 这么硬的地。**这不一定是问题**——生成器的职责
   就是在稀疏可行区里找解,可行率低正是任务难度的体现。但 G2 必须检查:
   若某些工况下 120 个 LHS 设计**一个可行的都没有**,该工况就没有参考前沿,
   必须像 v1 的 `build_union_refs` 那样整题剔除,否则考卷会被污染。
2. **wetsand 在 m≥8 的失效以 `deep_sink` 为主**,那是**罚接触模型出界**而非物理。
   `KC_RANGE` 已定在 [5e4, 2e6],工厂不会采到 2e4;能力图里保留 2e4 那一行只作
   趋势展示,**不得用于任何定量结论**。

---

# 实施记录 ④:pilot 数据推翻了实施记录 ③ 的一个说法(2026-08-18)

## 一、更正:"高质量端平方立方律接管"是**检查顺序的伪影**

实施记录 ③ 里我写:turf 上 slenderness 失效从 m=1 的 0 涨到 m=12 的 67,结论是
"平方立方律接管了设计问题"。**这个结论不成立,现予撤回。**

原因:`feasible_v2` 当时**只报第一个**违反的判据,顺序是 g_cap → s_max → slenderness。
低质量端峰值高、g_cap 先失效,把这些设计的结构状态整个遮住;高质量端峰值低、
过了 g_cap,才轮得到结构判据被检查。**看上去的"换手",一半是遮蔽解除。**

pilot 的 10,200 次仿真里**独立**评估结构判据(不看检查顺序):

| 质量档 | n | slenderness 失效率 |
|---|---|---|
| 1–2 kg | 3109 | **61.3%** |
| 2–4 kg | 2812 | 59.7% |
| 4–8 kg | 2799 | 55.6% |
| 8–12 kg | 1428 | **50.2%** |

**是轻微下降,不是上升。** 机制上说得通,而且比原来的说法更有意思:

```
平方立方律      载荷 ∝ m,截面 ∝ L² ∝ m^0.78  ⇒ 管径需求相对变粗   ← 不利
质量-地面耦合  绝对地面对重机体相对更软,峰值降 51%(24.7→12.0 g) ⇒ 载荷少涨   ← 有利
                                                        两者几乎抵消,净剩 6% 有利
```

**两个质量通道在结构判据上互相抵消。** 这是新结论,也是能力图那张脊的结构侧对应物。

**代码已修**:`feasible_v2` 改为返回**全部**违反项而非第一个,统计不再带顺序伪影。

## 二、slenderness 仍然是有效判别器,不是一刀切

按峰值分档(独立评估):

| 峰值 | n | slenderness 失效率 |
|---|---|---|
| 0–8 g | 1798 | 10.1% |
| 8–15 g | 2913 | 40.0% |
| 15–25 g | 2755 | 72.2% |
| 25–40 g | 1765 | 91.4% |
| >40 g | 917 | 99.2% |

低载荷设计基本都能造,高载荷设计基本造不出来——正是想要的行为。
(材料敏感性:管径 ∝ σ_y^(−1/3),换 al7075 管径 ×0.52、ti64 ×0.43,
slenderness 几乎不再卡人。默认沿用 E8 的 cfnylon,消融四臂同材料,可比。)

## 三、pilot 暴露的范围失配,已按数据定标

物理很稳:10,200 次仿真里 **ok 99.5%**、solver 0.4%、deep_sink 0.2%。
问题在**工况范围与设计盒不匹配**:峰值中位 16.0 g,而 g_cap 只抽到 15 g 封顶
—— 中位设计比最宽松的要求还超标。逐工况可行率中位 6.7%,**28% 的工况零可行**。

按 pilot 数据扫参(逐工况可行率中位 / 零可行工况占比):

| g_cap 上限 | k_c≤2e6 | k_c≤1e6 | k_c≤5e5 | k_c≤3e5 |
|---|---|---|---|---|
| 15 g | 6.4% / 4% | 8.6% / 3% | 10.0% / 1% | 18.1% / 0% |
| 20 g | 11.5% / 0% | 13.9% / 0% | 18.9% / 0% | 28.3% / 0% |
| **25 g** | 15.7% / 0% | **19.2% / 0%** | 23.3% / 0% | 35.3% / 0% |

**定标结果(两处都有物理依据,不是为了让数字好看):**

1. `KC_RANGE` 上界 2e6 → **1e6**。依据:足端不是刚体,弹性足垫与地面**串联**,
   有效接触刚度由较软者封顶;2e6 N/m 经一个 20 mm 半径的足垫传递并不现实。
2. `GCAP_RANGE` 上界 15 → **25 g**。依据:[4,15] 是按 v1 那个唯一且偏软的等效
   地面(4000·m·g,m=5 时约 2e5)标定的;v2 地形跨到 1e6,硬地上生物设计盒里
   **没有任何设计**能进 15 g。要求区间必须跟着环境区间一起放宽,否则任务本身无解。

定标后:逐工况可行率中位 19.2%,**零可行工况 0%**。

## 四、还有一条不能忽视的物理下限

```
v0 = 1.2 m/s,腿行程 8.4 mm(中位) → a = v²/(2s) = 8.7 g
要压到 5 g,需要行程 14.7 mm
```
**峰值是被行程卡住的,不是被地面卡住的。** 而行程又受制于 κ 的下限——
κ = k/(m·g·L_leg) 是簧扭矩比重力扭矩,κ 太小腿撑不住自重。
所以"把腿调软到任意程度"在物理上不成立,这是设计空间的硬边界,应写进论文。

---

# 实施记录 ⑤:G2 小试通过,v2 全链条跑通(2026-08-18)

范围定标后重建 pilot 池,训练对可行率 **14.9% → 24.0%**,零可行工况 0%。
随后跑 8 轮自提升(`outputs/v2_e5_bio_pilot`):

| 轮 | gap 中位 | gap 均值 | 可行率 | 覆盖 | 崩盘 | 训练对 | 池 |
|---|---|---|---|---|---|---|---|
| 0 | 40.8% | 51.8% | 79% | 49% | 3 | 3211 | 10,200 |
| 2 | 18.5% | 35.5% | 86% | 36% | 2 | 3942 | 15,490 |
| 4 | 14.0% | 25.9% | 84% | 30% | 2 | 4130 | 20,780 |
| 6 | **3.2%** | 12.5% | 85% | 35% | 1 | 4244 | 26,070 |
| 8 | 4.9% | 10.6% | 86% | 29% | 1 | 4342 | 31,360 |

**40.8% → 3.3%(末 3 轮中位),8 轮。可行率 79%→86%,崩盘 3→1。**
v2 全链条(质量条件先验 → 路径预埋工厂 → 按束防泄漏 → 结构判据在线 → 专家迭代)通。

对比 v1 需要 85 轮才到 5.9%——但两者考卷与物理都不同,**不可直接比较**,
只能说 v2 的机制没有因为改物理而失效。

## 一、小试暴露的两个考卷质量问题(已修)

1. **考卷只有 20 题,而 `--nexam 40`**。不是无解题太多,是**测试块本身就只有 20 个**:
   pilot 只有 130 束,15% 测试 = 19 束,多数是独立块(1 块/束)。
   已加打印,把"测试块总数 / 抽题数 / 无解剔除 / 参考过薄剔除"分开报,
   以后一眼能分清"题源不足"和"死题太多"。
   全量规模下:400 束 → 测试束 60 → **测试块 72**,故 `--nexam` 默认改为 72。
2. **2/20 的题只有 1–2 个可行参考设计**(300 个 LHS 里)。参考前沿是"最小值",
   由 1 个样本决定的最小值不可靠,这种题的 gap 是噪声。
   已加 `--min-feas-ref`(默认 5):参考可行数少于 5 的题整题剔除。

## 二、可以观察到的一个现象:覆盖率随轮次下降

覆盖 49% → 29%。生成器在收敛过程中从"铺开"转向"开采",这与 v1 的观察一致。
不是错误,但如果论文要强调"生成设计族而非单点",需要在报告里如实写明
**这是精英化的代价**,并给出覆盖-性能的权衡曲线。

## 三、全量运行参数(据小试定标)

```bash
bash run_v2_factory.sh                       # 主臂 bio 全量池, 15–25 分钟
bash run_v2_e5.sh                            # 40 轮, 约 2 小时
ARMS="geo elastic none" ROUNDS=20 bash run_v2_factory.sh && \
ARMS="geo elastic none" ROUNDS=20 bash run_v2_e5.sh    # 三个消融臂, 约 3 小时
```
轮数从 85 降到 40:小试第 6 轮就到底了,而且**循环可续跑**
(`trajectory.json` + `pool_increments.jsonl`),末 5 轮没平再加轮即可,不必一次押满。
