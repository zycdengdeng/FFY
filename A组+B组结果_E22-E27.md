# A 组 + B 组结果 · E22–E27

四个脚本都写完了，放在 `src/stage10_v2/`。**E23 / E24 / E25 已经跑完并出结果**，
E22 需要你本地跑一次重评（下面第 4 节）。

共用模块 `agroup_io.py` 刻意**不 import physics_v2 / factory_v2** —— 那条链会拉进 exudyn，
而 A 组是纯后处理，应该在任何一台只有 numpy 的机器上都能跑。常量在 `selfcheck()` 里
与真模块比对，防止两边悄悄漂开。

---

## 1 · E24 回弹软闸 —— 🔴 **训练目标确实是错的，必须改**

```
样本总数             240,000
现判据下可行         109,040  (45.4%)
其中回能比 > 5%       32,940  (占可行的 30.2%)   <<<
进训练集的前沿点      13,095，其中被打掉 3,099 (23.7%)

可行样本回能比分位：P50=0.000  P75=0.068  P90=0.138  P95=0.185  P99=0.289
足端离地(n_bounce≥1) 占可行样本 71.0%
```

**30.2% > 20% 的阈值。** 现有的"可行"里有三成会弹起来，进训练集的 Pareto 前沿点里有
23.7% 会弹。也就是说，**cVAE 现在学的目标里混了近四分之一会弹的设计**。
这条必须在 v2.5 重训前接进 `feasible_v2`，不然模型学的是错的东西。

另外 71% 的可行样本有足端离地 —— 按《回弹判据调研》v3 只记录不判死，但这个数太大，
值得在论文里单列一句（"接触不是单次的"这件事本身就有信息）。

**从 (b) 图看，打掉比例随质量单调上升**：2 kg 处约 22%，30 kg 处约 37%。
重机器更容易弹，这和"腿相对更细、储能占比更高"是一致的。

---

## 2 · E23 盒壁饱和 —— 待办 C4 可以关掉，但要换个说法

**全部可行设计**在盒里基本均匀，九维的贴壁率都在 5% 基线附近（最高 6.5%）：

| 维 | 下贴壁% | 上贴壁% | 全体中位 u | **前沿中位 u** |
|---|---|---|---|---|
| L1 (u_L) | 2.7 | 6.5 | 0.582 | **0.650** |
| κ 踝 | 3.6 | 4.9 | 0.506 | **0.321** |
| τ (ζ) | 4.9 | 4.8 | 0.486 | **0.206** |
| θ_A | 5.1 | 4.0 | 0.474 | **0.344** |
| 其余五维 | ~5 | ~5 | ~0.50 | ~0.47 |

**所以要把两件事分开说**：

- 盒子**没有**把可行域切掉 —— 全体可行设计不堆在壁上，v2.5 不必放宽；
- 但 Pareto 前沿**系统性地压在一侧** —— 待办 C4 用 5 个设计看到的就是这个，
  它没看错，只是把"最优解偏爱那一侧"说成了"盒子不够大"。

换算成物理量，落震最优偏爱：

| | 前沿中位 | 四分位 | 对照 |
|---|---|---|---|
| τ 松弛时间 | **0.025 s** | 0.014–0.042 | 盒 0.005–0.10 |
| κ 踝 | **3.1** | 1.9–4.9 | 盒 0.75–8.0 |
| θ_A 踝角 | **129°** | 121–141° | **真鸟落水实测 140° ± 14°**（E28） |
| L1 | **比异速中位长 14%** | −7% ~ +36% | — |

最后两行接得上 E28：**生成器的最优踝角比真鸟落水时更屈（129° vs 140°），
腿也比异速中位长 14%** —— 都在生物学跨度内，但方向是系统性的。这是可以写进论文的一句话。

---

## 3 · E25 后验塌缩 —— **待办 E1 的结论不复现**

```
① 编码器：z0 KL=0.220  z1 KL=0.224  z2 KL=0.599   总 KL 1.043 nat
   死维 0/3，active units 3/3
② 同条件采 216 次：σ_z 中位 = 盒宽的 5.9%
③ σ_z / σ_data 中位 = 0.21  —— 只保留了数据自身多样性的 21%
④ 复现 E1 口径（物理 x 空间 CV）：9 维中位 4.65%，E1 报的是 0.21% —— 差 22 倍
```

**没有塌缩。** E1 测的是 v2 的 7 维模型，此后换过先验、加过姿态两维、重训过多轮，
那条待办可以关掉 —— 但要把这个数写进报告，别让旧结论留在文档里。

不过口径**还是建议保守**，理由从"塌缩"换成"覆盖不足"：多样性只有数据自身的 21%，
模型在往条件均值收（cVAE 的常态）。所以：

> ✓ 给定工况，模型给出满足验收条件的仿生设计；采样能给出若干变体，
> 但不宣称覆盖了全部可行解，也不宣称其中有最优解。
> —— 与赵老师原话一致：*it is one solution, not necessarily the best one*。

---

## 4 · E22 D/L 标度 —— 脚本写好了，等你跑 + 等核实

**两件事卡着：**

**① 它不是零成本。** 工厂只存了 `KEYS_V2` 那 16 个量，**D 不在里面**
（`eval_v2` 其实返回了 `D_mm`，只是 factory 没落盘）。所以要对抽样的设计重跑一遍
`eval_v2`。本地多进程就能跑完，不用上 A100，但不是"零机时"。
→ 顺手把 `D_mm` / `seg_len` 加进 v2.5 的 `KEYS_V2`，下次这条就真免费了。

```
python src/stage10_v2/e22_dl_scaling.py --n 3000 --workers 8      # 抽样重评，可中断续跑
python src/stage10_v2/e22_dl_scaling.py --analyze-only --fig      # 只分析，秒出
```

**② 生物侧的指数我没写死在代码里。** 会议里那组 −0.38 / −0.57 / −0.12 / +0.02
**至今没核对过原始帧**，所以脚本要求用 `e22_bio_ref.json` 传进去，不填就跳过比较。

核帧时请顺便确认两件事：

- **横轴到底是段长还是体重**；
- **纵轴是 D 还是 D/L**。那几个数是**负的**，而 `log D ~ log L` 在任何合理力学下都必然为正
  （粗的骨头总是更长），所以它们多半是**细长比口径** `log(D/L) ~ α·log L`。
  脚本三种口径都算，核实后对上哪一种都能直接比。

脚本会先做**夹逼诊断**：D 被 `D_MIN = 4 mm` 和 `D_MAX_RATIO = 0.25` 顶住的比例。
超过 20% 直接判这条结论不能要 —— 那时 D 由几何上下界决定，不是力学，标度是假的。

---

---

## 5 · E26 q1_0 敏感度 —— 几何部分已出，扫描待跑

`e26_q1_sweep.py --geom-only` 是零仿真的，**不需要 exudyn，秒出**。它对每个 cVAE 设计
**闭式解**出让髋恰好落在足正上方的 q1_0*：

> a₂ = a₁+(π−θ_A)，a₃ = a₂−(π−θ_K)
> Δx = C·cos a₁ − S·sin a₁ = 0 → **a₁\* = atan2(C, S)**
> 其中 C = l₁ + l₂cosφ₂ + l₃cosφ₃，S = l₂sinφ₂ + l₃sinφ₃

144 条腿（6 工况 × 24 设计）的结果：

| | 值 |
|---|---|
| q1_0\*（垂线对齐所需） | **中位 63.1°**，跨度 60.5–86.8° |
| 模型常数 | 50° |
| 真鸟实测（E28） | 42.6°，跨度 27.6–54.8° |

**垂线对齐要求的姿态整体落在真鸟实测跨度之外**，比实测均值陡 21° ——
这是继 E28 的前倾角之后，**第二条独立证据**说明"髋在足正上方"不是生物学事实。
两条证据的来源完全不同：一条来自 12 段真实落水视频，一条来自我们自己生成的设计的几何。

各档 q1_0 对应的髋-足前倾角（中位）：

```
q1_0 = 28.0°  →  前倾 35.1°
q1_0 = 42.6°  →  前倾 20.5°   ← 真鸟实测的跗跖骨倾角
q1_0 = 50.0°  →  前倾 13.1°   ← 模型常数；真鸟实测前倾 30.5°±6.4°
q1_0 = 62.0°  →  前倾  1.2°   ← 垂线档
```

**一个要解释的细节**：生成设计在 50° 处只前倾 13.1°，**比真鸟（30.5°）更接近竖直**。
原因是它的踝更屈 —— θ_A 中位 **127.3°**，真鸟是 140°±14°。同样的 q1_0 下整条腿更直。
这和 E23 测到的前沿偏置（θ_A 前沿中位 129°）是同一件事，两条独立路径互相印证。

**扫描部分**（6 工况 × 24 设计 × 11 档 ≈ 1,600 次落震，本地多进程）：

```
python src/stage10_v2/e26_q1_sweep.py --ndes 24 --workers 8
python src/stage10_v2/e26_q1_sweep.py --analyze-only --fig
```

判据：peak_g 跨生物跨度（28–55°）变化 <10% → 永久常数；>25% → 升为第 10 维。

---

## 6 · E27 竖直化的结构收益 —— 脚本就绪，零新增仿真

**这条的性质变过一次。** 原方案是"由垂线约束反解 q1_0，给 bioprior 降一维"，
理由是生物学。E28 + E26 几何把这个理由否掉了，**那条方案撤销**。

剩下的是 Bigoni 那句话的**纯力学**内核：腿越竖直 → 越接近轴压而非弯曲 →
由弯矩定的 D 越小 → 腿越轻。这和生物无关，而且 E26 的缓存里
每一档都记了 `D_mm` / `leg_mass` / `M_Nm` / `F_peak`，**直接算，不用再跑仿真**。

用与 `e8_struct.size_segment` 同式的系数拆应力：
σ_bend = M/(0.05796·D³)，σ_ax = F/(0.2827·D²)。

```
python src/stage10_v2/e27_vertical_structural.py --fig
```

判据：减重 >15% → 写成**显式的性能-仿生取舍**，明说我们选了仿生一侧
（这比"我们就是仿生"有说服力得多）；减重不显著 → 彻底关掉。

**⚠ 一个必须写在结论旁边的边界**：现在机体被 `CreatePrismaticJoint(axis=[0,0,1])`
锁成只能竖直平动，髋-足水平偏移的力矩由约束反力承担。所以本实验测到的
"倾斜的弯矩代价"在当前模型里是真的；但 v2.5 加了 v_x 之后受力图会变，
**这条结论要重新测一次**。

---

## 对实验计划的影响

| 实验 | 状态 | 结果 |
|---|---|---|
| E22 | 脚本就绪，待跑 + 待核实 | — |
| E23 | ✅ 完成 | 盒子不动；前沿偏置写进论文；待办 C4 关闭 |
| E24 | ✅ 完成 | **软闸必须接**（打掉 30.2%）；`feasible_v2` 要改 |
| E25 | ✅ 完成 | 未塌缩；待办 E1 关闭；口径改为"覆盖不足" |
| E26 | 几何 ✅ / 扫描待跑 | 垂线要 63°，在实测跨度外 |
| E27 | 脚本就绪，等 E26 缓存 | — |
| E28 | ✅ 完成（早前） | q1_0 = 50° 保持常数 |

**C 组的盒子已经定了**：κ/τ/角度盒**不动**，`feasible_v2` **加回弹软闸**，
条件向量第 6 维放 Fr ∈ [0, 2]。

**要本地跑的只剩两件**：E22 的重评（约半小时）和 E26 的扫描（约 1,600 次落震）。
两件都不占 A100。跑完 C 组就可以上机了。
